{
  "name": "@ulalax/zlink",
  "version": "5.0.5",
  "description": "zlink Node.js bindings \u2014 high-performance async messaging library",
  "license": "MPL-2.0",
  "author": "ulalax",
  "repository": {
    "type": "git",
    "url": "https://github.com/ulala-x/zlink.git",
    "directory": "bindings/node"
  },
  "homepage": "https://github.com/ulala-x/zlink",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist/**/*",
    "prebuilds/**/*",
    "native/**/*",
    "binding.gyp",
    "README.md"
  ],
  "gypfile": true,
  "scripts": {
    "clean": "rm -rf dist dist-tools",
    "build": "npm run clean && tsc -p tsconfig.json && tsc -p tsconfig.tools.json && ln -sfn ../dist dist-tools/dist",
    "rebuild-native": "node-gyp rebuild",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "prepare": "npm run build",
    "pretest": "npm run build && npm run typecheck && npm run rebuild-native",
    "test": "./run_tests.sh",
    "presamples": "npm run build && npm run rebuild-native",
    "samples": "./run_samples.sh"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
