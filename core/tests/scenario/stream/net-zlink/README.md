# .NET Zlink STREAM Scenario Runner

Binary:

- `core/tests/scenario/stream/net-zlink/bin/Release/net8.0/StreamNetZlinkScenario.dll`

Batch script:

- `core/tests/scenario/stream/net-zlink/run_stream_scenarios.sh`

Notes:

- Current transport support is `tcp`.
- Uses Zlink .NET binding `SocketType.Stream` for both client/server.
