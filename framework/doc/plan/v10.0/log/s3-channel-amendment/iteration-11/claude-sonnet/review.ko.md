## 검토 결과

시작·종료 hash(`00d6cbef...`, `7d428591...`) 모두 일치, `scripts/verify-framework-doc-contracts.sh` CLEAN, `git diff --check` 통과. Iteration 11 scope 96개 파일 전체와 `AGENTS.md`, 두 원칙 문서를 읽고 5언어 exact interface·공통 spec·server spec 체인·7개 sample topology·E2E(Config 1·3·5·11·12)·feature-map·testdata·plan 문서를 교차 검증했다.

Finding 2건을 발견했다.

[계약][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:215 — MeshNode 강제 종료 reason 철자가 공통 spec·.NET과 다른 값으로 5언어 중 3언어에서 반복된다 — 공통 spec(framework/doc/framework/spec/server/54-graceful-drain-handoff.ko.md:43)과 .NET exact interface(framework/doc/framework/spec/server/languages/dotnet/05-route-mesh.ko.md:627, .NET guide 12-operations.ko.md:150, .NET regression-test-matrix.ko.md:386)는 `drain_state_publish_failed`를 쓰는데, C++ `drain_force_reason_t`(cpp/02-framework-interfaces.ko.md:215)는 `draining_state_publish_failed`, Java `ZLinkDrainForceReason`(java/01-system-structure.ko.md:964, 975)은 `DRAINING_STATE_PUBLISH_FAILED`, Node `ZLinkDrainForceReason`과 그 regression matrix(node/02-handler-interfaces.ko.md:393, node/internals/regression-test-matrix.ko.md:467)는 `DrainingStatePublishFailed`를 쓴다. Kotlin은 이 Java 값을 그대로 재사용하므로 사실상 4/5 언어가 어긋난다. iteration 11 manifest §3은 "terminal reason은 네 개의 snake_case 값으로 통일했다"고 선언하지만 실제로는 언어마다 다른 문자열이다. scripts/verify-framework-doc-contracts.sh:1977-1991의 `fixedDrainReasons`/canonical-spelling 검사는 .NET 두 파일(spec/server/languages/dotnet/05-route-mesh.ko.md, dotnet/internals/regression-test-matrix.ko.md)만 대상으로 해 이 cross-language drift를 잡지 못한다 — 제안: cpp/java(+kotlin이 공유)/node의 값을 `drain_state_publish_failed`/`DrainStatePublishFailed` 계열로 정정하고, verifier에 5언어 전수 비교 검사를 추가한다.

[원칙][medium] framework/doc/framework/common/e2e/README.ko.md:501 — Config 11 요약 행에 제거된 drain policy 용어가 현재 기능처럼 남아 있다 — 표는 "graceful drain(typed Draining field·연결 유지, actor 핸드오프, SPOT 정책 drain-natural/release-and-recreate, 강제 종료 통지, 동시 drain 폴백)"이라고 현재 능력으로 서술하지만, 실제 config-11-observability-ops.ko.md OBS-C3(§4, 라인 226-254)과 log/s3-drain-policy-amendment/audit.ko.md, 공통 spec(server/54-graceful-drain-handoff.ko.md §2.1)은 policy enum·builder option을 전부 제거하고 "admission seal → accepted turn 완료 → Actor handoff·STREAM barrier → local Spot close → row 제거"의 단일 고정 순서만 정의한다. 다섯 언어 ObservabilityOps feature-map(cpp/dotnet/java/java-kotlin/node)에도 `DrainNatural`/`ReleaseAndRecreate` 계열 용어가 전혀 없다(grep 0건, scope 전체에서도 이 한 곳뿐). 이는 documentation-principles.ko.md 원칙 8(현재 상태만 서술, changelog식 서술 금지)을 위반하며, scripts/verify-framework-doc-contracts.sh:1106의 `removedDrainPolicy` 정규식은 PascalCase `DrainNatural`/`ReleaseAndRecreate`만 매칭하고 이 README 자체가 `fixedDrainDocuments` 검사 목록(1107-1118행)에도 없어 하이픈 소문자 표기(`drain-natural`)를 완전히 놓친다 — 제안: 해당 셀을 고정 drain 순서 요약으로 교체하고, verifier의 `fixedDrainDocuments`에 이 README를 추가하거나 정규식에 하이픈 변형을 포함한다.

NOT CLEAN
