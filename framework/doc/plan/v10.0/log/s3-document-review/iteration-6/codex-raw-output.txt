START HASH 177/177 — aggregate `659d821d3ad41142634747bbf5f62436a439b92055b2f71057a76aebdcd351c2` — file-list `9f072f1af2b73fde7c08143da66cb651bae33074bfc915a45d18431ccf345f25`

END HASH 177/177 — aggregate `659d821d3ad41142634747bbf5f62436a439b92055b2f71057a76aebdcd351c2` — file-list `9f072f1af2b73fde7c08143da66cb651bae33074bfc915a45d18431ccf345f25`

[1차소스][blocker] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:1115 — 공개 함수가 선언되지 않은 `spot_handle_t`를 사용하며 C++ exact 계약 어디에도 handle 생성·resolve 표면이 없다 — C++ 정식 문서에서 이 타입은 두 함수 인자에만 나타나지만 공통 24 §2~3은 `(MeshName, Spot RID)`를 보유한 불투명 handle과 resolver를 요구하므로 호출자가 값을 만들 수 없다 — `spot_handle_t`의 exact 선언과 MeshName-aware resolver를 추가하고 declaration inventory로 검증한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:2514 — Actor client와 directory가 MeshName 없이 `ActorRef` 또는 actor id만 받는다 — 공통 21 §2는 한 process에 서로 다른 MeshName을 여러 개 허용하고 공통 22 §2·§5는 Actor identity와 호출을 MeshName context로 구분하지만 C++ 표면에는 mesh를 선택할 인자나 mesh-scoped client factory가 없다 — 모든 Actor 관리·resolve·send/request에 MeshName을 받거나 공개적으로 식별 가능한 mesh-scoped client를 정의한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/java/02-handler-interfaces.ko.md:1484 — Java Actor 관리·directory·client와 Spot resolver가 MeshName을 잃고 `SpotHandle`도 Spot RID만 공개한다 — 서로 다른 MeshName에서 같은 actor id 또는 Spot RID를 사용할 수 있는데 `sendToActor`, `find`, `resolveSpotHandle`은 이를 구분하지 못하며, 이는 MeshName을 명시하는 .NET exact 표면과 공통 22·24의 identity 계약에도 어긋난다 — Actor와 Spot API에 MeshName을 추가하고 `SpotHandle`이 `(MeshName, Spot RID)`를 공개하도록 고정한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/kotlin/02-handler-interfaces.ko.md:366 — Kotlin Actor directory와 SpotHandle resolver 확장이 MeshName 없는 Java 호출을 그대로 정식 API로 고정한다 — `findActor(actorId)`와 `resolveSpotHandle(spotRid)`는 한 process의 여러 MeshName에서 대상을 유일하게 정할 수 없다 — Java owner 계약과 함께 MeshName-aware 시그니처로 바꾸고 Kotlin 확장도 같은 인자를 투영한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/node/02-handler-interfaces.ko.md:68 — Node Actor client·directory·manager와 Spot resolver가 MeshName을 받지 않고 `SpotHandle`에도 `spotRid`만 있다 — 공통 계약이 허용한 다중 MeshName process에서 같은 identity의 대상 mesh를 선택할 공개 방법이 없다 — MeshName 인자 또는 명시적 mesh-scoped client factory를 정의하고 handle에 MeshName을 보존한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:1479 — C++ Actor packet member는 Spot instance의 member callback이면서 Actor object도 함께 받는다 — 공통 22 §3은 Actor payload가 Actor queue로 직접 들어가야 하고 Actor handler에 mutable Spot object를 제공하지 말라고 규정하지만 이 표면은 두 owner를 한 callback에 결합한다 — Actor packet handler를 Actor와 Actor context만 받는 표면으로 분리하고 Spot 상태 변경은 명시적 Spot send/request로 수행한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/java/02-handler-interfaces.ko.md:266 — Java Spot Actor handler가 `TSpot`과 `TActor`를 동시에 받으며 lifecycle callback도 mutable `TActor`를 받는다 — 공통 22 §3과 23 §4는 Actor/Spot claim 분리와 Spot callback의 mutable Actor 비보유를 요구하고 .NET exact는 Actor-only handler와 immutable `ZLinkActorMembership`으로 투영한다 — Java도 Actor-only handler와 immutable membership snapshot으로 변경한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/kotlin/02-handler-interfaces.ko.md:114 — Kotlin Spot Actor handler가 mutable Spot과 Actor를 한 callback에 노출하고 join/leave callback도 `TActor`를 받는다 — 두 실행 문맥의 owner 상태를 동시에 노출해 공통 22 §3과 23 §4의 직렬성 경계를 구현할 수 없다 — Actor handler는 Actor만 받고 lifecycle은 immutable membership을 받도록 Java/Kotlin 표면을 함께 수정한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/node/02-handler-interfaces.ko.md:1428 — Node Spot Actor handler가 `spot`과 `actor`를 함께 받고 lifecycle도 mutable Actor를 전달한다 — 공통 Actor queue 직접 dispatch와 mutable cross-owner object 금지 계약에 정면으로 어긋난다 — Actor-only handler와 immutable membership callback으로 바꾸고 Spot 작업은 명시적 메시징으로 분리한다.

[1차소스][high] framework/doc/framework/common/e2e/config-10-spot-actor-transfer.ko.md:136 — E2E가 target `OnJoinedActor`가 Actor instance를 직접 받는 것을 성공 조건으로 요구한다 — 공통 23 §4는 Spot control callback이 mutable Actor object를 보유하지 못하게 하며 공통 E2E는 새 public API의 근거가 될 수 없다 — callback에는 membership snapshot만 사용하고 Actor state 복원은 Actor handler 또는 별도 Actor query evidence로 확인한다.

[1차소스][blocker] framework/doc/framework/common/sample/zoneworld/README.ko.md:114 — Actor 이동 handler가 Actor와 zone Spot을 동시에 받아 Spot 상태를 직접 변경하고 §8.3에서는 Spot이 Actor instance를 보관한다 — 공통 22 §3과 23 §4가 금지한 양방향 mutable owner 노출을 샘플의 필수 구조로 만들며 .NET exact interface로는 구현할 수도 없다 — 좌표 사본 갱신은 명시적 Spot message로 보내고 Spot은 ActorRef 또는 immutable membership만 보관하며 push는 Actor message로 요청한다.

[1차소스][high] framework/doc/framework/common/sample/bingo/README.ko.md:551 — room Spot이 Actor public method를 직접 호출해 bound-session push를 수행하도록 규정한다 — Spot이 mutable Actor를 직접 호출하는 경로는 공통 22 §3의 Actor/Spot 실행 경계를 우회한다 — Spot은 Actor send를 제출하고 Actor handler가 자기 bound session으로 push하도록 흐름을 바꾼다.

[1차소스][blocker] framework/doc/framework/common/sample/bingo/README.ko.md:1367 — 다섯 언어 공통 샘플의 .NET 코드가 exact 계약에 없는 `UseDrainPolicy(ZLinkSpotDrainPolicy.DrainNatural)`을 호출한다 — 해당 타입과 메서드는 Java·Kotlin·Node·C++ exact에는 있으나 .NET exact 문서와 .NET contract inventory에는 없으므로 예제가 컴파일 가능한 다섯 언어 투영이 아니다 — 공통 계약 승인 후 .NET exact interface에 같은 정책을 먼저 추가하거나 이 .NET 예제를 제거한다.

[1차소스][high] framework/doc/framework/spec/02-interaction-model.ko.md:43 — 모든 route 오류 request의 Framework 자동 재전송을 절대 금지하지만 Spot direct 계약은 안전한 stale-target request를 한 번 다시 제출하도록 허용한다 — server/24 §5의 명시적 예외와 현재 문장이 동시에 참일 수 없어 구현자가 서로 다른 retry 의미를 선택하게 된다 — 이 문장에 Spot stale-route의 실행 전 확인 가능한 단일 refresh 예외를 명시하거나 24 §5의 재제출을 제거한다.

[1차소스][high] framework/doc/framework/spec/server/languages/node/02-handler-interfaces.ko.md:481 — Node error object가 공통 숫자 오류 값을 보존하지 않는다 — 공통 05 §13은 언어별 exception/error object가 kind와 숫자 값을 모두 보존하도록 요구하지만 `ZLinkFrameworkException`은 문자열 `kind`와 `isRetriable`만 제공하고 숫자는 별도 전역 map에만 있다 — exception에 안정적인 numeric `code`를 노출하거나 공통 계약이 별도 map을 허용하도록 명시적으로 바꾼다.

[원칙][high] framework/doc/framework/spec/stream-connector/32-stream-connector.ko.md:265 — 현재 10.0.0 정식 wire spec에 구형 peer 응답을 수용하는 이전 버전 호환 동작이 섞여 있다 — 같은 문서 §3은 Response/Error의 `name_len = 0`을 현재 계약으로 고정하며 정식 문서는 이전 버전이나 전환 이력을 포함하지 않아야 한다 — legacy decoder 정책은 전환 문서로 옮기고 정식 spec에는 10.0.0 입력 계약만 남긴다.

[원칙][high] framework/doc/framework/spec/stream-connector/32-stream-connector.ko.md:506 — 정식 spec이 언어 gap 문서를 링크하고 향후 구현·삭제·교체 및 gap 종료 계획을 지시한다 — manifest의 iteration 5 수정 설명과 정식 문서 현재 상태 원칙에 반하며 임시 gap 문서를 정식 계약의 근거로 역참조한다 — 계약 문장만 남기고 gap 링크, 구현 지시, 전환 완료 조건은 gap/plan 문서로 이동한다.

[원칙][medium] framework/doc/framework/common/README.ko.md:16 — 현재 공통 문서의 지원 언어 설명이 다섯 exact interface 집합과 다르다 — Python·Go·Rust를 현재 대상처럼 열거하면서 Kotlin은 누락하고, 같은 범위의 sample 문서는 `.NET, Java, Kotlin, Node.js, C++` 다섯 언어를 현재 지원 집합으로 고정한다 — 현재 10.0.0의 다섯 언어만 적고 향후 언어 방향은 plan 문서로 분리한다.
