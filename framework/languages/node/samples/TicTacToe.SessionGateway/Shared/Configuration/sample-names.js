const SampleNames = Object.freeze({
  routeChannel: 'sample-route',
  apiNode: 'api-server',
  playNode: 'play-server',
  sessionNode: 'session-server',
  playerActorType: 'player'
});

const SampleTimings = Object.freeze({
  requestTimeout: 7000
});

module.exports = { SampleNames, SampleTimings };
