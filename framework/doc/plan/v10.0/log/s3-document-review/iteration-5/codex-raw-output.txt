시작 aggregate hash: `f1d9cdc5c2e18d79dbac1a68d2a501d66b39a920f731033b6b71f88091954d1c`
종료 aggregate hash: `f1d9cdc5c2e18d79dbac1a68d2a501d66b39a920f731033b6b71f88091954d1c`
검토 파일 수: `227/227`

[1차소스][blocker] framework/doc/framework/spec/04-async-execution-policy.ko.md:39 — one-way submit의 잘못된 입력을 결과로 반환한다고 정의하지만 다른 정식 계약과 언어별 enum이 이를 표현하지 못한다 — `05-framework-api.ko.md:244`는 invalid argument/state를 언어별 local call 오류로 처리하고, C++/.NET/Java/Node의 `SubmitStatus`에는 invalid-input 값이 없다 — 잘못된 입력을 동기 local 오류로 할지 submit result로 할지 하나로 결정하고 공통·언어별 계약을 일치시킨다.

[1차소스][blocker] framework/doc/framework/spec/server/41-location-store-redis.ko.md:146 — canonical transfer ID의 값 영역과 직렬화가 언어별 exact interface 및 byte fixture와 양립하지 않는다 — fixture는 임의 문자열 `transfer-42`를 요구하지만 C++는 16-byte 배열, Java는 `UUID`, .NET은 `Guid`, Node는 `string`을 사용한다 — transfer ID의 공통 값 영역과 UTF-8 형식을 고정하고 모든 언어 타입과 fixture를 같은 표현으로 맞춘다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:2408 — C++ Actor public surface가 exact signature 없이 forward declaration으로만 남아 있다 — `actor_client_t`, `actor_send_call_t`, `actor_request_call_t`의 member가 없고 `actor_directory_t::ensure`는 `...` placeholder를 사용하지만 C++ E2E는 `send_to_actor(...).async()`와 `request_to_actor(...).async<TReply>()`를 public API로 단언한다 — Actor client, directory, call object의 입력·결과·terminator를 완전한 C++ 선언으로 고정한다.

[1차소스][blocker] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:2386 — C++ `spot_manager_t`가 생성·조회·종료 owner라고 설명하면서 exact member를 하나도 선언하지 않는다 — C++ SpotService feature map은 `get_or_create_spot`과 `close_spot`을 public API로 사용한다 — 생성, 조회, close의 인자·결과·오류를 정식 C++ interface로 선언한다.

[원칙][high] framework/doc/framework/spec/stream-connector/32-stream-connector.ko.md:539 — 동결된 10.0.0 정식 계약 안에 배포 설계 대안 세 건이 미결정 상태로 남아 있다 — Unity 배포 채널, WebGL adapter 위치, Maven 좌표가 선택되지 않아 하나의 구현 가능한 배포 계약이 아니다 — 결정을 확정해 정식 문서에는 선택한 계약만 남기고 대안 검토는 임시 plan으로 옮긴다.

[원칙][high] framework/doc/framework/spec/stream-connector/32-stream-connector.ko.md:547 — 정식 spec이 구현 차이 절과 `90-implementation-gap.ko.md` 링크를 포함한다 — 공개 계약 관리 문서 §4는 정식 spec이 전환 inventory를 참조하거나 구현 진행 상태를 설명하지 못하게 한다 — 해당 절을 삭제하고 차이는 임시 전환 inventory에서만 관리한다.

[원칙][high] framework/doc/framework/spec/server/languages/README.ko.md:20 — server exact-interface 정식 문서가 현재 구현 차이를 설명하며 `90-implementation-gap.ko.md`를 직접 참조한다 — `00-public-contract-governance.ko.md:47-48`의 정식 spec 책임과 정면으로 충돌한다 — 현재 목표 계약만 긍정문으로 남기고 구현 차이 문구와 링크를 제거한다.

[원칙][high] framework/doc/framework/spec/stream-connector/languages/typescript/03-stream-connector.ko.md:230 — TypeScript exact spec에 구현 완료 상태, 구현 계획 G3·G4 로그, 내부 regression matrix가 섞여 있다 — 정식 interface 문서는 현재 계약만 소유하며 구현 진행과 검증 로그는 임시 문서가 소유해야 한다 — exact signature와 관찰 가능한 계약만 남기고 구현 상태·계획·로그 링크를 이동한다.

[1차소스][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:1291 — C++ metadata 정책의 owner와 method 이름이 세 형태로 충돌한다 — 선언은 `message_metadata_policy_t::forward(key)`, root options는 정의되지 않은 `metadata_policy_builder_t`, 산문·예제는 `add_forwarded_metadata_key(key)`를 사용한다 — builder 타입과 key 등록 method를 하나로 통일하고 모든 예제를 같은 선언에 맞춘다.

[1차소스][high] framework/doc/framework/spec/03-message-model.ko.md:61 — bound-session relay metadata의 방향 의미가 정해지지 않아 다섯 언어가 같은 기능을 투영하지 못한다 — .NET은 session→actor와 actor→session을 별도 allowlist로 두고, Java/C++는 무방향 key 목록, Node는 key를 받지 않는 전체 on/off boolean을 제공한다 — 공통 계약에서 대칭 또는 방향별 allowlist를 결정하고 모든 언어에 같은 표현력을 제공한다.

[1차소스][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:762 — C++ send call이 admission 결과를 반환하지 않는다 — `send_call_t::submit()`과 `route_send_call_t::submit()`은 `void`지만 공통 §1.3은 backpressure·not-found·shutdown을 구분하는 bounded result를 요구하고 다른 언어는 `SubmitResult`를 반환한다 — C++ send call에 non-blocking/blocking admission 결과 terminator를 선언한다.

[1차소스][high] framework/doc/framework/spec/server/languages/kotlin/02-handler-interfaces.ko.md:393 — Kotlin convenience send/request가 Java 정본의 MeshName과 call-result 계약을 제거한다 — Java `ZLinkClient.sendToChannel`은 `meshName`을 받고 `ZLinkSendCall`을 반환하지만 Kotlin extension은 `channelName`만 받고 `Unit`을 반환하며, 이를 정당화할 scoped receiver도 없다 — Java 계약을 보존하도록 MeshName과 call/result를 노출하거나 명시적인 mesh-scoped receiver 계약을 추가한다.

[1차소스][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:1593 — C++ 정식 산문과 공통 E2E가 존재하지 않는 파일 logging API를 public surface로 사용한다 — `logging_builder_t`의 실제 exact 선언은 `use_console()`과 `set_level()`뿐인데 산문은 `use_file(...)`, 공통 E2E는 `use_rotating_file(...)`까지 요구한다 — 파일 sink를 계약으로 채택한다면 exact signature를 먼저 선언하고, 아니면 해당 호출을 제거한다.

[1차소스][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:1611 — C++ message-flow runtime toggle 예제가 선언되지 않은 `dispatch.diagnostics.message_flow` field를 사용한다 — `dispatch_options_t`의 exact 정의에는 observer setter만 있고, 공통 E2E는 또 다른 `app.set_message_flow_mode(...)` 이름을 사용한다 — toggle owner와 method를 하나로 결정해 정확한 선언과 예제를 일치시킨다.

[1차소스][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:2554 — exact interface의 codec 등록 method가 `add(...)` placeholder로 남아 있다 — template payload type 외에 어떤 serializer·factory·ownership을 전달하는지 선언하지 않아 구현과 contract test가 동일 signature를 만들 수 없다 — 실제 인자, 제약, 반환과 중복 등록 오류를 완전한 C++ 선언으로 고정한다.

[1차소스][high] framework/doc/framework/common/e2e/config-7-monitoring.ko.md:32 — 공통 E2E가 정식 exact interface에 없는 `AddZLinkMonitoring(...)`을 framework public API로 규정한다 — .NET exact 문서에는 해당 API나 대응 monitoring builder 선언이 없고 ZoneWorld sample도 같은 미승인 이름을 사용한다 — 공통 의미와 다섯 언어 exact interface를 먼저 정식화하거나 E2E를 이미 정식화된 monitoring 표면으로 다시 쓴다.

[1차소스][high] framework/languages/java/e2e-kotlin/DiscoveryRegistryHa/feature-map.ko.md:6 — Kotlin feature map이 선언되지 않은 `monitoringLocationRuntimeQuery()`를 public acquisition 경로로 사용한다 — Java/Kotlin exact 문서에는 해당 accessor나 factory가 없고 `ZLinkLocationRuntimeQuery`의 operation만 존재한다 — query 획득 경로를 exact interface에 정의하거나 이미 정식화된 DI/bean 경로를 사용한다.

[1차소스][high] framework/languages/java/e2e/ResilienceLifecycle/feature-map.ko.md:35 — Java E2E가 제거 대상 builder 계층의 `clientServerChannel(...).configureServerSocket().weight(...)`를 사용한다 — RouteMesh inventory는 ClientServerChannel builder를 금지하고 Java exact 계약은 `ChannelName(...).setWeight(...)`와 runtime options를 제공한다 — 현재 RouteMesh builder/runtime option으로 시나리오를 다시 기술한다.

[1차소스][high] framework/languages/java/e2e-kotlin/ResilienceLifecycle/feature-map.ko.md:28 — Kotlin E2E도 제거 대상 `clientServerChannel(...).configureServerSocket().weight(...)`를 public runtime 경로로 사용한다 — Java/Kotlin 정식 surface는 ChannelName weight와 RouteMesh runtime options다 — 정식 builder/runtime option으로 교체한다.

[1차소스][high] framework/languages/node/e2e/RegistryMessaging/feature-map.ko.md:19 — Node E2E가 exact interface에 없는 `configureServerSocket().weight`를 완료된 public API로 단언한다 — Node exact 계약은 `ZLinkMeshChannelBuilder.setWeight(weight)`를 선언한다 — RM-C7을 정식 ChannelName weight API로 수정한다.

[1차소스][high] framework/languages/java/e2e/SpotService/feature-map.ko.md:51 — Java E2E가 존재하지 않는 `context.runWorker`를 public API로 기록한다 — Java exact interface는 CPU/I/O 책임을 분리한 `runCpuWorker`와 `runIoWorker`만 선언한다 — 실제 scenario에 맞는 정식 worker method 이름과 call terminator를 사용한다.

[1차소스][high] framework/languages/java/e2e-kotlin/SpotService/feature-map.ko.md:44 — Kotlin E2E도 존재하지 않는 `context.runWorker`를 public API로 기록한다 — Kotlin은 Java worker surface를 재사용하며 exact 계약에는 `runCpuWorker`/`runIoWorker`만 있다 — 정식 worker method로 교체한다.

[원칙][medium] framework/doc/framework/common/sample/tictactoe/README.ko.md:348 — 현재 샘플 설명이 금지된 과거 abstraction `RouteMeshChannel`의 부재를 서술한다 — current-state 문서는 사라진 표면을 호명하지 않고 현재 resolver→`SpotHandle` 경로를 긍정문으로 설명해야 한다 — 첫 문장을 제거하고 handle 기반 전송 계약만 남긴다.

[원칙][medium] framework/doc/framework/common/e2e/config-2-spot-service.ko.md:430 — 검증 질문이 “메시지를 밀어 넣다”, “session이 깨지다”, “남에게 번지다”라는 구어체·의인화 표현을 사용한다 — 문서 원칙 7.7은 객체와 상태를 중립적인 기술 표현으로 쓰도록 요구한다 — “입력해도 session 상태가 손상되거나 다른 session에 영향을 주지 않는가”로 고친다.

[원칙][low] framework/doc/framework/spec/stream-connector/languages/java/03-stream-connector.ko.md:303 — “얇게 얹는다”는 회의체 표현이다 — 문서 원칙 7.7이 `얹는다`를 명시적으로 금지하고 `추가한다`를 권한다 — “Kotlin extension은 Java typed payload 표면에 관용 API를 추가한다”로 쓴다.

[원칙][low] framework/doc/framework/common/sample/event/shoppingmall.ko.md:614 — workflow 재실행을 “루프를 다시 태운다”라고 비유한다 — 같은 표현이 864행에도 반복되며 전문 기술 문서의 중립적 동작 설명이 아니다 — “9.1 loop를 다시 실행한다”로 통일한다.

[원칙][low] core/doc/spec/core/socket/README.ko.md:823 — 표준 전문 용어 `transport`를 “트랜스포트”로 음차한다 — 문서 원칙 7.1은 `transport`를 영어 그대로 쓰도록 명시하며 838행에도 같은 표현이 반복된다 — 두 위치를 `transport`로 통일한다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:3273-3275|note=[used read-only review and exact live evidence preferences]
</citation_entries>
<rollout_ids>
019ec366-9686-7df0-a110-078fd5cf9f37
</rollout_ids>
</oai-mem-citation>
