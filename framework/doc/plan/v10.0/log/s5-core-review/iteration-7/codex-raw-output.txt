$ nl -ba framework/doc/plan/v10.0/log/s5-core-review/iteration-7/manifest.ko.md | sed -n '12,21p;53,68p'
    12	## 2. Snapshot
    13	
    14	| 항목 | 값 |
    15	|---|---|
    16	| Stage / iteration | S5 / 7 (연장 전체 pass) |
    17	| Acceptance candidate commit | `f8c35e6fe` |
    18	| 직전 candidate | `b1e6c81fb` (iteration 6) |
    19	| Scope 파일 수 | 631 |
    20	| Scope aggregate SHA-256 | `cdbc1b1053c4931d0610968d640c133b5ea9b07964a2a57cd6d379c6f2478af6` |
    21	| Scope 정의 | `git ls-files core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md` 각 파일 sha256의 aggregate |
    53	## 5. Known risk (명시 판정 대상, 동일 4건)
    54	
    55	1. TSAN auto-HWM lock-order 계열(기존).
    56	2. TSAN raw command mailbox ypipe 계열(기존).
    57	3. raw socket teardown 관찰(pipe_t::detach_peer_backref·asio blob_t) — 9.x
    58	   raw 기계 계열.
    59	4. ctx_term linger(기존).
    60	
    61	## 6. 리뷰어 지시
    62	
    63	- 실행 계약 동일: core/ 읽기 전용, scope hash 시작·종료 기록, 결과는
    64	  `iteration-7/`에 R1=`codex-review.ko.md`, R2=`claude-sonnet-review.ko.md`.
    65	- iteration 6 수정 4건의 해소 판정 + 전체 scope 재검토 + I1·I2·I3 축별 판정
    66	  + known risk 4건 명시 판정.
    67	- blocker·high·medium 없음 + 세 축 CLEAN이면 마지막 줄 정확히
    68	  `CORE REVIEW CLEAN`.

$ git ls-files core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | xargs sha256sum | sha256sum
cdbc1b1053c4931d0610968d640c133b5ea9b07964a2a57cd6d379c6f2478af6  -

$ git rev-parse HEAD
f8c35e6fecbe0c8ec13a7cac0e7fffbd24218f0d

$ git ls-files core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | wc -l
631

$ git diff --stat b1e6c81fb..f8c35e6fe -- core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md
 core/doc/internals/services-internals.ko.md        |   6 +-
 core/doc/internals/services-internals.md           |   8 +-
 core/doc/spec/core/04-errno-map.ko.md              |   2 +-
 core/doc/spec/core/04-errno-map.md                 |   2 +-
 core/src/api/mesh/mesh_actor_api.cpp               |  39 +++---
 core/src/api/mesh/mesh_messaging_api.cpp           | 141 +++++++++++++--------
 core/src/api/mesh/mesh_monitor_api.cpp             |   5 +-
 core/src/api/mesh/mesh_node_api.cpp                |  33 +++--
 core/src/api/mesh/mesh_stream_session_api.cpp      |  38 ++++--
 core/src/runtime/services/mesh/mesh_runtime.cpp    |  32 ++++-
 .../integration/test_mesh_lifecycle_contracts.cpp  |  42 ++++++
 11 files changed, 250 insertions(+), 98 deletions(-)

$ git diff --check b1e6c81fb..f8c35e6fe -- core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md

$ git diff --stat 8206fd44d..f8c35e6fe -- core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | tail -1
 46 files changed, 3961 insertions(+), 1899 deletions(-)

$ python3 core/tests/contract/check_public_surface.py . core/build/lib/libzlink.so
PUBLIC SURFACE CONTRACT: PASS
functions=196 exports match, removed identifiers absent

$ ctest --test-dir core/build -N | tail -2

Total Tests: 85

$ rg -n "RUN_TEST" core/tests/integration/test_mesh_peer_admission.cpp core/tests/integration/test_mesh_monitor_matrix.cpp core/tests/integration/test_mesh_stress.cpp core/tests/integration/test_mesh_lifecycle_contracts.cpp | awk -F: '{count[$1]++} END {for (f in count) print f, count[f]}'
core/tests/integration/test_mesh_peer_admission.cpp 12
core/tests/integration/test_mesh_monitor_matrix.cpp 6
core/tests/integration/test_mesh_stress.cpp 3
core/tests/integration/test_mesh_lifecycle_contracts.cpp 10

$ find core/include core/src core/tests core/packaging core/doc/spec/core core/doc/internals -type f -empty -print

$ rg -n "^(<<<<<<<|=======|>>>>>>>)" core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md

$ rg -n "language-exchange|문서작성" core/doc/spec/core core/doc/internals CHANGELOG.md

$ nl -ba core/src/api/mesh/mesh_node_api.cpp | sed -n '310,328p;415,478p'
   310	zlink_request_result_t zlink_mesh_node_shutdown (void *mesh_node_, uint32_t timeout_ms_)
   311	{
   312	    mesh_node_t *node = as_mesh_node (mesh_node_);
   313	    if (!node) {
   314	        errno = EFAULT;
   315	        return ZLINK_REQUEST_INVALID_ARGUMENT;
   316	    }
   317	
   318	    std::unique_lock<std::mutex> lock (node->mutex);
   319	    //  Re-entry (a concurrent shutdown/destroy on this handle) deadlocks; a
   320	    //  sequential shutdown after a TIMED_OUT drain is legal and waits again.
   321	    if (node->shutdown_active) {
   322	        errno = EDEADLK;
   323	        return ZLINK_REQUEST_INVALID_STATE;
   324	    }
   325	    if (node->state == ZLINK_MESH_NODE_STOPPED)
   326	        return ZLINK_REQUEST_OK;
   327	
   328	    node->shutdown_active = true;
   415	zlink_close_result_t zlink_mesh_node_destroy (void **mesh_node_p_)
   416	{
   417	    if (!mesh_node_p_) {
   418	        errno = EFAULT;
   419	        return ZLINK_CLOSE_INVALID_HANDLE;
   420	    }
   421	    mesh_node_t *node = as_mesh_node (*mesh_node_p_);
   422	    if (!node) {
   423	        errno = EFAULT;
   424	        return ZLINK_CLOSE_INVALID_HANDLE;
   425	    }
   426	
   427	    {
   428	        std::lock_guard<std::mutex> lock (node->mutex);
   429	        //  A shutdown in flight still owns this node past its drain wait
   430	        //  (its emit/completion tail runs unlocked), so destroying now is
   431	        //  the same-handle lifecycle re-entry the contract ends with
   432	        //  EDEADLK (§11).
   433	        if (node->shutdown_active) {
   434	            errno = EDEADLK;
   435	            return ZLINK_CLOSE_BUSY;
   436	        }
   437	        uint32_t live_facades = 0;
   438	        uint32_t live_timers = 0;
   439	        for (std::map<std::string, spot_state_t>::iterator it = node->spots.begin ();
   440	             it != node->spots.end (); ++it) {
   441	            live_facades += it->second.facade_count;
   442	            live_timers += it->second.timer_count;
   443	        }
   444	        if (node->publisher_count > 0 || node->monitor_count > 0
   445	            || node->stream_session_count > 0 || live_facades > 0 || live_timers > 0) {
   446	            errno = EBUSY;
   447	            return ZLINK_CLOSE_BUSY;
   448	        }
   449	
   450	        //  Forced termination of any non-stopped node happens under the same
   451	        //  lock hold as the admission checks above, so no shutdown can start
   452	        //  between the check and the teardown commitment.
   453	        if (node->state != ZLINK_MESH_NODE_STOPPED) {
   454	            node->state = ZLINK_MESH_NODE_STOPPED;
   455	            node->last_changed_ms = now_ms ();
   456	        }
   457	        for (std::map<owner_id_t, owner_state_t>::iterator it = node->owners.begin ();
   458	             it != node->owners.end (); ++it) {
   459	            for (int d = 0; d < 2; ++d) {
   460	                if (it->second.domains[d].claimed)
   461	                    it->second.domains[d].revoked = true;
   462	            }
   463	        }
   464	        node->operations.clear ();
   465	        node->cv.notify_all ();
   466	    }
   467	
   468	    //  Close the public entry door before tearing anything down: a caller
   469	    //  that has not yet validated the handle now fails with EFAULT instead
   470	    //  of racing the teardown below.
   471	    unregister_node (node);
   472	    wire_stop (node);
   473	    //  All Spot timers are gone (they gate destroy with EBUSY), so the
   474	    //  node's dedicated timer scheduler can wind down.
   475	    zlink_timer_release_spot_node_scheduler (node);
   476	    delete node;
   477	    *mesh_node_p_ = NULL;
   478	    return ZLINK_CLOSE_OK;

$ nl -ba core/src/runtime/services/mesh/mesh_runtime.cpp | sed -n '395,405p'
   395	mesh_node_t *as_mesh_node (void *handle_)
   396	{
   397	    if (!handle_)
   398	        return NULL;
   399	    {
   400	        std::lock_guard<std::mutex> lock (registry ().mutex);
   401	        if (!registry ().live_nodes.count (handle_))
   402	            return NULL;
   403	    }
   404	    mesh_node_t *node = static_cast<mesh_node_t *> (handle_);
   405	    return node->check_tag () ? node : NULL;

$ nl -ba core/tests/integration/test_mesh_lifecycle_contracts.cpp | sed -n '566,602p'
   566	//  lifecycle re-entry the contract ends with EDEADLK (§11): the node
   567	//  survives, the parked shutdown completes once the claim releases, and only
   568	//  then does a destroy succeed.
   569	void test_destroy_during_shutdown_wait_is_deadlock_error ()
   570	{
   571	    void *ctx = zlink_ctx_new ();
   572	    TEST_ASSERT_NOT_NULL (ctx);
   573	    void *node = new_started_node (ctx, "sd-race-mesh", "jobs");
   574	
   575	    zlink_msg_t part;
   576	    make_payload (&part, "job-1");
   577	    TEST_ASSERT_EQUAL_INT (ZLINK_SUBMIT_OK,
   578	                           zlink_mesh_node_send_to_channel (node, "jobs", NULL, &part, 1,
   579	                                                            ZLINK_SEND_FLAGS_NONE));
   580	    zlink_msg_close (&part);
   581	    zlink_mesh_claim_t claim;
   582	    take_ready_claim (node, ZLINK_MESH_OWNER_NODE, ZLINK_MESH_READY_APPLICATION, &claim);
   583	
   584	    //  The held claim parks the shutdown in its drain wait.
   585	    std::atomic<int> shutdown_rc (-1);
   586	    std::thread shutdown_thread ([&] () {
   587	        shutdown_rc.store (static_cast<int> (zlink_mesh_node_shutdown (node, 3000)));
   588	    });
   589	    msleep (300);
   590	    TEST_ASSERT_EQUAL_INT (-1, shutdown_rc.load ());
   591	
   592	    void *doomed = node;
   593	    TEST_ASSERT_EQUAL_INT (ZLINK_CLOSE_BUSY, zlink_mesh_node_destroy (&doomed));
   594	    TEST_ASSERT_EQUAL_INT (EDEADLK, zlink_errno ());
   595	    TEST_ASSERT_NOT_NULL (doomed);
   596	
   597	    //  Releasing the claim lets the parked shutdown drain and finish.
   598	    TEST_ASSERT_EQUAL_INT (ZLINK_CLOSE_OK, zlink_mesh_claim_release (&claim));
   599	    shutdown_thread.join ();
   600	    TEST_ASSERT_EQUAL_INT (static_cast<int> (ZLINK_REQUEST_OK), shutdown_rc.load ());
   601	
   602	    TEST_ASSERT_EQUAL_INT (ZLINK_CLOSE_OK, zlink_mesh_node_destroy (&node));

$ nl -ba core/doc/spec/core/service/01-mesh-node.ko.md | sed -n '506,513p'
   506	## 11. Thread safety와 오류 우선순위
   507	
   508	send, request, publish, weight 변경, peer intent와 query는 thread-safe다. lifecycle configure 함수는 start와
   509	동시에 호출할 수 없다. 같은 handle의 shutdown과 destroy를 재진입하면 `EDEADLK`다.
   510	
   511	입력 검증은 argument, state, target lookup, backpressure 순서로 수행한다. 따라서 draining 상태의 새 submit은
   512	target 상태와 관계없이 `ZLINK_SUBMIT_INVALID_STATE`, `errno == ESHUTDOWN`이다. invalid argument는 state
   513	조회 전에 검증한다.

$ nl -ba core/doc/spec/core/04-errno-map.ko.md | sed -n '24,45p;94,100p'
    24	## 2. Submit result
    25	
    26	| Result | errno | 의미 |
    27	|---|---|---|
    28	| `ZLINK_SUBMIT_OK` | - | 함수가 정한 ownership 전이가 완료됨 |
    29	| `ZLINK_SUBMIT_BACKPRESSURED` | `EAGAIN`, `ETIMEDOUT`, `ENOBUFS` | queue 또는 reservation capacity 부족 |
    30	| `ZLINK_SUBMIT_NOT_CONNECTED` | `ENOTCONN` | target pipe 또는 session 연결 없음 |
    31	| `ZLINK_SUBMIT_NOT_FOUND` | `ENOENT` | channel member, Spot, Actor 또는 binding 없음 |
    32	| `ZLINK_SUBMIT_NOT_ADMITTED` | `EACCES` | target route는 식별했지만 handshake 또는 신규 outbound weight 같은 현재 admission 정책이 submit을 거부함 |
    33	| `ZLINK_SUBMIT_TERMINATED` | `ETERM`, `ESHUTDOWN` | Context 또는 service lifecycle 종료 |
    34	| `ZLINK_SUBMIT_INVALID_HANDLE` | `EFAULT` | handle이 `NULL`이거나 종류가 다름 |
    35	| `ZLINK_SUBMIT_INVALID_ARGUMENT` | `EINVAL`, `EMSGSIZE` | 잘못된 pointer, count, name, metadata 또는 flags |
    36	| `ZLINK_SUBMIT_NOT_SUPPORTED` | `ENOTSUP` | handle에서 지원하지 않는 operation |
    37	| `ZLINK_SUBMIT_INVALID_STATE` | `EBUSY`, `ESTALE`, `EALREADY`, `ESHUTDOWN` | lifecycle, generation, token 또는 binding state 오류 |
    38	| `ZLINK_SUBMIT_THREAD_VIOLATION` | `EDEADLK`, `EPERM` | 금지한 callback 재진입 또는 thread 사용 |
    39	| `ZLINK_SUBMIT_OUT_OF_MEMORY` | `ENOMEM` | 필요한 storage 확보 실패 |
    40	| `ZLINK_SUBMIT_SEQ_EXHAUSTED` | `EOVERFLOW` | operation sequence 공간 소진 |
    41	| `ZLINK_SUBMIT_INTERNAL_ERROR` | 보존된 errno | 다른 공개 분류가 없는 내부 실패 |
    42	
    43	이 결과는 raw socket send/request submit, MeshNode node·channel·publisher, Spot direct·channel·publish,
    44	Actor messaging과 STREAM session operation에 적용한다. service complete multipart input은 성공과 실패 모두
    45	borrowed ownership을 유지한다. raw socket input ownership은 각 socket 문서가 정한다.
    94	|---|---|---|
    95	| `ZLINK_HANDLER_INVALID_ARGUMENT` | `EINVAL` | handler 또는 mask가 잘못됨 |
    96	| `ZLINK_HANDLER_BUSY` | `EBUSY` | 배타적인 receive model이 이미 등록됨 |
    97	| `ZLINK_HANDLER_NOT_SUPPORTED` | `ENOTSUP` | handle에서 handler를 지원하지 않음 |
    98	| `ZLINK_CLOSE_BUSY` | `EBUSY`, `EDEADLK` | `EBUSY` — active child handle, callback 또는 API가 존재함. `EDEADLK` — 같은 handle의 shutdown·destroy lifecycle 재진입(01-mesh-node §11) |

$ nl -ba core/src/runtime/services/mesh/mesh_runtime.cpp | sed -n '490,549p;665,680p;760,768p'
   490	int validate_metadata (const uint8_t *data_, size_t size_)
   491	{
   492	    if (size_ == 0 || size_ > ZLINK_MESH_APPLICATION_METADATA_MAX) {
   493	        errno = EINVAL;
   494	        return -1;
   495	    }
   496	    if (size_ < 2) {
   497	        errno = EINVAL;
   498	        return -1;
   499	    }
   500	    const uint8_t version = data_[0];
   501	    const uint8_t count = data_[1];
   502	    if (version != 1) {
   503	        errno = EINVAL;
   504	        return -1;
   505	    }
   506	    size_t offset = 2;
   507	    std::set<std::string> keys;
   508	    for (uint8_t entry = 0; entry < count; ++entry) {
   509	        if (offset + 1 > size_) {
   510	            errno = EINVAL;
   511	            return -1;
   512	        }
   513	        const uint8_t key_len = data_[offset];
   514	        offset += 1;
   515	        if (key_len == 0 || offset + key_len > size_) {
   516	            errno = EINVAL;
   517	            return -1;
   518	        }
   519	        if (!valid_utf8 (data_ + offset, key_len)) {
   520	            errno = EINVAL;
   521	            return -1;
   522	        }
   523	        std::string key (reinterpret_cast<const char *> (data_ + offset), key_len);
   524	        if (!keys.insert (key).second) {
   525	            errno = EINVAL;
   526	            return -1;
   527	        }
   528	        offset += key_len;
   529	        if (offset + 2 > size_) {
   530	            errno = EINVAL;
   531	            return -1;
   532	        }
   533	        const uint16_t value_len = static_cast<uint16_t> ((data_[offset] << 8) | data_[offset + 1]);
   534	        offset += 2;
   535	        if (offset + value_len > size_) {
   536	            errno = EINVAL;
   537	            return -1;
   538	        }
   539	        if (!valid_utf8 (data_ + offset, value_len)) {
   540	            errno = EINVAL;
   541	            return -1;
   542	        }
   543	        offset += value_len;
   544	    }
   545	    if (offset != size_) {
   546	        errno = EINVAL;
   547	        return -1;
   548	    }
   549	    return 0;
   665	    //  The ready-index node and the deque slot are the only fallible steps
   666	    //  left; take them before the counters so an allocation failure leaves
   667	    //  the mailbox exactly as it was and maps to ENOMEM instead of escaping
   668	    //  the C ABI.
   669	    const uint64_t record_bytes = record_->byte_size;
   670	    const std::pair<owner_id_t, int> ready_key (owner_, static_cast<int> (domain_));
   671	    bool ready_inserted = false;
   672	    try {
   673	        ready_inserted = node_->ready.insert (ready_key).second;
   674	        mailbox.records.push_back (std::move (record_));
   675	    }
   676	    catch (const std::bad_alloc &) {
   677	        if (ready_inserted)
   678	            node_->ready.erase (ready_key);
   679	        errno = ENOMEM;
   680	        return -1;
   760	            try {
   761	                monitor->events.push_back (event_);
   762	                monitor->cv.notify_all ();
   763	            }
   764	            catch (const std::bad_alloc &) {
   765	                //  The queue is bounded and lossy by contract; dropping the
   766	                //  event under allocation pressure is the same observable
   767	                //  behavior as an overflow drop.
   768	            }

$ nl -ba core/src/api/mesh/mesh_node_api.cpp | sed -n '66,80p'
    66	zlink_mesh_operation_id_t register_operation (mesh_node_t *node_,
    67	                                              zlink_mesh_operation_kind_t kind_,
    68	                                              const owner_id_t &requester_,
    69	                                              uint32_t timeout_ms_)
    70	{
    71	    std::lock_guard<std::mutex> lock (node_->mutex);
    72	    pending_operation_t op;
    73	    memset (&op.join_actor, 0, sizeof (op.join_actor));
    74	    op.id.high = node_->lifecycle_generation;
    75	    op.id.low = node_->next_operation_serial++;
    76	    op.kind = kind_;
    77	    op.requester = requester_;
    78	    op.deadline_ms = timeout_ms_ > 0 ? now_ms () + timeout_ms_ : 0;
    79	    node_->operations[op.id.low] = op;
    80	    return op.id;

$ nl -ba core/src/runtime/services/mesh/mesh_wire.cpp | sed -n '215,240p;363,395p'
   215	zlink_submit_result_t wire_submit_data (mesh_node_t *node_,
   216	                                        const rid_bytes_t &peer_rid_,
   217	                                        wire_type_t type_,
   218	                                        uint64_t correlation_,
   219	                                        const std::string &channel_,
   220	                                        const zlink_mesh_metadata_view_t *metadata_,
   221	                                        const zlink_msg_t *parts_,
   222	                                        size_t part_count_,
   223	                                        zlink_send_flags_t flags_)
   224	{
   225	    if (!node_->router_socket) {
   226	        errno = ENOTCONN;
   227	        return ZLINK_SUBMIT_NOT_CONNECTED;
   228	    }
   229	    std::vector<unsigned char> envelope =
   230	      make_envelope (static_cast<unsigned char> (type_), metadata_ ? wire_flag_metadata : 0);
   231	    if (type_ == wire_node_request || type_ == wire_channel_request)
   232	        put_u64 (envelope, correlation_);
   233	    if (type_ == wire_channel_send || type_ == wire_channel_request) {
   234	        put_u8 (envelope, static_cast<unsigned char> (channel_.size ()));
   235	        put_bytes (envelope, channel_.data (), channel_.size ());
   236	    }
   237	    std::vector<unsigned char> metadata;
   238	    if (metadata_)
   239	        metadata.assign (metadata_->data, metadata_->data + metadata_->size);
   240	
   363	zlink_submit_result_t wire_submit_actor_data (mesh_node_t *node_,
   364	                                              const rid_bytes_t &peer_rid_,
   365	                                              bool is_request_,
   366	                                              uint64_t correlation_,
   367	                                              const zlink_actor_ref_t *source_actor_,
   368	                                              const zlink_actor_ref_t &target_actor_,
   369	                                              const zlink_mesh_metadata_view_t *metadata_,
   370	                                              const zlink_msg_t *parts_,
   371	                                              size_t part_count_,
   372	                                              zlink_send_flags_t flags_)
   373	{
   374	    if (!node_->router_socket) {
   375	        errno = ENOTCONN;
   376	        return ZLINK_SUBMIT_NOT_CONNECTED;
   377	    }
   378	    std::vector<unsigned char> envelope = make_envelope (
   379	      static_cast<unsigned char> (is_request_ ? wire_actor_request : wire_actor_send),
   380	      metadata_ ? wire_flag_metadata : 0);
   381	    if (is_request_)
   382	        put_u64 (envelope, correlation_);
   383	    if (source_actor_) {
   384	        put_actor_ref (envelope, *source_actor_);
   385	    } else {
   386	        put_u8 (envelope, 0);
   387	    }
   388	    put_actor_ref (envelope, target_actor_);
   389	    std::vector<unsigned char> metadata;
   390	    if (metadata_)
   391	        metadata.assign (metadata_->data, metadata_->data + metadata_->size);
   392	
   393	    const zlink_routing_id_t target = rid_value (peer_rid_);
   394	    return send_data_message (node_, target, envelope, metadata_ ? &metadata : NULL, parts_,
   395	                              part_count_, flags_);

$ nl -ba core/src/api/mesh/mesh_actor_api.cpp | sed -n '1377,1395p'
  1377	    const bool blocking = (flags_ & ZLINK_SEND_FLAGS_DONTWAIT) == 0;
  1378	    if (admit_record (node, destination, domain_application, record, blocking,
  1379	                      blocking ? static_cast<uint32_t> (node->sndtimeo_ms < 0 ? 0 : node->sndtimeo_ms)
  1380	                               : 0)
  1381	        != 0) {
  1382	        if (is_request) {
  1383	            std::lock_guard<std::mutex> lock (node->mutex);
  1384	            node->operations.erase (op_id.low);
  1385	            node->reply_routes.erase (reply_serial);
  1386	        }
  1387	        switch (errno) {
  1388	            case EAGAIN:
  1389	            case ETIMEDOUT:
  1390	                return ZLINK_SUBMIT_BACKPRESSURED;
  1391	            case ESHUTDOWN:
  1392	                return ZLINK_SUBMIT_INVALID_STATE;
  1393	            default:
  1394	                return ZLINK_SUBMIT_INTERNAL_ERROR;
  1395	        }

$ nl -ba core/src/api/mesh/mesh_stream_session_api.cpp | sed -n '190,210p;1099,1103p'
   190	zlink_submit_result_t send_stream_complete (void *stream_,
   191	                                            const zlink_routing_id_t *session_rid_,
   192	                                            const zlink_msg_t *parts_,
   193	                                            size_t part_count_,
   194	                                            zlink_send_flags_t flags_)
   195	{
   196	    size_t total_size = 0;
   197	    for (size_t i = 0; i < part_count_; ++i) {
   198	        const size_t part_size = zlink_msg_size (&parts_[i]);
   199	        if (part_size > SIZE_MAX - total_size) {
   200	            errno = EMSGSIZE;
   201	            return ZLINK_SUBMIT_INVALID_ARGUMENT;
   202	        }
   203	        total_size += part_size;
   204	    }
   205	
   206	    zlink_msg_t complete;
   207	    if (zlink_msg_init_size (&complete, total_size) != 0) {
   208	        errno = ENOMEM;
   209	        return ZLINK_SUBMIT_INTERNAL_ERROR;
   210	    }
  1099	
  1100	    //  Raw STREAM has one byte stream rather than multipart frame boundaries.
  1101	    //  Coalesce the borrowed parts and perform one consuming submit so failure
  1102	    //  cannot expose a prefix of the logical message.
  1103	    return send_stream_complete (stream, &session_rid, parts_, part_count_, flags_);

$ nl -ba core/src/api/mesh/mesh_monitor_api.cpp | sed -n '136,158p'
   136	zlink_close_result_t zlink_mesh_node_monitor_close (void **monitor_p_)
   137	{
   138	    if (!monitor_p_) {
   139	        errno = EFAULT;
   140	        return ZLINK_CLOSE_INVALID_HANDLE;
   141	    }
   142	    monitor_state_t *monitor = as_monitor (*monitor_p_);
   143	    if (!monitor) {
   144	        errno = EFAULT;
   145	        return ZLINK_CLOSE_INVALID_HANDLE;
   146	    }
   147	    mesh_node_t *node = monitor->node;
   148	    {
   149	        std::unique_lock<std::mutex> node_lock (node->mutex);
   150	        {
   151	            std::lock_guard<std::mutex> lock (monitor->mutex);
   152	            if (monitor->handler_active) {
   153	                //  Formal close mapping: an active callback is EBUSY
   154	                //  (EDEADLK belongs to handler registration and node
   155	                //  lifecycle re-entry, not to close).
   156	                errno = EBUSY;
   157	                return ZLINK_CLOSE_BUSY;
   158	            }

$ nl -ba core/doc/internals/services-internals.ko.md | sed -n '23,33p'
    23	| `src/api/mesh/mesh_api.cpp` | poller·timer 등 세로 관심사가 mesh로 들어오는 seam |
    24	
    25	계층 규칙: `api/mesh/*`는 공개 signature 검증과 결과 매핑을 소유하고, 상태
    26	변경은 `mesh_runtime`/`mesh_wire`의 함수로 내려보낸다. 예외는 세로 관심사
    27	seam인 `mesh_api.cpp`다: Spot timer registry(cancellation 포함)와 turn
    28	admission 상태(`timer_turn_active`·`timer_count`)는 이 seam이 직접
    29	소유·변경한다 — timer 기계는 hook만 제공하고 Spot 결합 지식은 mesh 쪽 한
    30	곳에 모은다. raw socket 계층
    31	(`runtime/sockets/`)은 mesh를 모른다. mesh가 raw ROUTER에 요구하는 유일한
    32	확장은 비소비 기록 조사인 `routed_target_writable()`(NODROP 원자 reserve용)
    33	하나다.

$ nl -ba CHANGELOG.md | sed -n '56,70p'; nl -ba core/tests/integration/test_mesh_lifecycle_contracts.cpp | sed -n '777,786p'
    56	### Verification (release-candidate scope)
    57	- Full core suite green: 85/85 CTest targets, including the two-process
    58	  remote matrix (`test_mesh_peer_admission`, 12 cases: admission/readiness,
    59	  MeshName-mismatch rejection with monitor evidence, remote request/reply,
    60	  Spot direct, multicast with duplicate guard, NODROP unreachable-target
    61	  accounting via send-fault injection, Actor lookup/messaging/join,
    62	  transfer fence, channel round-robin with zero-weight exclusion,
    63	  MIXED intent merge, drain/reconnect), the monitor event matrix
    64	  (`test_mesh_monitor_matrix`, 6 cases), dispatch stress
    65	  (`test_mesh_stress`, 3 cases) and lifecycle contracts
    66	  (`test_mesh_lifecycle_contracts`, 9 cases including the bind/destroy
    67	  race hammer).
    68	- Sanitizers: ASAN/UBSAN+leak clean on the mesh suite; TSAN reports zero
    69	  races in new mesh code (four pre-existing 9.x machinery observations are
    70	  tracked for review).
   777	    RUN_TEST (test_claims_are_process_unique_across_nodes);
   778	    RUN_TEST (test_monitor_handler_reentry_is_deadlock_error);
   779	    RUN_TEST (test_spot_ends_after_last_reference_and_generation_advances);
   780	    RUN_TEST (test_actor_destroy_waits_for_held_claim);
   781	    RUN_TEST (test_shutdown_detaches_timeoutless_operations);
   782	    RUN_TEST (test_peers_query_output_is_invariant_on_invalid_element);
   783	    RUN_TEST (test_public_strings_reject_invalid_utf8);
   784	    RUN_TEST (test_timer_destroy_overlapping_fire_completes);
   785	    RUN_TEST (test_destroy_during_shutdown_wait_is_deadlock_error);
   786	    RUN_TEST (test_stream_session_bind_destroy_race_leaves_no_binding);

$ nl -ba core/src/runtime/core/ctx_auto_hwm_recalc.cpp | sed -n '80,116p' | head -4; nl -ba core/src/runtime/core/mailbox.cpp | sed -n '39,56p;64,97p' | head -6
    80	    scoped_lock_t runtime_lock (_slot_sync);
    81	    _auto_hwm.mark_applied ();
    82	
    83	    if (!enabled)
    39	void zlink::mailbox_t::send (const command_t &cmd_)
    40	{
    41	    _sync.lock ();
    42	    _cpipe.write (cmd_, false);
    43	    const bool ok = _cpipe.flush ();
    44	    if (!ok) {

$ nl -ba core/src/runtime/core/pipe.cpp | sed -n '704,730p' | tail -8; nl -ba core/src/runtime/engine/asio/asio_engine.cpp | sed -n '1845,1862p' | head -11
   723	    detach_peer_backref ();
   724	
   725	    //  Notify the user that all the references to the pipe should be dropped.
   726	    zlink_assert (_sink);
   727	    _sink->pipe_terminated (this);
   728	
   729	    if (ack_peer)
   730	        send_pipe_term_ack (_peer);
  1845	    const zlink::blob_t *routing_id =
  1846	      _connection_facade.session ? &_connection_facade.session->peer_routing_id () : NULL;
  1847	    const unsigned char *routing_id_data = routing_id ? routing_id->data () : NULL;
  1848	    const size_t routing_id_size = routing_id ? routing_id->size () : 0;
  1849	    _connection_facade.socket->event_disconnected (_endpoint_uri_pair, disconnect_reason,
  1850	                                                   routing_id_data, routing_id_size);
  1851	    _connection_facade.session->flush ();
  1852	    _connection_facade.session->engine_error (!_connection_facade.handshaking, reason_);
  1853	    unplug ();
  1854	
  1855	    destroy_after_callbacks ();

$ nl -ba core/src/runtime/sockets/common/socket_base.cpp | sed -n '129,134p'; nl -ba core/src/runtime/sockets/common/socket_base_lifecycle.cpp | sed -n '137,161p' | tail -9
   129	    _term_pipe_acks_registered = 0;
   130	    _term_pipe_acks_received = 0;
   131	    options.socket_id = sid_;
   132	    options.ipv6 = (parent_->get (ZLINK_INTERNAL_OPT_IPV6) != 0);
   133	    options.linger.store (parent_->get (ZLINK_INTERNAL_OPT_BLOCKY) ? -1 : 0);
   134	
   153	        pipe->terminate (false);
   154	        ++term_pipe_count;
   155	    }
   156	    register_term_acks (term_pipe_count);
   157	    _term_pipe_acks_registered = term_pipe_count;
   158	    _term_pipe_acks_received = 0;
   159	
   160	    //  Continue the termination process immediately.
   161	    own_t::process_term (linger_);

$ git ls-files core/include core/src core/tests core/packaging core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | xargs sha256sum | sha256sum
cdbc1b1053c4931d0610968d640c133b5ea9b07964a2a57cd6d379c6f2478af6  -
