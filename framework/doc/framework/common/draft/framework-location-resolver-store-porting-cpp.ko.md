# Location Resolver/Store 포팅 진행 — C++

> 이 문서는 [framework-location-resolver-store.ko.md](framework-location-resolver-store.ko.md)
> 계약을 C++ framework에 적용하는 작업의 진행 추적 문서다. 계약 내용은 반복하지 않는다.
> 계약이 궁금하면 항상 원본 draft를 보고, 두 문서가 충돌하면 원본 draft가 기준이다.
>
> spot 메시징 표면(캐시 제거, `ZLinkSpotAddress` 반환)은
> [framework-spot-address-messaging.ko.md](framework-spot-address-messaging.ko.md)가 우선한다.
> 두 draft를 함께 읽고 포팅한다. 단, spot 메시징 표면 자체의 전환(rid-only overload 제거,
> stale 주소 fail-fast 분류, bridge relay framing)은 spot-address draft의 별도 작업이며
> (dotnet 포함 미구현) 이 문서의 범위가 아니다. 이 문서 범위에서는 resolver가 spot full
> 주소(`ZLinkSpotAddress`)를 반환하는 조회 표면까지 맞춘다.
>
> 다른 언어 진행 문서: [node](framework-location-resolver-store-porting-node.ko.md) ·
> [java(+kotlin)](framework-location-resolver-store-porting-java.ko.md).
> 전체 순서는 **node → java(+kotlin) → cpp**이며 cpp는 마지막이다.

경로: `framework/languages/cpp/`

## 1. 상태 보드

상태 표기: ⬜ 미착수 · 🟨 진행 중 · ✅ 완료

| 단계 | 상태 | 완료일 |
|------|:---:|--------|
| P1. 공통 모델/계약 (원본 §6–8) | ✅ | 2026-07-03 |
| P1a. key codec — dotnet 바이트 호환 (원본 §6.5) | ✅ | 2026-07-03 |
| P2. in-memory store + 계약 테스트 (원본 §13) | ✅ | 2026-07-03 |
| P3. location runtime — owner lease/lifecycle (원본 §5, 6.6, 9, 15.1, 16.1) | ✅ | 2026-07-03 |
| P4. 자동 연결 reconcile loop (원본 §14) | ✅ | 2026-07-03 |
| P5. spot/actor/route resolver 연결 (원본 §15–17) | ✅ | 2026-07-03 |
| P6. Redis extension (원본 §12) | ✅ | 2026-07-04 |
| P7. 등록 API/option (원본 §20.2, 20.4) | ✅ | 2026-07-03 |
| P7a. monitoring event source 교체 (원본 §20.5) | ✅ | 2026-07-03 |
| P8. 레거시 registry/discovery 제거 + 회귀 가드 (원본 §20.1) | ✅ | 2026-07-03 |
| P9. E2E 전환 (원본 §22) | ✅ | 2026-07-03 |
| P10. sample 전환 — TicTacToe·Bingo만 (원본 §22) | ✅ | 2026-07-03 |
| P11. POSD/DDD 리팩토링 루프 (원본 §21) | ✅ | 2026-07-04 |
| G. 완료 게이트 — codex 리뷰 통과 (누락 0건·리팩토링 요소 0건, 6절) | ✅ | 2026-07-04 |

## 2. 현재 상태 (2026-07-03 조사)

- location 계약 코드 0건 (`location_store`/`peer_location`/`owner_lease` 등 키워드로 소스·헤더
  grep 0건). core `zlink::service::discovery_t`/`registry_t`를 framework 런타임이 직접
  소유(`runtime/host/framework_runtime.cpp:49-61`)하고 socket에 `attach_discovery`로 위임한다:
  - 자동 연결: `runtime/channels/channel_outbound_exchange.cpp`(client dealer),
    `runtime/channels/channel_host_service.cpp`(server), `runtime/spots/spot_node_host_service.cpp`,
    `runtime/channels/route_channel_host_service.cpp`(route mesh router — spot route bridge
    attach 포함). registry 연결 헬퍼는 `runtime/registry/discovery_registry_connection.{hpp,cpp}`
  - embedded registry + topology query: `contracts/registry/registry.hpp`
    (`registry_builder_t`/`discovery_builder_t`/`registry_query_t`/`registry_query_client_t`),
    `runtime/registry/registry_runtime.{hpp,cpp}` (topology projection, spot route bridge)
  - spot resolve: `runtime/channels/route_channel_host_service.cpp`의
    `native_spot_route_discovery_bridge_t`, `registry_runtime`의 `resolve_spot_remote_address`
  - actor 위치: spot node 로컬 맵(`runtime/spots/spot_runtime.hpp`의 `actor_spot_rids`,
    `record_actor_spot_location_unlocked`) + `runtime/host/actor_gateway_spot_bridge.cpp`
- 확장 패키지 디렉토리는 codec 2종뿐(`extensions/framework-codec-{messagepack,protobuf}`) —
  Redis location extension 디렉토리가 없어서 신설이 필요하다. 신설 시
  `extensions/extension_boundaries.hpp`의 known extension descriptor 목록에도 등록한다.
- 등록 패턴: fluent builder. `contracts/configuration/framework_options.hpp`의
  `zlink_framework_options_t` + nested builder, 옵션 액션은 `add_zlink_action(...)`으로 지연 적용,
  상태/검증은 `contracts/configuration/detail/framework_options_{state,validation}.hpp`.
  제거 대상 API: `use_discovery()`, `enable_registry()`, `add_registry_peer()`,
  `use_registry_spot_remote_addresses()`, `use_registry_spot_resolver()` 계열(spot node/mesh 빌더
  포함).

## 3. 포팅 기준: dotnet 구현 inventory

아래 dotnet 구현을 기능 기준으로 포팅한다. 언어 관용(네이밍, 비동기 모델, builder 등록 패턴)은
cpp를 따르되 관찰 가능한 동작과 store에 저장되는 key/값은 같아야 한다.

| 구역 | dotnet 위치 (`framework/languages/dotnet/`) | 내용 |
|------|---------------------------------------------|------|
| 계약 | `src/Zlink.Framework/Contracts/Locations/{Stores,Models,Resolvers,Options}.cs` | store 5종 + 통합 `IZLinkLocationStore`, optional watch/change stamp, row/key/filter/page/topology/status 모델, resolver 4종 + `IZLinkLocationRuntimeQuery`, location option |
| 런타임 | `src/Zlink.Framework/Runtime/Locations/` (17개 파일) | `ZLinkLocationRuntime`(owner lease heartbeat), `ZLinkLocationLifecycle`(claim/renew/remove, 소유권 상실), `ZLinkAutoConnect{Planner,Loop,Reconciler}`, `ZLinkOwnerLeaseTracker`, `ZLinkObservedLocationGenerations`, `ZLinkLocationKeyCodec`, `ZLinkStoreLocationResolvers`, `ZLinkLocationAddressResolvers`, `ZLinkInMemoryLocationStore`, `ZLinkLocationRuntimeQueryService`, `ZLinkLocationEventEmitter` 등 |
| actor 소유권 | `src/Zlink.Framework/Runtime/Actors/ZLinkActorSessionLocationOwnership.cs` | `IgnoredStale` → local instance deactivate |
| 등록 API | `src/Zlink.Framework/Contracts/Configuration/Builders.cs:186-216`, `src/Zlink.Framework/Runtime/Configuration/Builders/ZLinkFrameworkOptionsBuilder.cs:93-138` | per-role `Add...LocationStore<T>()` 5종, 인스턴스 통합 등록 `AddLocationStore(store)`, `UseInMemoryLocationStores()`, `ConfigureLocations()`. 혼용은 검증 오류 |
| 호스팅 | `src/Zlink.Framework.AspNetCore/ZLinkLocationHostedService.cs` 외 2 | 시작 시 lease 등록 → advertise, 종료 시 lease 제거 + owner row bulk remove, polling event diff |
| Redis extension | `src/Zlink.Framework.Locations.Redis/` | `ZLinkRedisLocationStore`(통합 store + change stamp), Lua script 원자성, key prefix, row JSON codec |
| 테스트 | `tests/Zlink.Framework.ContractTests/Locations/LocationContracts.cs`(공유 스위트), `tests/Zlink.Framework.UnitTests/Runtime/*Location*·*AutoConnect*·OwnerLeaseTrackerTests.cs`, `tests/Zlink.Framework.Locations.Redis.Tests/` | InMemory·Redis가 같은 계약 스위트를 통과(parity). 원본 §22 필수 테스트의 실체 |
| 회귀 가드 | `tests/Zlink.Framework.UnitTests/Documentation/Regression.cs`, `tests/Zlink.Framework.SampleRegressionTests/Regression.cs` | 레거시 registry/discovery API 재유입 금지 |

### 3.1 언어가 달라도 지켜야 하는 것 (원본 draft 역참조)

- **key 직렬화 바이트 호환** — `ZLinkLocationKeyCodec`의 canonical 문자열(길이 prefix 세그먼트,
  rid는 소문자 hex, auto-connect type/role은 canonical 소문자 문자열)은 언어와 무관하게 같아야
  한다(§6.5). 서로 다른 언어 node가 같은 Redis를 공유하는 것이 전제다.
- **owner lease와 location row는 같은 물리 저장소**(§6.6) — `NewClaim`의 만료 판정 원자성.
- **resolver에 캐시 없음** — 모든 조회가 store에 도달(§8, §10).
- **claim-then-activate** 순서(§17)와 `IgnoredStale` 소유권 상실 규칙(§9).
- **compatibility wrapper 없음** — 기존 registry/discovery API는 한 번에 제거한다(§20.1).
- **레거시 코드는 작업하면서 정리한다** — 각 단계에서 새 location 표면으로 교체한 지점의
  레거시 코드(registry/discovery 계약·런타임·설정, dead config, 미사용 import/헬퍼/테스트)는
  다음 단계로 미루지 않고 그 단계에서 함께 삭제한다. deprecated 표기, 주석 처리, 죽은 코드로
  남기지 않는다. P8은 잔여분 최종 확인과 회귀 가드 추가 단계이지, 정리를 모아두는 단계가
  아니다.
- **wall clock 금지** — lease 만료는 store 기준 시각 + local monotonic 경과로만 판정(§6.6, 7.5).
- 바로 구현할 수 없는 항목은 feature map에 gap으로 남기고, sample에 private helper나 raw 우회
  경로를 넣지 않는다(§21 포팅 순서 4).
- **공통 e2e/sample 문서는 이미 location store 기준으로 변경되어 있다** — 공통 e2e 시나리오
  문서(`framework/doc/framework/common/e2e/` config-1~8)와 공통 sample 문서
  (`framework/doc/framework/common/sample/`)가 검증 기준 정본이다. e2e/sample 구현을 이
  문서들에 맞춰 변경해야 하며, 코드에 맞춰 문서를 되돌리지 않는다.
- **버그는 회피하지 않는다** — 진행 중 버그를 발견하면 우회(workaround, 재시도 감싸기, 테스트
  완화, 시나리오 축소)하지 않는다. 원인을 파악하고, 그 버그를 재현하는 회귀 테스트를 먼저
  작성한 뒤, 버그를 수정하고 나서 하던 작업을 이어간다. 버그가 기존 framework/core에 있어도
  같은 원칙을 적용한다.
- 포팅 완료 판정은 원본 draft **§24 구현 적용 확인표**를 cpp에 적용해서 한다. 단, §24.3의
  cache 관련 항목("대체 예정" 표기 2건)은 spot-address draft의 캐시 제거 결정에 따라 "구현하지
  않음"이 정답이다 — cache를 만들면 안 된다.

## 4. 작업 항목

- [x] P1. 공통 모델/계약: `contracts/locations/` 헤더 신설 — 모델/store/resolver/runtime query/
      option. 비동기 표면은 cpp framework의 기존 async 관례를 따른다
- [x] P1a. key codec — dotnet과 바이트 호환 검증 테스트 포함
- [x] P2. in-memory store + 계약 테스트(gtest, InMemory·Redis 공유 스위트)
- [x] P3. location runtime: owner lease heartbeat + lifecycle write 훅
      — spot: `runtime/spots/spot_runtime.cpp`(`actor_spot_rids` 갱신 지점),
        `runtime/registry/registry_runtime.cpp`의 spot route projection 대체
      — actor: `runtime/host/actor_gateway_runtime.cpp`(`update_actor_ref`/`destroy_actor`,
        claim-then-activate)
      — host: `runtime/host/framework_runtime.cpp` 시작/종료(lease 등록·제거, owner row bulk
        remove)
- [x] P4. 자동 연결 reconcile loop: `attach_discovery` 경로 전부를 store 기반 planner/loop/
      reconciler + core socket connect/disconnect로 교체
- [x] P5. resolver 연결: `native_spot_route_discovery_bridge_t`·
      `resolve_spot_remote_address` → location 기반 resolve로 교체
- [x] P6. Redis extension(`extensions/framework-locations-redis`) — vcpkg에 async Redis client
      의존 추가(`vcpkg.json`), Lua script, dotnet과 같은 key/값 스키마. Redis I/O는 core 메시징
      엔진의 고정 I/O 스레드와 공유하지 않고 전용 worker/event loop에서만 실행한다.
      CMake 타깃/export/install과 ctest 등록, `extensions/extension_boundaries.hpp` known
      extension 목록 반영, coverage 게이트(`ZLINK_FRAMEWORK_CPP_ENABLE_COVERAGE`) 통과까지 포함
- [x] P7. 등록 API: `zlink_framework_options_t`에 store 인스턴스/팩토리 등록 빌더 메서드 +
      `framework_options_state` 슬롯 + 검증. `use_discovery()`/`enable_registry()`/
      `use_registry_spot_resolver()` 계열 제거
- [x] P7a. monitoring: registry event source → location runtime event source(원본 §20.5)
- [x] P8. `contracts/registry/registry.hpp`·`runtime/registry/` 제거 + 회귀 가드
- [x] P9. e2e 전환: 공통 시나리오 문서(`framework/doc/framework/common/e2e/` config-1~8 전체)가
      검증 기준이며 dotnet 단계에서 이미 location/store 기준으로 rename됨
      (`config-1-location-messaging.ko.md`, `config-6-store-failure-recovery.ko.md`).
      cpp e2e 디렉토리는 legacy 이름이므로 전환 시 rename 또는 매핑 —
      `DiscoveryRegistryHa` → store 장애/복구, `RegistryMessaging` → location 기반 messaging,
      나머지 시나리오의 registry/discovery 전제 교체. 단위 테스트
      `test_cpp_framework_registry_topology.cpp` 교체. e2e 구현은 변경된 문서에 맞춰 바꾼다
      (코드에 맞춰 문서를 되돌리지 않는다)
- [x] P10. sample 전환: 공통 sample 문서(`framework/doc/framework/common/sample/`)도 이미
      location store 기준으로 변경되어 있으므로 sample 구현을 문서에 맞춰 변경한다 —
      registry host 제거, store 등록, actor 재연결 흐름, run script, README.
      **범위는 `TicTacToe`, `Bingo` 두 sample까지만이다** — 그 외 sample은 이번 포팅 범위에서
      제외한다
- [x] P11. POSD/DDD 리팩토링 루프
- [x] 원본 §24 확인표 점검 (§24.3 cache 항목은 spot-address draft 기준 "캐시 없음"으로 판정)
- [x] cross-language 검증: 같은 Redis instance에 dotnet과 cpp가 함께 붙어 서로의
      peer/spot/actor/route row를 읽는 검증 (key codec 바이트 호환의 실측 근거)

## 5. 검증

- 단위/계약: CMake 빌드 후 `ctest` (`ZLINK_FRAMEWORK_CPP_BUILD_TESTS=ON`)
- e2e: `e2e/<Scenario>/run_e2e.sh [SCENARIO|all]` (`ZLINK_CPP_E2E_BUILD_DIR`로 빌드 디렉토리 지정)
- sample: `ZLINK_FRAMEWORK_CPP_BUILD_SAMPLES=ON` 빌드 후 `ctest -L framework-sample-smoke`,
  또는 `ZLINK_CPP_BUILD_DIR=<build>`를 지정해 각 sample의 `run_sample.sh` 직접 실행

## 6. 완료 처리 기준 — codex 리뷰 게이트

이 문서의 모든 작업 항목이 구현·검증 통과로 체크된 뒤에도, 아래 두 가지 codex 에이전트 리뷰
게이트를 통과해야 문서를 완료 처리한다. codex 리뷰는 한 요청에 한 항목만 담고, 두 리뷰의 병렬
실행은 허용한다.

1. **적용 완결성 리뷰** — 원본 draft 계약(§24 확인표 포함, cache 항목은 3.1의 단서 적용)과 이
   문서의 체크리스트를 기준으로, cpp 구현에 누락되거나 왜곡 적용된 계약이 없는지 리뷰한다.
2. **POSD/DDD 리팩토링 리뷰** — AGENTS.md의 POSD 원칙(깊은 모듈, 정보 은닉, 복잡성 하향, 오류를
   정의로 제거, 위험 신호 제거)과 DDD 관점(owner lease, claim, fencing, reconcile 같은 location
   도메인 개념이 코드 구조와 이름에 그대로 드러나는가)으로 의미 있는 리팩토링 요소가 남아
   있는지 리뷰한다(원본 §21의 리뷰-리팩토링 루프와 같은 기준).

리뷰에서 나온 이슈는 반영하고(계약 변경이 필요해 보이면 구현 우회가 아니라 원본 draft 수정으로
분리) 전체 테스트 green을 확인한 뒤 재리뷰한다. 두 리뷰 모두 **남은 이슈 0건**이 될 때까지
반복하며, 0건이 되면 1절 상태 보드의 완료 게이트(G)를 ✅로 바꾸고 이 문서를 완료 처리한다.
P11(POSD/DDD 리팩토링 루프)의 완료 판정도 이 게이트의 리뷰 2 통과가 기준이다. 리뷰 라운드마다
지적·반영 요지를 진행 기록에 남긴다.

## 7. 진행 기록

| 날짜 | 내용 |
|------|------|
| 2026-07-03 | 진행 문서 작성. 현황 조사 완료 — core discovery attach 위임 구조, location 코드 0건 확인 |
| 2026-07-03 | codex 리뷰 반영: attach 경로 정정(streams 제외·route_channel_host_service 추가), P6에 CMake/ctest/extension boundary/coverage 게이트 추가, P9 config-1~8 전체로 확대, sample 검증 `ctest -L framework-sample-smoke`로 구체화, §24 cache 단서, P7a 상태 보드 추가 |
| 2026-07-03 | P1/P1a 완료: C++ public `contracts/locations/location.hpp`와 runtime key codec/test 신설. `ctest -R test_cpp_framework_location_key_codec` 통과, `test_cpp_framework_contract_headers.cpp` 문법 검사 통과 |
| 2026-07-03 | 전체 `zlink_framework` 빌드는 기존 `attach_discovery` 잔존으로 차단됨: `channel_host_service.cpp`, `channel_outbound_exchange.cpp`가 제거된 core discovery API(`auto_connect_type`, `attach_discovery`, `discovery_t::member_peers`)를 참조. 다음 단계는 P2/P3 후 P4 attach 경로 교체 |
| 2026-07-03 | P2 완료: runtime 내부 `in_memory_location_store_t` 추가, owner lease와 peer/spot/actor/route row를 한 저장소에서 관리, generation guard/change stamp/page/filter 계약 테스트 추가. `ctest -R 'test_cpp_framework_(location_key_codec\|in_memory_location_store)'` 통과 |
| 2026-07-03 | P3 진행: 독립 `location_runtime_t` 추가. start 시 owner lease 선등록, heartbeat, owner-stamped writes, IgnoredStale ownership-loss callback, stop 시 lease 제거와 owner row bulk remove 검증. actor/spot/framework host lifecycle 연결은 남음 |
| 2026-07-03 | P3 진행: runtime 내부 `location_lifecycle_t` 추가. actor location row의 claim-before-activate, conflict reject, owner generation token release, IgnoredStale deactivation callback 검증. `ctest -R 'test_cpp_framework_(location_key_codec\|in_memory_location_store\|location_runtime\|location_lifecycle)'` 통과. 실제 spot/actor/framework host 연결은 남음 |
| 2026-07-03 | P3 진행: `add_zlink_framework()` 기본 service graph에 in-memory `location_store_t`, `location_runtime_t`, `location_lifecycle_t`와 `location_host_service_t`를 등록. hosted service 시작 시 owner lease를 선등록하고 역순 종료에서 제거하도록 연결. `app.cpp` object target 빌드와 location focused ctest 통과. spot join/destroy 지점의 lifecycle write 연결은 남음 |
| 2026-07-03 | P4 완료: `channel_outbound_exchange`, `channel_host_service`, `spot_node_host_service`, `route_channel_host_service`의 core `attach_discovery`/native discovery 위임 제거. `location_auto_connect_host_service_t`가 client/server·route mesh peer row를 publish/reconcile하고, spot node host가 spot mesh peer row를 publish/reconcile한다. spot route bridge는 `route_location_t(SpotName)` store row 기반으로 교체. `zlink_framework` 빌드 통과, removed discovery API grep 0건, location focused ctest 통과 |
| 2026-07-03 | P8 일부 진행: core native registry/discovery 소유(`framework_runtime_t::discovery/registry`)와 embedded registry hosted service, `discovery_registry_connection` helper 제거. `registry_query_client_t`는 P8에서 public registry surface 삭제 전까지 명시적 disconnected stub로 둠 |
| 2026-07-03 | P3 완료: `spot_node_runtime_t`에 `location_lifecycle_t`를 바인딩하고, actor local join은 location row `NewClaim` 후 활성화, join 승인 후 row renew, admission reject 시 신규 claim release, entry 이동 renew, destroy 시 owner token release로 연결. spot create/close도 spot location claim/release로 연결. `test_cpp_framework_location_lifecycle`에 actor move renew와 spot claim/release 회귀 추가, focused location ctest 통과 |
| 2026-07-03 | P5 완료: `store_location_resolvers_t` 기본 resolver(peer/spot/actor/route)를 추가하고 DI에 등록. `spot_node_runtime_t::resolve_spot`의 legacy `registry_runtime_t::resolve_spot_remote_address` fallback을 `spot_location_resolver_t` store lookup으로 교체. `registry_query_t`/`registry_runtime_t`의 `resolve_spot_remote_address` 표면을 제거하고 validation도 discovery endpoint가 아니라 route channel만 요구하도록 변경. `test_cpp_framework_store_location_resolvers` 추가, `test_cpp_framework_registry_topology`의 stale native registry/discovery 검증 제거. actor 재연결 sample/e2e 호출부는 P9/P10에서 §17 흐름으로 전환 |
| 2026-07-03 | P6 진행: Redis client는 `redis-plus-plus`로 결정하고 `vcpkg.json`에 dependency를 추가. `zlink::framework_locations_redis` extension target/export/install 경로와 `locations-redis` extension descriptor 추가. `extensions/framework-locations-redis/include/zlink/locations/redis.hpp`에 Redis option과 row JSON codec 기반을 추가해 Actor/Route row의 PascalCase 필드, routing id hex, route value base64 스키마를 고정했다. `test_cpp_framework_locations_redis`와 extension descriptor test 통과. 아직 Lua script, 실제 `redis-plus-plus` store 구현, Redis 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: Redis key schema helper를 추가해 `row`/`gen`/`keys`/`own`/`lease`/`leases`/`stamp` 물리 키와 peer/spot/actor/route row key를 고정했다. Peer/Spot/Actor/Route JSON codec은 dotnet과 같은 PascalCase 필드, 숫자 enum, routing id hex, route value base64를 사용하도록 맞췄고 `test_cpp_framework_locations_redis`에 key/row 호환 회귀를 추가했다. `cmake --build ... --target test_cpp_framework_locations_redis -j2`, `ctest -R test_cpp_framework_locations_redis --output-on-failure` 통과. 실제 `redis-plus-plus` store, Lua script, Redis 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: dotnet `ZLinkRedisLocationScripts`와 같은 Redis Lua script 상수를 C++ Redis extension에 추가했다. Write는 Redis `TIME` 기준 `updatedAtMs`, owner lease 존재 시 `NewClaim` conflict, generation counter 유지, owner index/stamp 갱신을 원자 처리하고, Remove/RemoveByOwner/RenewLease/RemoveLease/ListLeases도 같은 키 스키마를 사용한다. script 반환값(`stored`/`conflict`/`stale`)을 C++ `location_write_result_t`로 매핑하는 helper와 회귀 테스트를 추가했다. `cmake --build ... --target test_cpp_framework_locations_redis -j2`, `ctest -R test_cpp_framework_locations_redis --output-on-failure` 통과. 실제 `redis-plus-plus` store 호출부, Redis 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: Redis extension public store 타입 `redis_location_store_t`를 추가해 peer/spot/actor/route/owner lease 통합 store와 change stamp store 계약 표면을 고정했다. 현재 로컬 빌드는 `redis++_DIR-NOTFOUND`이므로 write 계열은 `StoreUnavailable`, read 계열은 retriable failure를 반환하는 명시적 fallback으로 둔다. `test_cpp_framework_locations_redis`에 통합 계약 타입 변환과 client 미탑재 fallback 회귀를 추가했고 `cmake --build ... --target test_cpp_framework_locations_redis -j2`, `ctest -R test_cpp_framework_locations_redis --output-on-failure` 통과. 실제 `redis-plus-plus` 호출부, Redis 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: CMake Redis extension target이 vcpkg `redis-plus-plus`의 `redis++::redis++_static` target도 감지하도록 수정했다. `redis_location_store_t`의 peer/spot/actor/route write, owner-guarded remove, owner lease renew/remove, change stamp read를 `redis-plus-plus` client와 Lua script 호출로 연결했고, client가 없거나 Redis 연결이 실패하면 write 계열은 `StoreUnavailable`을 반환한다. 별도 vcpkg build dir(`build-redis-vcpkg`)에서 `redis-plus-plus:x64-linux@1.3.15` 설치 후 `cmake --build ... --target test_cpp_framework_locations_redis -j2`, `ctest -R test_cpp_framework_locations_redis --output-on-failure` 통과. 아직 row materialize 기반 resolve/list, RemoveByOwner/ListLeases 실제 호출부, Redis 서버 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: Redis row hash의 `json`/`gen`/`updatedAtMs`를 읽어 store-issued generation/update time을 row에 덮어쓰는 materialize 경로를 추가했다. Spot/Actor/Route resolve, Peer unpaged list, Spot/Actor/Route paged list, RemoveByOwner Lua 호출부를 `redis-plus-plus`로 연결했다. 일반 build와 vcpkg `build-redis-vcpkg` 모두에서 `cmake --build ... --target test_cpp_framework_locations_redis -j2`, `ctest -R test_cpp_framework_locations_redis --output-on-failure` 통과. 아직 ListLeases 실제 parsing, Redis 서버 연결 ctest, coverage gate는 남음 |
| 2026-07-03 | P6 진행: `list_owner_leases()`를 Redis `TIME`/`SMEMBERS`/`GET`/`PTTL` 기반으로 구현했다. snapshot store time은 Redis `TIME`을 사용하고 lease expiry는 Redis PTTL로 계산해 application wall clock을 쓰지 않는다. 만료된 lease index entry는 lazy cleanup한다. fallback 테스트에 list/RemoveByOwner/ListLeases read failure를 추가했고 일반 build와 vcpkg `build-redis-vcpkg` 모두에서 `test_cpp_framework_locations_redis` 빌드 및 ctest 통과. 아직 실제 Redis 서버 연결 ctest와 coverage gate는 남음 |
| 2026-07-03 | P6 진행: 사용자 피드백 반영. 이전 동기 Redis 호출 경로를 제거하고 `redis-plus-plus` async client(`async-std` feature, libuv event loop)를 사용하는 전용 `redis_location_worker_t`로 교체했다. Redis 명령은 worker thread에서만 future completion을 기다리므로 core 메시징 엔진의 고정 I/O thread와 공유하지 않는다. vcpkg `build-redis-vcpkg`에서 실제 Redis roundtrip(`RenewLease`→Actor `NewClaim`→resolve/list/change stamp/list leases/remove by owner)을 통과했고, 일반 build의 client 미탑재 fallback도 통과했다. coverage 전체 빌드는 P10 범위 Bingo sample protobuf 타입 누락으로 차단되어 P6 완료 표시는 보류한다 |
| 2026-07-03 | P6 coverage gate 재확인: `build-coverage-p6`를 `ZLINK_FRAMEWORK_CPP_BUILD_SAMPLES=OFF`, `ZLINK_FRAMEWORK_CPP_BUILD_E2E=OFF`, engine adapter off로 구성하고 framework test target만 빌드했다. Redis/location focused tests는 통과했지만 전체 `ctest -R test_cpp_framework_`는 기존 sample parity/doc layout/label/package/runtime 실패 9건과 coverage 36.9%(80% 미만)로 실패했다. 따라서 Redis 구현은 기능 검증 완료 상태이나 P6 체크박스는 전체 coverage gate 해결 전까지 보류한다 |
| 2026-07-03 | P7 진행: C++ `zlink_framework_options_t`에 `add_peer_location_store<T>()`, `add_spot_location_store<T>()`, `add_actor_location_store<T>()`, `add_route_location_store<T>()`, `add_owner_lease_store<T>()`, `add_location_store(shared_ptr<location_store_t>)`, `use_in_memory_location_stores()`, `configure_locations()`를 추가했다. per-role 등록은 C++ runtime의 통합 store 소비 구조에 맞춰 다섯 역할이 같은 `location_store_t` 구현 타입을 가리켜야 하며, 일부만 등록하거나 in-memory/instance 등록과 섞으면 검증 오류를 낸다. 앱 기본 in-memory store는 사용자 등록 이후 fallback으로만 등록되도록 옮겼고, location options가 `location_runtime_t`에 전달되도록 연결했다. `test_cpp_framework_store_location_resolvers`, public header smoke, Redis extension ctest 통과. legacy `use_discovery`/registry API 제거는 P8 잔여 범위다 |
| 2026-07-03 | P7 완료/P8 진행: `zlink_framework_options_t`의 `use_discovery()`, `enable_registry()`, `add_registry_peer()`, framework/spot mesh의 `use_registry_spot_remote_addresses()`/`use_registry_spot_resolver()` 계열을 제거하고 public header 회귀 가드를 추가했다. spot mesh는 `add_spot_node(name)`에서 내부 mesh 이름을 node 이름으로 자동 설정한다. route channel host는 `registry_query_t`/`registry_runtime_t` attach·detach 경로를 더 이상 받지 않고, app DI도 `registry_query_t` singleton을 주입하지 않는다. module/spot/registry topology 테스트와 C++ sample 호출부의 legacy API 사용을 제거했다. `contracts/registry/registry.hpp`와 `runtime/registry/` 자체 제거는 P8 잔여다 |
| 2026-07-03 | P7a 완료: monitoring public event source를 registry에서 location으로 교체했다. `add_registry_events()`/`registry_event_payload_t`/`registry_event_kind_t` 대신 `add_location_events()`/`location_event_payload_t`/`location_event_kind_t`를 사용하고, payload는 `location_runtime_status_t`, `location_topology_entry_t`, `location_service_summary_t`를 담는다. health check도 `add_location_check()`로 교체했다. `test_cpp_framework_monitoring` 빌드와 ctest 통과 |
| 2026-07-03 | P8 완료: public `contracts/registry/registry.hpp`, `runtime/registry/`, `discovery_snapshot_t`/`bind_discovery`, zlink builder registry/discovery 메서드, registry/discovery monitoring API, registry topology test target을 제거했다. 직접 route host service 단위 테스트는 legacy discovery 대신 명시 endpoint `connect()`로 같은 route dispatch 의미를 검증한다. 회귀 grep은 negative contract guard(`add_registry_peer`)와 location topology/handler serializer registry false positive만 남았다. `ctest -R 'test_cpp_framework_(contract_headers\|monitoring\|channel_messaging\|module_hosted\|store_location_resolvers\|spot_runtime\|locations_redis\|extensions)' --output-on-failure` 8/8 통과 |
| 2026-07-03 | P9 진행(Config-1 slice): C++ `RegistryMessaging` e2e의 registry role/target을 제거하고, provider/workflow/consumer가 Redis `redis_location_store_t`를 공유하도록 전환했다. `RM-A1`은 registry topology 조회 대신 provider HTTP endpoint의 `/locations/peers`가 `location_store_t::list_peers()` 결과를 반환해 peer location row 두 개를 검증한다. server bind가 location auto-connect에 publish되도록 `channel_builder_t::enable_server()`가 server discovery flag를 유지하게 수정했다. 검증: `ZLINK_CPP_E2E_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 180s framework/languages/cpp/e2e/RegistryMessaging/run_e2e.sh RM-A1` 통과, `ctest -R 'test_cpp_framework_(channel_messaging\|store_location_resolvers\|location_runtime\|locations_redis\|contract_headers)' --output-on-failure` 5/5 통과 |
| 2026-07-03 | P9 진행(Config-1 slice): RM-C4/RM-C5 store consumer 경로와 RM-C7 weighted 분산을 Redis location store 기반으로 재검증했다. C++ request 경로는 요청마다 임시 DEALER를 만들기 때문에, 자동 연결 bundle이 peer location row의 `weight`를 보존해 첫 후보 endpoint를 weight 비율대로 고르도록 수정했다. 회귀 테스트는 `test_cpp_framework_channel_messaging`에 75/25와 weight 0 후보 제외 검증을 추가했다. 검증: `ctest -R '^test_cpp_framework_channel_messaging$' --output-on-failure` 통과, `run_e2e.sh RM-A1`, `RM-C4`, `RM-C5`, `RM-C7` 통과. RM-C7 결과는 `api-a=75`, `api-b=25` |
| 2026-07-03 | P9 진행(Config-1 slice 완료): stale `DISCOVERY_CONSUMER` env 이름을 `STORE_CONSUMER` 기준으로 정리하고 Config-1 전체 sweep를 Redis location store 기반으로 통과시켰다. 이 과정에서 cross-process owner id 충돌로 scale-in 때 다른 process row가 함께 지워지는 문제를 고쳐 owner id에 monotonic tick/random/counter를 포함했고, client-server 자동 연결은 server rid와 충돌하지 않는 `dealer` role rid를 파생해 쓰도록 수정했다. weighted 자동 연결은 endpoint block이 아니라 weight slot을 interleave하도록 바꿔 equal weight 편향을 제거했고, runner cleanup은 bounded SIGTERM/SIGKILL로 고정했다. Redis Docker publish는 loopback port로 제한했다. 검증: `cmake --build ... --target test_cpp_framework_location_runtime test_cpp_framework_channel_messaging zlink_cpp_e2e_registry_messaging_provider zlink_cpp_e2e_registry_messaging_client -j2` 통과, `ctest -R '^(test_cpp_framework_location_runtime\|test_cpp_framework_channel_messaging)$' --output-on-failure` 2/2 통과, `ZLINK_CPP_E2E_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 560s framework/languages/cpp/e2e/RegistryMessaging/run_e2e.sh all` 통과(`logs/20260703-191402-27862`) |
| 2026-07-03 | P9 진행(Config-2 slice 완료): C++ SpotService에서 registry role/target과 `use_registry_spot_resolver()` 호출을 제거하고 play/session/gateway/multi-node/requester role이 Redis `redis_location_store_t`를 공유하도록 전환했다. runner는 외부 `ZLINK_REDIS_E2E_ENDPOINT`가 없으면 loopback Redis container를 시작하고 scenario별 key prefix를 사용한다. spot route bridge 기본 route channel은 spot node가 route mesh channel을 하나만 accept할 때 자동 선택하도록 보완했다. `SM-C1`에서 request timeout을 transport disconnect로 오인해 peer를 끊던 `native_route_backend_t` 정책을 수정해 timeout 뒤 같은 route channel의 정상 spot route request가 유지되도록 했다. 검증: `cmake --build framework/languages/cpp/build-redis-vcpkg --target zlink_cpp_e2e_spot_service_play zlink_cpp_e2e_spot_service_session zlink_cpp_e2e_spot_service_gateway zlink_cpp_e2e_spot_service_multinode zlink_cpp_e2e_spot_service_multinode_requester zlink_cpp_e2e_spot_service_client -j2` 통과, `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 260s framework/languages/cpp/e2e/SpotService/run_e2e.sh SM-C1` 통과(`logs/20260703-194053-28429`), `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 900s framework/languages/cpp/e2e/SpotService/run_e2e.sh all` 통과(`logs/20260703-194121-30065`) |
| 2026-07-03 | P9 진행(Config-3 slice 완료): C++ PubSub에서 registry role/target과 `use_discovery().add_registry_endpoint(...)` 경로를 제거하고 publisher/subscriber가 Redis `redis_location_store_t`를 공유하도록 전환했다. Subscriber는 publisher endpoint를 직접 받지 않고 location store의 fanout peer row를 통해 publisher를 발견한다. 이 과정에서 fanout auto-connect 누락을 수정했다. `location_auto_connect_host_service_t`가 publisher peer row를 publish하고 subscriber bundle에 discovered endpoint를 reconcile하도록 했으며, `channel_host_service_t`와 app inbound predicate가 discovery-only subscriber도 host하도록 보완했다. Redis live test에는 fanout peer write/list 회귀를 추가했다. 검증: `ctest -R '^(test_cpp_framework_channel_messaging\|test_cpp_framework_store_location_resolvers)$' --output-on-failure` 통과, 전용 Redis container로 `ctest -R '^test_cpp_framework_locations_redis$' --output-on-failure` 통과, `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 900s framework/languages/cpp/e2e/PubSub/run_e2e.sh all` 통과(`logs/20260703-200255-8623`) |
| 2026-07-03 | P9 진행(Config-4 slice 완료): RegistrationCodec는 공통 문서상 위치 resolve를 다루지 않는 수동 endpoint 연결 config라 Redis location store 등록이 필요 없다. 현재 C++ RegistrationCodec 코드에는 `use_discovery`/registry 호출이 없음을 grep으로 확인했고, location store 포팅 이후에도 정상 server, JSON-only peer, codec requester, invalid role, HTTP-only client runner가 통과했다. RC-A2는 C++ annotation/decorator public contract gap으로 유지한다. 검증: `cmake --build framework/languages/cpp/build-redis-vcpkg --target zlink_cpp_e2e_registration_codec_server zlink_cpp_e2e_registration_codec_invalid_duplicate zlink_cpp_e2e_registration_codec_json_only_peer zlink_cpp_e2e_registration_codec_codec_requester zlink_cpp_e2e_registration_codec_client -j2` 통과, `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 420s framework/languages/cpp/e2e/RegistrationCodec/run_e2e.sh` 통과(`logs/20260703-200739-24698`) |
| 2026-07-03 | P9 진행(Config-5 slice 완료): C++ ResilienceLifecycle에서 registry role/target과 `Server/Registry` 레거시 코드를 제거하고 Provider/Consumer가 Redis `redis_location_store_t`를 공유하도록 전환했다. Consumer `/topology`와 `/topology/wait`는 `location_store_t::list_owner_leases()`와 `list_peers()`를 조회하고 live owner row만 반환한다. runner는 외부 Redis가 없으면 loopback Redis container를 시작하고 RL-C4는 registry outage가 아니라 Redis location store pause/unpause를 검증한다. runtime drain 중 server weight override가 location peer row에 반영되지 않던 문제를 고쳐 auto-connect publish loop가 weight 변경 시 peer row를 `renew`하도록 했다. 검증: `cmake --build framework/languages/cpp/build-redis-vcpkg --target zlink_cpp_e2e_resilience_lifecycle_provider zlink_cpp_e2e_resilience_lifecycle_consumer zlink_cpp_e2e_resilience_lifecycle_client -j2` 통과, `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 900s framework/languages/cpp/e2e/ResilienceLifecycle/run_e2e.sh` 통과(`logs/20260703-205544-18048`) |
| 2026-07-03 | P9 진행(Config-6 slice 완료): C++ Config-6을 `DiscoveryRegistryHa` 레거시 registry HA 구현에서 Redis location store 기반 StoreFailure 구현으로 교체했다. CMake target은 `zlink_cpp_e2e_store_failure_provider`, `zlink_cpp_e2e_store_failure_consumer`, `zlink_cpp_e2e_store_failure_client`로 바꾸고 registry/embedded/probe target과 `Server/Registry`, `Server/Embedded`, `Server/Probe`, `Client/Scenarios/dr_*` 레거시 코드를 제거했다. provider/consumer는 Redis `redis_location_store_t`를 공유하고 `/query/status`, `/query/peers`, `/profile/request`, `/shutdown`, `/admin/crash` public endpoint만으로 SF-A1, SF-A2, SF-B1, SF-B2, SF-C1, SF-C2, SF-D1, SF-D2, SF-D3를 검증한다. 이 과정에서 `location_runtime_query_t` DI 구현을 추가하고 Redis/in-memory store read가 만료된 owner lease row를 제외하도록 고쳤으며, Redis async client에 500ms connect/socket timeout을 설정해 Redis pause가 core I/O thread와 무관한 전용 worker에서 bounded failure로 드러나게 했다. 검증: `cmake --build framework/languages/cpp/build-redis-vcpkg --target zlink_cpp_e2e_store_failure_provider zlink_cpp_e2e_store_failure_consumer zlink_cpp_e2e_store_failure_client -j2` 통과, `ZLINK_CPP_E2E_BUILD_DIR=/home/hep7/project/kairos/zlink/framework/languages/cpp/build-redis-vcpkg timeout 900s framework/languages/cpp/e2e/DiscoveryRegistryHa/run_e2e.sh all` 통과(`logs/20260703-212414-2415`, `logs/20260703-212420-3257`, `logs/20260703-212425-3891`, `logs/20260703-212433-4740`, `logs/20260703-212446-6015`, `logs/20260703-212508-7157`, `logs/20260703-212513-8240`, `logs/20260703-212523-9016`, `logs/20260703-212542-10036`) |
| 2026-07-03 | P9 진행(Config-7 slice 완료): C++ RuntimeMonitoring에서 `Server/Registry` role과 registry target, client registry option을 제거하고 service/filtered/throwing/trigger role이 Redis `redis_location_store_t`를 공유하도록 전환했다. `location_runtime_query_t` 기반 monitoring host service를 추가해 `add_location_events()` source가 `StatusChanged`, `TopologyChanged`, `ServiceSummaryChanged`를 발행한다. service SPOT mesh는 `.NET` 기준처럼 router endpoint와 pub/sub endpoint를 모두 공개하고, Redis location store의 spot mesh peer row로 peer를 발견한다. `run_e2e.sh`는 Redis-capable `build-redis-vcpkg`를 기본 build dir로 사용한다. 검증: `framework/languages/cpp/e2e/RuntimeMonitoring/run_e2e.sh` 통과(`logs/20260703-214231-52862`, MON-A1, MON-A2, MON-A3, MON-A4, MON-A5, MON-B1, MON-B2, MON-C1, MON-D1 passed) |
| 2026-07-03 | P9 완료(Config-8 slice 완료): C++ YieldDispatch에서 registry role/target과 registry discovery host 코드를 제거하고 Delay/Play/Session/Client가 Redis `redis_location_store_t`를 공유하도록 전환했다. Play/Session spot mesh는 `yield.spot.route`만 SPOT route bridge로 accept하고, Session은 control route와 spot route를 분리해 actor bind는 Play control route로, Spot/actor relay는 spot route로 보낸다. 이 과정에서 route handler invocation이 handler service scope를 만들도록 고쳤고, route mesh auto-connect가 Redis location peer row로 추가한 endpoint를 native route socket에 주기적으로 반영하도록 수정했다. `spot_context_t::request_to()`는 spot node가 accept한 route mesh가 하나일 때 그 channel을 사용해 control route와 spot route가 공존해도 target Spot으로 보낸다. 검증: `framework/languages/cpp/e2e/YieldDispatch/run_e2e.sh` 통과(`logs/20260703-222236-90101`, YD-A1~YD-D4, YD-E1, YD-E3, YD-E4 static gate, YD-E5 passed; YD-E2는 public cancellation token contract gap 유지) |
| 2026-07-03 | P10 완료(TicTacToe/Bingo sample slice): TicTacToe는 공통 sample 문서 기준처럼 location auto-connect를 쓰지 않는 수동 endpoint + Redis room route store 샘플로 유지하되, 사용하지 않는 registry topology 필드와 discovery 명명을 제거했다. Bingo는 registry role/target/runner 경로를 제거하고 API/Play/Session role이 Redis `redis_location_store_t`를 공유하도록 전환했다. match queue와 location store는 같은 Redis container를 쓰지만 key prefix를 분리한다. `common_codecs.hpp`는 메시지 계약 header를 직접 include해 Protobuf codec 등록이 include 순서에 의존하지 않게 했다. 검증: `ZLINK_CPP_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 240s framework/languages/cpp/samples/TicTacToe/run_sample.sh` 통과, `ZLINK_CPP_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 300s framework/languages/cpp/samples/Bingo/run_sample.sh` 통과 |
| 2026-07-03 | P11 진행(적용 완결성 1차 리뷰): P9/P10 범위와 core framework/Redis extension에서는 제거된 `attach_discovery`, `use_discovery()`, registry query/runtime, registry spot resolver 호출이 잡히지 않음을 확인했다. 범위 밖 DeliveryDispatch e2e에 남아 있던 제거된 public API 호출은 Redis `redis_location_store_t` 등록과 route mesh accept 방식으로 전환했고 registry target/runner role도 제거했다. 해당 e2e target 빌드는 통과했지만 runner는 `null reply: Server closed the connection`으로 실패해 후속 안정화가 필요하다. DeliveryDispatch sample registry target은 P10 범위 밖 잔여로 남아 있어 완료 게이트는 닫지 않는다. |
| 2026-07-04 | P11 진행: DeliveryDispatch sample도 registry role/target/runner 경로를 제거하고 Redis `redis_location_store_t` 공유 구성으로 전환했다. `Server/Registry` 빈 디렉터리까지 제거했고 sample parity/layout/label 계약 테스트의 stale registry label·sample label 기대값을 현재 location store 구조에 맞췄다. 검증: `ZLINK_CPP_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 420s framework/languages/cpp/samples/DeliveryDispatch/run_sample.sh` 통과, `ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^(test_cpp_framework_layout_contract|test_cpp_framework_label_contract|test_cpp_framework_messaging)$' --output-on-failure` 통과. |
| 2026-07-04 | P11 진행: DeliveryDispatch e2e의 Tracking role에서 같은 client-server channel을 `enable_server()`와 `use_handler_group()` 두 호출로 나눠 등록하던 레거시 패턴을 제거하고 한 builder chain으로 합쳤다. 이 split 때문에 `DeliveryStatusReq`가 tracking handler에 닿지 않아 dispatch center가 첫 status publish에서 실패했고, client는 `courier offer wait failed`로 끝났다. 같은 패턴이 남아 있던 sample Tracking도 함께 정리했다. 검증: `ZLINK_CPP_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 420s framework/languages/cpp/e2e/DeliveryDispatch/run_e2e.sh` 통과(`delivery-dispatch e2e result=passed`), `ZLINK_CPP_BUILD_DIR=framework/languages/cpp/build-redis-vcpkg timeout 420s framework/languages/cpp/samples/DeliveryDispatch/run_sample.sh` 재통과, `rg -n "attach_discovery|use_discovery\\(|enable_registry\\(|add_registry_peer\\(|use_registry_spot|registry_query_t|registry_runtime_t|discovery_registry_connection|bind_discovery"` no-hit(대상: C++ framework, P9 e2e, DeliveryDispatch sample/e2e, Bingo/TicTacToe sample). |
| 2026-07-04 | P6 coverage gate 재확인: coverage build의 stale `.gcda`/package consumer profile 충돌을 정리했고 `install_consumer.cmake`에서 consumer 실행 coverage 출력을 별도 `GCOV_PREFIX`로 격리했다. `ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_' -E '^test_cpp_framework_coverage_threshold$' --output-on-failure`는 36/36 통과했다. `test_cpp_framework_coverage_threshold`는 stamp mismatch 없이 실행되지만 전역 runtime line coverage가 54.27%(7804/14380, 102 files)로 80% 기준에 미달한다. 현재 script는 header-only location 구현을 basename `.gcov` 파일로 덮어쓰는 한계도 있어 location/Redis coverage 합산 기준 정리가 필요하다. Redis extension 기능 검증은 유지되지만 P6 완료 표시는 coverage 기준 해결 전까지 보류한다. |
| 2026-07-04 | P6 coverage gate 진행: coverage script가 object별 gcov 작업 디렉터리를 사용하고 source line 기준으로 중복 header coverage를 병합하도록 고쳤다. coverage build에서는 framework test target에도 `--coverage`를 적용해 tests가 직접 실행하는 header-only runtime/location 코드가 coverage에 잡히도록 했다. Redis extension test target도 coverage 대상에 포함했다. 최신 검증: `find ... -name '*.gcda' -delete && ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_' --output-on-failure`는 threshold를 제외한 36개가 모두 통과했고, threshold는 61.33%(7255/11829, 90 files)로 80% 기준 미달이다. focused location/Redis run 기준 `redis.hpp`는 75.60%(254/336)까지 측정된다. P6 완료 표시는 전역 coverage 기준 해결 전까지 보류한다. |
| 2026-07-04 | P6 coverage gate 진행: `test_cpp_framework_store_location_resolvers`에 `store_location_runtime_query_t` 상태/토폴로지/요약 조회, `location_auto_connect_host_service_t`의 client-server/fanout peer publish·cleanup, `location_monitoring_host_service_t`의 snapshot 발행·status-only fallback·빈 source no-op 회귀를 추가했다. 단일 타깃 검증은 통과했고 focused coverage는 `store_location_resolvers.hpp` 88.37%(114/129), `location_auto_connect_host_service.hpp` 65.65%(172/262), `location_monitoring_host_service.hpp` 94.44%(51/54)로 상승했다. 전체 `find ... -name '*.gcda' -delete && ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_' --output-on-failure`는 threshold 직전 36개가 모두 통과했지만 최종 coverage gate는 64.14%(7587/11829, 90 files)로 80% 기준에 미달한다. 주요 잔여 저커버 영역은 기존 `spot_node_host_service.cpp`, `stream_host_service.cpp`, `actor_gateway_spot_bridge.cpp`, `http-client/src/runtime/*` 쪽이며 Redis/location 기능 검증 완료와 별개로 P6 완료 표시는 계속 보류한다. |
| 2026-07-04 | P6 coverage gate 진행: app hosted-service 경로로 `spot_node_host_service_t`와 `stream_host_service_t` lifecycle 회귀를 추가했다. Spot mesh node는 public `add_spot_mesh()` 설정으로 local peer row publish와 app stop 후 cleanup을 검증하고, stream node는 public `add_stream_node()` 설정으로 loopback TCP listener start/stop을 검증한다. `test_cpp_framework_store_location_resolvers` 단일 실행은 통과했다. 전체 `find ... -name '*.gcda' -delete && ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_' --output-on-failure`는 threshold 직전 36개가 모두 통과했고 최종 coverage gate는 66.17%(7827/11829, 90 files)로 상승했지만 80% 기준에는 미달한다. 잔여 저커버 영역은 `http-client/src/runtime/compression.cpp`, `http-client/src/runtime/cookie_jar.cpp`, `actor_gateway_spot_bridge.cpp`, stream/spot host의 deeper packet path, actor/spot route dispatcher 쪽이다. |
| 2026-07-04 | P6 coverage gate 참고 측정: denominator에 포함되는 `test_cpp_http_client`를 framework tests와 함께 실행하면 http-client runtime coverage가 반영되어 threshold 수치는 69.85%(8262/11829, 90 files)까지 오른다. 이 run에서 `test_cpp_framework_channel_messaging`가 한 번 실패했지만 같은 build에서 단독 재실행하면 통과했다. 별도 non-framework sweep에서는 `test_cpp_http_client`, stream connector sample/perf smoke는 통과했고 `test_cpp_stream_connector`는 JSON parse error로 실패했다. 이 실패들은 P6 완료 조건을 닫지 못하게 하는 잔여 검증 이슈로 남긴다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `test_cpp_framework_ActorGateway_actor_session_relay`에 app 구성 경로에서 `configure_actor_gateway_spot_bridge()`가 설치한 public actor gateway → SPOT join/relay bridge를 검증하는 회귀를 추가했다. 기존 private dispatcher 직접 호출이 아니라 `app_t`, `actor_gateway_t`, `spot_node_builder_t`, typed JSON serializer 경로를 사용한다. 단독 검증은 통과했고 전체 `find ... -name '*.gcda' -delete && ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_' --output-on-failure`는 threshold 직전 36개가 모두 통과했다. 최종 coverage gate는 67.47%(7981/11829, 90 files)로 상승했지만 80% 기준에는 아직 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `test_cpp_framework_channel_messaging`의 actor route internal dispatcher 검증을 확장해 `can_handle_*`, route request reply decode, route send path를 같은 bound-session fixture에서 확인했다. 단독 `test_cpp_framework_channel_messaging`은 통과했다. 최신 전체 sweep은 threshold 직전 36개가 모두 통과하고, coverage gate만 67.67%(8005/11829, 90 files)로 실패한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: coverage sweep에 포함되는 `test_cpp_stream_connector`의 typed request 실패를 정리했다. request reply 내부 결과가 응답 codec을 잃고 `parse_json<T>()`만 호출하던 경로를 `request_reply_t{codec,payload}` 보존과 `apply_packet_payload()` 기반 decode로 교체했고, `json_stream_connector`의 legacy raw/protobuf JSON fallback도 제거했다. 단독 `test_cpp_stream_connector`는 통과했다. 최신 `find ... -name '*.gcda' -delete && ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^(test_cpp_framework_|test_cpp_stream_connector$)' --output-on-failure`는 기능 테스트 37개가 모두 통과하고, coverage gate만 67.97%(9617/14149, 104 files)로 실패한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: actor gateway → SPOT bridge 회귀를 보강했다. `build_route_internal_dispatchers()`가 명시 route와 spot node snapshot의 implicit route dispatcher를 모두 등록하는지 확인하고, remote SPOT/entry SPOT이 resolve됐지만 route mesh가 설정되지 않은 public error path가 `spot_route_not_found`로 내려오는지 검증했다. focused `test_cpp_framework_ActorGateway_actor_session_relay`, 인접 `test_cpp_framework_channel_messaging`/`test_cpp_stream_connector`는 모두 통과했다. 최신 전체 sweep은 기능 테스트 37개가 모두 통과하고, coverage gate만 68.11%(9637/14149, 104 files)로 실패한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: app stream host 회귀를 stop-only listener 검증에서 loopback TCP request/response roundtrip 검증으로 확장했다. test client가 stream frame을 직접 보내고 `echo_stream_session_t`가 `stream.reply_packet()`으로 응답해 `stream_host_service_t`의 `read_frame`/session dispatch/transport writer 경로를 밟는다. focused `test_cpp_framework_store_location_resolvers`, 인접 `test_cpp_framework_stream_framework`/`test_cpp_stream_connector`는 통과했다. 최신 전체 sweep은 coverage gate가 69.27%(9801/14149, 104 files)로 실패했고 `test_cpp_framework_http_integration`이 1회 실패했지만 단독 재실행은 통과했다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `test_cpp_framework_in_memory_location_store`에 owner token 기반 remove fencing, 만료된 owner lease row 필터링, 잘못된 continuation token fallback 회귀를 추가했다. focused `test_cpp_framework_in_memory_location_store`와 location bundle(`in_memory_location_store`, `store_location_resolvers`, `location_runtime`, `location_lifecycle`)은 통과했다. 최신 sweep은 `test_cpp_stream_connector` 포함 기능 테스트 37개 중 `test_cpp_framework_app_host`와 `test_cpp_framework_http_integration`이 5초대 실패를 1회 냈지만 두 타깃 단독 재실행은 통과했다. coverage gate는 69.33%(9809/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `location_auto_connect_host_service_t`의 route mesh dealer→router reconcile 회귀를 추가해 store peer row 발견 시 route runtime auto connection을 만들고, row 제거 시 auto connection만 해제하며 수동 `connect()` endpoint는 보존하는지 검증했다. spot node host 회귀는 pub/sub endpoint가 spot mesh peer row metadata(`pub-endpoint`)에 publish되는지까지 확장했다. focused `test_cpp_framework_store_location_resolvers` 통과. 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과했고 coverage gate는 69.28%(9803/14149, 104 files)로 실패한다. 같은 산출물에 `test_cpp_http_client`를 추가 실행하면 denominator에 포함된 http-client coverage가 반영되어 72.82%(10303/14149, 104 files)까지 오르지만 80% 기준에는 아직 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: app-level client/server actual request 회귀를 추가했다. 한 app 안에서 server와 discovery client가 같은 `location_store_t`를 공유하고, client는 직접 endpoint를 받지 않은 채 store에 publish된 server peer row를 `location_auto_connect_host_service_t`로 발견한 뒤 `channel_outbound_exchange` request 경로로 응답을 받는다. focused `test_cpp_framework_store_location_resolvers` 통과. 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과했고 coverage gate는 69.69%(9860/14149, 104 files)로 실패한다. 같은 산출물에 `test_cpp_http_client`를 추가 실행하면 73.22%(10360/14149, 104 files)까지 오르지만 80% 기준에는 아직 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `test_cpp_framework_spot_runtime`에 spot runtime이 `location_lifecycle_t`를 바인딩한 상태에서 SPOT create 시 spot location row를 claim하고, `close_spot()` 시 owner generation token으로 release하는 회귀를 추가했다. focused `test_cpp_framework_spot_runtime` 통과. 최신 sweep은 `test_cpp_framework_http_integration`이 5초대 실패를 1회 냈지만 단독 재실행은 통과했고, 다른 기능 테스트는 통과했다. coverage gate는 69.81%(9878/14149, 104 files)로 실패한다. 같은 산출물에 `test_cpp_http_client`를 추가 실행하면 73.33%(10376/14149, 104 files)까지 오르지만 80% 기준에는 아직 미달한다. |
| 2026-07-04 | P6/P11 안정화 진행: 반복되던 `test_cpp_framework_http_integration` 5초대 단발 실패를 추적하기 위해 app host test의 HTTP port 분산 폭을 넓히고, readiness 실패 시 endpoint와 마지막 client 오류 및 실패 phase를 출력하도록 보강했다. HTTP 앞부분 재현 sweep과 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과했다. coverage gate는 69.79%(9875/14149, 104 files)로 실패한다. 같은 산출물에 `test_cpp_http_client`를 추가 실행하면 73.33%(10375/14149, 104 files)까지 오르지만 80% 기준에는 아직 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: actor gateway 회귀를 보강해 app 구성 경로의 actor location resolver fallback을 검증했다. live owner lease가 있는 actor location row를 in-memory store에 등록하고, public `actor_gateway_t` relay가 로컬 route miss 뒤 `actor_location_resolver_t`를 거쳐 원격 actor 위치를 찾은 다음 route channel 미구성 오류를 `spot_route_not_found`로 반환하는지 확인한다. 같은 테스트에 actor bound-session 내부 route request dispatch를 추가해 `actor_route_internal_dispatcher_t`와 `route_packet_dispatcher_t`가 정상 response envelope를 만드는지도 검증했다. focused `test_cpp_framework_ActorGateway_actor_session_relay` 통과, 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과, `test_cpp_http_client` 추가 후 coverage gate는 73.64%(10420/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `test_cpp_framework_spot_runtime`의 route bridge fixture를 확장해 `spot_route_internal_dispatcher_t`가 SPOT actor packet route request와 disconnect route request를 실제로 처리하는지 검증했다. route join으로 만든 원격 actor에 `relay.request` packet을 보내 reply payload와 current actor ref update를 확인하고, disconnect route request가 `onDisconnectActor` callback과 accepted reply를 만드는지도 확인한다. focused `test_cpp_framework_spot_runtime` 통과, 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과, `test_cpp_http_client` 추가 후 coverage gate는 74.51%(10543/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: app-level fanout actual publish 회귀를 추가했다. 한 app 안에서 publisher와 discovery subscriber가 같은 `location_store_t`를 공유하고, subscriber는 직접 endpoint 없이 fanout peer row를 통해 publisher endpoint를 발견한 뒤 publish handler가 `profile.changed` event를 수신하는지 검증한다. focused `test_cpp_framework_store_location_resolvers` 통과, 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과, `test_cpp_http_client` 추가 후 coverage gate는 75.13%(10630/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: `spot_node_host_service_t`의 remote spot mesh reconcile 회귀를 추가했다. app-level spot mesh host가 local spot peer row를 publish한 뒤, 같은 store에 들어온 remote spot peer row와 `pub-endpoint` metadata를 발견하고, 같은 remote node rid의 endpoint takeover를 처리한 다음 stop 시 local row만 제거하고 remote row를 보존하는지 확인한다. focused `test_cpp_framework_store_location_resolvers` 통과, 최신 clean sweep(`test_cpp_framework_*` + `test_cpp_stream_connector`, threshold 제외)은 37/37 통과, `test_cpp_http_client` 추가 후 coverage gate는 75.81%(10726/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: diagnostics와 Redis extension의 public 실패 계약 회귀를 보강했다. `test_cpp_framework_message_flow`는 sample rate 0의 healthy transition suppression, error transition 보존, 비동기 observer callback, diagnostics enum name token을 검증한다. `test_cpp_framework_spot_timer`는 timer 기본 생성/cancel/move, 빈 이름 reject, unknown exception failure event를 검증한다. `test_cpp_framework_spot_runtime`은 미구성 `spot_context_t`의 close/outbound/request 실패 계약을 검증한다. `test_cpp_framework_locations_redis`는 Redis unavailable 상태에서 peer/spot/route/owner lease/change stamp public method가 write는 `StoreUnavailable`, read는 retriable failure를 반환하는지 검증한다. focused 테스트는 모두 통과했고, 최신 coverage gate는 76.62%(10841/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: route bridge와 actor route 내부 dispatcher의 실패 계약을 보강했다. `test_cpp_framework_channel_messaging`은 route packet dispatcher의 malformed envelope, unsupported route message kind, body 없는 routed command, endpoint 없는 client request의 `disconnected` 실패를 검증한다. `test_cpp_framework_ActorGateway_actor_session_relay`는 actor bound-session route internal dispatcher가 body 없는 send/request를 `request_protocol_error`로 거부하는지 검증한다. `test_cpp_framework_locations_redis`는 Redis unavailable 상태의 direct `remove_actor()`도 `StoreUnavailable`로 반환하는지 추가 검증한다. focused `channel_messaging`/`ActorGateway_actor_session_relay`/`locations_redis`는 통과했고, 최신 coverage gate는 76.81%(10868/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: channel/spot/location lifecycle의 public 실패 계약을 추가로 보강했다. `test_cpp_framework_channel_messaging`은 serializer가 연결된 endpoint 없는 client request가 `disconnected`를 반환하고, 바인딩되지 않은 route request와 기본 `route_client_t`의 route/spot request가 `request_protocol_error`를 반환하는지 검증한다. `test_cpp_framework_spot_runtime`은 비어 있거나 route mesh가 없는 `spot_context_t::send_to()`/`request_to()`가 `spot_route_not_found`로 끝나는지, `spot_node_runtime_t::bind_spot_location_resolver()` 뒤 `resolve_spot()`이 store resolver로 위임해 `spot_address_t`의 node/spot 값을 반환하는지 확인한다. `test_cpp_framework_location_lifecycle`은 추적되지 않은 actor update/renew/release가 stale로 무시되고, spot claim 충돌과 추적되지 않은 spot release가 저장소 row를 깨지 않는지 검증한다. focused `channel_messaging`/`spot_runtime`/`location_lifecycle`/`stream_connector`는 통과했지만 coverage gate는 77.19%(10921/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: actor gateway와 stream connector, SPOT disconnect 경로를 public 계약 중심으로 보강했다. `test_cpp_framework_ActorGateway_actor_session_relay`는 serializer 미설정 typed create, 빈 actor bind, actor type mismatch, stale generation, destroy 실패 계약을 검증한다. `test_cpp_stream_connector`는 manual dispatch의 즉시 매칭 async wait, close 이후 async wait 실패, pending wait가 있는 immediate connector에서 sync request가 async request pump로 위임되는 경로를 검증한다. `test_cpp_framework_spot_runtime`은 erased disconnect의 빈 actor ref, stale generation, 없는 actor, 없는 spot context, actor factory 미등록 실패를 확인한다. threshold 제외 37개 sweep는 통과했고 coverage gate는 77.72%(10997/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 coverage gate 진행: actor gateway, channel runtime, stream connector의 남은 실패 계약을 추가 보강했다. `test_cpp_framework_ActorGateway_actor_session_relay`는 handler 미등록 relay request, unbound/disconnected relay request, join 실패 경로를 검증한다. `test_cpp_framework_channel_messaging`은 native route backend close/no-bridge 실패 계약, SPOT route client send/request backend 전달, standalone channel capability builder 계약을 검증한다. `test_cpp_framework_spot_runtime`은 `spot_node_manager_t` wrapper의 create/find/list/resolve/close와 기본 manager 실패 계약을 검증한다. `test_cpp_stream_connector`는 내부 async send/request pump의 closed/disconnected/validation/write failure와 receive/wait failure 계약을 검증한다. focused `actor_gateway`/`channel_messaging`/`spot_runtime`/`stream_connector`는 통과했고, coverage gate는 79.19%(11204/14149, 104 files)로 아직 80% 기준에 미달한다. |
| 2026-07-04 | P6/P11 완료: coverage gate의 마지막 미달분을 public 계약 중심 회귀로 보강했다. `test_cpp_framework_spot_runtime`은 `leaveActor`/`destroyActor` 경계와 manager typed create/get-or-create를 검증하고, `test_cpp_framework_channel_messaging`은 channel/runtime option, route builder, message bus, capability/channel/zlink builder move 계약을 검증한다. `test_cpp_stream_connector`는 codec registry, 기본 connector, observer registration move/close 계약을 검증한다. 검증: `ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^(test_cpp_framework_|test_cpp_stream_connector$)' -E '^test_cpp_framework_coverage_threshold$' --output-on-failure` 37/37 통과, `ctest --test-dir framework/languages/cpp/build-coverage-p6 -R '^test_cpp_framework_coverage_threshold$' -V` 통과. coverage 최종 수치는 80.01%(11320/14149, 104 files)이다. |
| 2026-07-04 | cross-language 검증 완료: 같은 `zlink-redis-test` Redis instance(`127.0.0.1:16379`)에서 shared prefix `zlink:cross:cpp-dotnet:1783105314`를 사용했다. `.NET`의 `Dotnet_Writes_Rows_For_Cpp_To_Read`가 dotnet prefix에 peer/spot/actor/route row를 쓰고 C++ `CrossLanguageReadsDotnetRows`가 모두 읽었다. C++ `CrossLanguageWritesRowsForDotnetToRead`가 cpp prefix에 같은 네 종류 row를 쓰고 `.NET`의 `Dotnet_Reads_Cpp_Rows`가 모두 읽었다. C++ Redis target 검증도 `ctest --test-dir framework/languages/cpp/build-redis-vcpkg -R '^test_cpp_framework_locations_redis$' --output-on-failure` 통과. |
| 2026-07-04 | 완료 게이트 리뷰 완료: 적용 완결성 리뷰에서 원본 §24 확인표를 재점검했고 §24.3 cache 항목은 spot-address 정본에 따라 캐시 미구현이 맞다고 판정했다. `attach_discovery`, `use_discovery()`, `enable_registry()`, `add_registry_peer()`, registry query/runtime/connection 계열 검색은 C++ framework/e2e/sample 범위에서 제거 검증용 negative contract test 외 실사용 no-hit였다. `channel_outbound_exchange`, `channel_host_service`, `spot_node_host_service`, `route_channel_host_service` 경로는 location resolver/store 기반 자동 연결·SPOT route bridge 회귀와 coverage gate로 검증됐다. POSD/DDD 리뷰에서도 owner lease, claim, fencing, reconcile 개념이 location runtime/store/resolver 모듈 안에 모여 있고 Redis I/O가 전용 async worker thread로 격리되어 남은 리팩토링 이슈 0건으로 판정했다. 완료 게이트 G를 닫는다. |

> 갱신 규칙: 단계 착수/완료 때 1절 상태 보드와 4절 체크리스트를 갱신하고, 이 표에 한 줄 기록을
> 남긴다. 계약이 바뀌면 이 문서가 아니라 원본 draft를 고치고 여기서는 참조만 한다.
