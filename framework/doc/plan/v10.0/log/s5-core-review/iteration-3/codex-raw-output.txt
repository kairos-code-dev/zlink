S5 Core iteration 3 independent review raw output
reviewer: R1 / Codex (GPT-5)
start: 2026-07-17T21:15:27+09:00
end: 2026-07-17T21:21:00+09:00
snapshot: 25617130eeeb1dc464aec6eca1a8378888aee42a

[required inputs]
read first:
- framework/doc/plan/v10.0/log/s5-core-review/iteration-3/manifest.ko.md
- framework/doc/plan/v10.0/log/s5-core-review/iteration-2/finding-ledger.ko.md
reviewer contract applied: findings contain issue, file:line evidence, impact,
change scope and verification direction; no coordinator design choice is a
finding unless the selected implementation violates the frozen contract.

[scope hash at start]
command:
files=$(git ls-files core/include core/src core/tests core/packaging \
  core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | sort -u)
printf '%s\n' "$files" | xargs sha256sum | sha256sum
result:
START_COUNT 631
1ca3763f80f568890e2e3e888b4b1dd93e2d15dab9deb9125c12b316fa1f6598  -
verdict: manifest count/hash match; the required f6598 suffix matches.

[snapshot and delta]
git rev-parse HEAD
25617130eeeb1dc464aec6eca1a8378888aee42a

git diff --stat a01b537f8ce..25617130ee
30 files changed, 983 insertions(+), 66 deletions(-)
verdict: manifest delta matches.

git diff --check a01b537f8ce..25617130ee -- core \
  .github/workflows/core-conan-release.yml
result: no output (clean)

[iteration-2 finding verdicts]
F-I1-01(재) PARTIAL / UNRESOLVED (high)
- Delta adds unreachable_out_ in mesh_wire.hpp:61-72 and mesh_wire.cpp:279-358.
  Commit failures under NODROP now increment unreachable_out_ at
  mesh_wire.cpp:346-356 and return OK. publish_common subtracts that post-commit
  value from the already captured snapshot at mesh_messaging_api.cpp:901-918.
- Frozen contract mesh-node.ko.md:367-371 creates one snapshot before delivery,
  and lines 380-386 require every target in that snapshot to accept or none.
  Relabeling a commit-time failure after partial delivery does not change which
  targets were in the snapshot. Therefore partial success remains possible.
- Impact: a successful NODROP call can omit one snapshotted remote target while
  another receives the message; reported snapshot history is rewritten.
- Change scope: remote NODROP reserve/commit and publish detail accounting in
  mesh_wire.{hpp,cpp} and mesh_messaging_api.cpp.
- Verification direction: force a peer pipe to disappear between reserve and
  commit and assert either zero delivery with backpressure or delivery to every
  original snapshot target, with immutable snapshot accounting.

F-I1-03(재) PARTIAL / UNRESOLVED (high)
- Delta adds session_bindings_pending and session_bindings_remove_actor
  (mesh_stream_session_api.cpp:1168-1198) and calls them from actor destroy
  (mesh_actor_api.cpp:677-737).
- Actor destroy sets draining at mesh_actor_api.cpp:635-642. However bind_actor
  validates only existence and generation under node mutex at
  mesh_stream_session_api.cpp:595-611, releases that mutex, and later inserts
  the binding under the session registry/service locks at lines 618-675.
- A bind that validates before actor removal can insert after the destroy-side
  removal pass and then complete successfully at lines 686-689. The bound-send
  path trusts the stale binding without revalidating actor liveness at lines
  1018-1068. Thus the new-admission gate and final removal are not atomic.
- Impact: destroy can return success while a session retains and uses a binding
  to the destroyed Actor generation; a concurrent bind can also report success
  for authority that no longer exists.
- Change scope: the internal Actor-draining admission check and the atomic
  ordering between bind registration and destroy-side binding removal.
- Verification direction: pause bind after Actor validation, complete destroy,
  resume bind, and assert it returns the draining/stale result and leaves no
  binding or successful bound send for the destroyed generation.

N1 RESOLVED
- timer_api.cpp:131-153 removes registration, releases scheduler mutex, cancels
  a parked Spot turn and only then waits for scheduler_busy_refs.
- mesh_api.cpp:296-325 rechecks the registry cancellation while parked.
- lifecycle test lines 481-541 covers overlap destroy and claim-held cancel.

N-I1-01/N3 RESOLVED
- mesh_runtime.cpp:703-712 pins monitor_emit_refs while holding node mutex and
  releases it at lines 760-763.
- mesh_monitor_api.cpp:148-168 clears node->monitor and waits for the pin count
  to drain before delete. The former capture/delete TOCTOU is closed.

N-I1-02 RESOLVED
- mesh_actor_api.cpp:709-731 captures spot_present before maybe_end_spot_locked
  can erase the iterator and does not use spot_it after erase/unlock.

N2 RESOLVED
- mesh_wire_ingress.cpp:518-550 decrements active_actor_count, admits LEFT and
  then calls maybe_end_spot_locked for the previous Spot generation.

N-I2-01 RESOLVED
- mesh_api.cpp:250-276 creates Spot timers via zlink_timer_new_for_spot_node.
- timer_scheduler_backend.cpp:231-250 resolves one scheduler per MeshNode;
  lines 270-284 remove it at node destroy, called by mesh_node_api.cpp:454-457.
- This isolates Spot claim waits from generic timers and other MeshNodes. The
  coordinator-selected per-MeshNode topology is consistent with the contract.

N-I3-01 RESOLVED
- core-conan-release.yml:65-87 parses the selected conandata entry and fails
  unless sha256 is a string of length 64. The current 10.0.0 entry intentionally
  fails this gate until S6-05 fills its tag-derived digest.
- README.md:7-18 documents URL + sha256 and the mandatory workflow gate.

N-I3-02 RESOLVED
- git ls-files core/packaging/conan contains only .gitignore, README.md,
  conandata.yml and conanfile.py; pycache is untracked and ignored.
- README.md:13-18 now uses 10.0.0/kairos-code-dev and the core/v<VERSION> tag.

N-I3-03 RESOLVED
- candidate git diff --check is clean; mesh_wire.cpp ends with one newline and
  no extra blank line.

N4 RESOLVED as declaration removal
- valid_utf8_public has no source/header use. mesh_c_internal.hpp:34-40 no
  longer declares it. A residual comment is recorded separately as new I3.
- zlink_timer_cleanup_spot has no use, and timer_handle_t no longer carries the
  obsolete owner_spot field (timer_api_internal.hpp:18-55).

[new findings]
N3-I1-01 high: local allocation can break NODROP all-or-none after remote commit
- Issue/evidence: publish_common commits the remote leg first at
  mesh_messaging_api.cpp:844-861. It allocates and copies each local record only
  afterward at lines 863-889; allocation failure returns OUT_OF_MEMORY at
  lines 864-868 and copy failure returns INTERNAL_ERROR at lines 881-882.
- Contract: service/01-mesh-node.ko.md:380-386 requires all snapshot targets or
  none for NODROP and details for every successful result.
- Impact: a remote target and earlier local targets can receive the message
  while the call returns failure and later local targets receive nothing;
  retry can duplicate delivery.
- Change scope: Logical Multicast reserve/commit preparation in
  mesh_messaging_api.cpp (and only the internal wire seam if needed).
- Verification direction: inject deterministic local record allocation/copy
  failure with at least one remote and two local targets; assert zero delivery
  on failure, or prove all fallible preparation finishes before first commit.

N3-I3-01 low: removed declaration leaves an orphan source comment
- Issue/evidence: mesh_c_internal.hpp:35-40 contains the standalone comment
  "UTF-8 validity for public names and topics" followed by no declaration;
  the delta deleted only valid_utf8_public.
- Impact: the internal header advertises a nonexistent helper and leaves the
  N4 cleanup incomplete for maintainers.
- Change scope: remove or relocate that one stale comment in
  mesh_c_internal.hpp; no behavior/public API change.
- Verification direction: scoped grep/visual header review confirms the comment
  describes an actual adjacent declaration, and valid_utf8_public remains absent.

[P4 whole-scope review]
Reviewed from the current snapshot rather than accepting the iteration-2
ledger as proof:
- core/src/runtime/services/mesh/ all 8 files: wire codec/admission/ingress/
  egress split, ownership, peer generation, reply/transfer and event paths.
- core/src/api/mesh/ all 11 files: lifecycle, dispatch, messaging, Actor,
  transfer, monitor and STREAM session paths.
- timer_api.cpp, timer_api_internal.hpp and timer_scheduler_backend.cpp.
- socket_base* common runtime, especially routed_target_writable and linger.
- core/include/zlink public headers, five test_mesh_* sources, contract checks,
  core/CMakeLists.txt, Conan recipe/workflow, and both internals documents.

[static and contract gates]
unittest_public_contract_headers:
1 Tests 0 Failures 0 Ignored; OK

contract_public_surface:
PUBLIC SURFACE CONTRACT: PASS
functions=196 exports match, removed identifiers absent

removed-identifier word-boundary scan:
no production hit; only the intended absence assertion at
test_thread_safe_contract_policy.cpp:156.

zero-byte scan: no hit.
candidate diff check: clean.
CMake: all four mesh wire translation units and all five test_mesh targets are
registered (core/CMakeLists.txt:878-890; core/tests/CMakeLists.txt:93-97).
YAML: workflow and conandata parse successfully. The 10.0.0 digest gate returns
EXPECTED_FAIL_MISSING_DIGEST, as required before S6-05 fills the digest.

[dynamic mesh evidence]
The current five core/build/bin/test_mesh_* binaries were invoked read-only.
Every case failed at MeshNode start with result 705 because this sandbox denies
TCP bind; no test body was reached:
- test_mesh_node_basic: 8/8 pre-body failures
- test_mesh_peer_admission: 11/11 pre-body failures (including spawned sides)
- test_mesh_monitor_matrix: 6/6 pre-body failures
- test_mesh_stress: 3/3 pre-body failures
- test_mesh_lifecycle_contracts: 8/8 pre-body failures
These results are neither defect evidence nor positive proof. No build was run
because the action contract forbids writes below core/. Manifest-reported
85/85, ASAN clean and TSAN classification remain prior evidence only.

[known risks]
TSAN auto-HWM lock-order: ACCEPTED / tracked, no new RouteMesh finding.
ctx_auto_hwm_recalc.cpp:80-116 still holds _slot_sync while preparing/applying
socket plans. No independent TSAN body run was possible.

TSAN raw command-mailbox ypipe: ACCEPTED / tracked, no new RouteMesh finding.
mailbox.cpp:39-56 and 89-97 serialize cpipe send/recv with _sync, but the prior
machine warning has not been independently eliminated here.

ctx_term linger: ACCEPTED existing raw-socket risk / tracking remains.
socket_base.cpp:129-134 defaults blocky-context linger to -1 and
socket_base_lifecycle.cpp:137-161 terminates attached pipes with that lifecycle.
test_mesh_peer_admission.cpp:2035-2039 still uses a 1500 ms teardown ordering
delay; it avoids rather than proves a bounded ctx_term. No new S5 regression
evidence was found.

[axis verdict]
I1 NOT CLEAN: F-I1-01 and F-I1-03 remain high; N3-I1-01 is a new high.
I2 CLEAN: no finding. Per-MeshNode scheduler and current module boundaries do
not add a contract-free public surface or caller-visible internal policy.
I3 NOT CLEAN: N3-I3-01 low orphan comment. Packaging/public-surface/diff gates
are otherwise clean.

[scope hash at end]
time: 2026-07-17T21:21:00+09:00
HEAD: 25617130eeeb1dc464aec6eca1a8378888aee42a
END_COUNT 631
1ca3763f80f568890e2e3e888b4b1dd93e2d15dab9deb9125c12b316fa1f6598  -
verdict: start, end and manifest count/hash all match; core/ and workflow have
no working-tree changes.

overall: NOT CLEAN
