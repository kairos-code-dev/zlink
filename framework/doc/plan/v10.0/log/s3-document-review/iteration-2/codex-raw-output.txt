동결 hash는 시작·종료 모두 `12d908d39baa0a6bbaaaddbdedc465f464dc9bc3e8dc1c03494b0be49b36d749`로 일치했다.

[원칙][high] framework/doc/framework/spec/server/languages/cpp/02-framework-interfaces.ko.md:141 — 공개 interface spec이 최소 파일 구조와 내부 runtime owner를 규정한다 — `src/runtime/channels/*`의 파일별 책임과 .NET 내부 타입 대응까지 서술해 spec 독자 범위를 벗어난다 — 공개 signature와 계약만 남기고 runtime 구조는 internals 또는 v10.0 임시 plan으로 옮긴다.

[원칙][high] framework/doc/framework/spec/server/languages/cpp/61-embedded-http-server.ko.md:160 — 정식 spec에 단계별 구현 계획과 미래 전환을 기록한다 — “1차 구현”과 “perf 고도화에서 … 전환”은 현재 10.0.0 목표 계약이 아니며, 같은 문서의 `테스트 계획`과 완료 gate도 구현 진행 문구를 포함한다 — 최종 보장만 기술하고 단계·gate는 v10.0 plan으로 옮긴다.

[원칙][low] framework/doc/framework/spec/server/languages/cpp/61-embedded-http-server.ko.md:445 — 존재하지 않는 `cpp-http-hosting.ko.md`를 계약 원본으로 지목한다 — 같은 디렉터리의 실제 문서는 `60-http-hosting.ko.md`다 — 실제 문서에 대한 Markdown 링크로 교체한다.

[원칙][low] framework/doc/framework/spec/server/languages/java/02-handler-interfaces.ko.md:1228 — 존재하지 않는 `system-structure.ko.md`를 세 번 반복 참조한다 — Java spec 디렉터리의 실제 문서는 `01-system-structure.ko.md`다 — 실제 소유 문서와 해당 절을 한 번만 정확히 링크한다.

[원칙][medium] framework/languages/node/e2e/SpotService/feature-map.ko.md:35 — table cell 내부의 escape되지 않은 `|`가 행을 여러 열로 분리한다 — 221개 문서 table 검사에서 이 행만 header보다 많은 cell을 생성했다 — pipe를 escape하거나 marker를 table 밖 code block으로 옮긴다.

[원칙][low] core/doc/spec/core/socket/03-sub.ko.md:9 — `control plane`을 “제어 플레인”으로 번역했다 — 문서 원칙 7.1~7.3은 `data plane`·`control plane` 같은 익숙한 jargon을 영어 그대로 쓰도록 정한다 — `control plane`으로 쓰고 필요하면 쉬운 설명을 먼저 붙인다.

[원칙][low] core/doc/spec/core/socket/README.ko.md:8 — `data plane`을 “데이터 플레인”으로 음차했다 — 문서 원칙 7.2는 소스와 연결되는 이름 `data plane`을 그대로 보존하도록 명시한다 — `data plane API`로 고친다.

[원칙][low] framework/doc/framework/common/e2e/config-2-spot-service.ko.md:332 — actor 위치를 “actor가 사는 spot”으로 의인화한다 — AGENTS.md는 actor가 “사는가”를 “존재하는가”로 쓰도록 명시하며 같은 표현이 362·382행에도 반복된다 — 세 위치 모두 “actor가 존재하는 Spot/노드”로 고친다.

[원칙][low] framework/doc/framework/common/sample/event/gamequest.ko.md:317 — reconnect를 “다른 노드로 붙으면”이라는 구어체로 표현한다 — AGENTS.md는 연결 대상을 “붙는다”로 표현하지 않도록 명시한다 — “다른 노드에 다시 연결하면”으로 고친다.

[1차소스][high] framework/languages/cpp/e2e/AutomaticTurnDispatch/feature-map.ko.md:3 — 존재하지 않는 이전 Config 8 문서와 `ATD-*` 19개를 정본으로 사용한다 — 현재 정본은 `config-8-execution-turn.ko.md`의 `TD-A1`~`TD-G1` 27개이며 공통 규칙은 모든 ID를 행으로 요구한다 — 정본 경로를 바꾸고 27개 `TD-*` 시나리오를 각각 매핑한다.

[1차소스][high] framework/languages/java/e2e/AutomaticTurnDispatch/feature-map.ko.md:3 — Config 8을 낡은 `ATD-*` 19개 track으로 고정한다 — 현재 정본은 27개 `TD-*` 시나리오이고 기존 경로 `config-8-automatic-turn-dispatch.ko.md`는 존재하지 않는다 — 현재 정본의 각 ID별 상태와 근거로 다시 작성한다.

[1차소스][high] framework/languages/java/e2e-kotlin/AutomaticTurnDispatch/feature-map.ko.md:3 — 존재하지 않는 Config 8 경로와 `ATD-*` 증거를 사용한다 — 현재 `config-8-execution-turn.ko.md`는 `TD-*` 27개를 정의하므로 map의 source와 scenario 의미가 일치하지 않는다 — 27개 `TD-*`를 현재 Kotlin 상태에 맞춰 각각 매핑한다.

[1차소스][medium] framework/languages/node/e2e/AutomaticTurnDispatch/feature-map.ko.md:9 — 여러 Config 8 ID를 `TD-A1~A5` 같은 범위 한 행으로 합쳤다 — 공통 E2E README 462행은 config의 모든 시나리오 ID를 행으로 두도록 요구한다 — 27개 ID를 개별 행으로 분리해 상태와 gap을 각각 기록한다.

[1차소스][medium] framework/languages/java/e2e-kotlin/SpotActorTransfer/feature-map.ko.md:9 — Config 10 ID를 범위별 한 행으로 묶었다 — 정본은 `ST-A1`~`ST-F6` 20개를 개별 시나리오로 정의하고 공통 규칙도 ID별 행을 요구한다 — 20개 시나리오를 각각 한 행으로 분리한다.

[1차소스][medium] framework/languages/node/e2e/SpotActorTransfer/feature-map.ko.md:10 — `ST-A1/A2/A3`처럼 여러 시나리오를 한 행에 병합했다 — Config 10의 admission·failure·ordering 조건은 ID별로 독립적이며 공통 README 462행이 개별 행을 요구한다 — 각 `ST-*` ID를 별도 행으로 만든다.

[1차소스][high] framework/languages/cpp/e2e/RegistryMessaging/feature-map.ko.md:14 — P0 `RM-B3 provider crash failover` 행이 없다 — 정본 `config-1-location-messaging.ko.md:182`가 RM-B3를 정의하지만 map은 RM-B2 다음 RM-C1로 넘어가며 scoped 검색도 일치 항목이 없다 — RM-B3의 구현 상태와 failover 증거를 별도 행으로 추가한다.

[1차소스][high] framework/languages/java/e2e-kotlin/RegistryMessaging/feature-map.ko.md:16 — P0 `RM-B3`가 누락됐다 — 정본 182행의 provider crash failover 시나리오가 map의 RM-B2와 RM-C1 사이에 존재하지 않는다 — 미구현이어도 명시적 상태·원인과 함께 RM-B3 행을 둔다.

[1차소스][medium] framework/languages/java/e2e-kotlin/DiscoveryRegistryHa/feature-map.ko.md:26 — `SF-E1` store 지연 비블로킹 시나리오가 없다 — 정본 `config-6-store-failure-recovery.ko.md:203`은 P1 SF-E1을 정의하지만 map은 SF-D3에서 끝난다 — SF-E1을 개별 행으로 추가하고 harness 대기 여부를 명시한다.

[1차소스][high] framework/languages/cpp/e2e/SpotService/feature-map.ko.md:68 — P0 `SM-C6 Logical Multicast NoDrop 기본 정책`이 누락됐다 — 정본 `config-2-spot-service.ko.md:316`은 기본 `NoDrop=true`와 atomic admission을 요구하지만 map은 SM-C5 다음 SM-D1로 넘어간다 — SM-C6의 구현 또는 gap을 별도 항목으로 기록한다.

[1차소스][high] framework/languages/dotnet/e2e/SpotService/feature-map.ko.md:28 — P0 SM-C6 행이 없다 — Config 2 정본 316~325행의 기본 NoDrop·backpressure 계약이 SM-C5와 SM-D1 사이에서 누락됐다 — SM-C6 상태와 blocking/non-blocking 검증 근거를 추가한다.

[1차소스][high] framework/languages/java/e2e/SpotService/feature-map.ko.md:82 — P0 SM-C6이 누락됐다 — 정본은 SM-C6을 독립 시나리오로 정의하지만 Java map은 SM-C5 다음 SM-D1로 이동하고 scoped 검색도 일치 항목이 없다 — SM-C6을 별도 항목으로 추가한다.

[1차소스][high] framework/languages/java/e2e-kotlin/SpotService/feature-map.ko.md:79 — 정본 시나리오 `SM-B9`, `SM-C5`, `SM-C6`, `SM-D15`가 모두 빠졌는데 남은 parity 항목이 없다고 주장한다 — Config 2는 네 ID를 각각 240·306·316·480행에서 정의하며 map 193행은 대기 항목이 없다고 단정한다 — 네 ID를 각각 구현·blocked·deferred 중 하나로 기록하고 근거를 붙인다.

[1차소스][high] framework/languages/node/e2e/SpotService/feature-map.ko.md:44 — P0 SM-C6 행이 없다 — 정본 316행의 NoDrop 기본 정책이 SM-C5와 SM-D1 사이에서 누락됐고 87행의 실행 ID 목록에도 SM-C6이 없다 — SM-C6의 상태와 실제 backpressure 검증 증거를 별도 행으로 추가한다.
