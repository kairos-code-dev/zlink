S5 Core iteration 4 independent review raw output
reviewer: R1 / Codex
snapshot: 59b3ea9400327e74e80b0f8d74763c89ccfdf141
mode: core/ read-only; only this raw output and codex-review.ko.md are written

[required inputs]
read first:
- framework/doc/plan/v10.0/log/s5-core-review/iteration-4/manifest.ko.md
- framework/doc/plan/v10.0/log/s5-core-review/iteration-3/finding-ledger.ko.md
reviewer contract applied: each finding contains issue, file:line evidence,
impact, change scope and verification direction. No implementation design is
selected by the reviewer.

[scope hash at start]
command:
files=$(git ls-files core/include core/src core/tests core/packaging \
  core/CMakeLists.txt core/doc/spec/core core/doc/internals CHANGELOG.md | sort -u)
printf '%s\n' "$files" | wc -l
printf '%s\n' "$files" | xargs sha256sum | sha256sum
result:
631
d9621658e16a04c53c646286f3386d99f6ac28faf3196616eec4d71e51a8e413  -
verdict: manifest count/hash match.

[snapshot and delta]
git rev-parse HEAD
59b3ea9400327e74e80b0f8d74763c89ccfdf141

git diff --stat 25617130e..59b3ea940
10 files changed, 742 insertions(+), 21 deletions(-)

source delta:
- core/src/api/mesh/mesh_actor_api.cpp: +3/-1
- core/src/api/mesh/mesh_c_internal.hpp: +0/-1
- core/src/api/mesh/mesh_messaging_api.cpp: +28/-19
- core/src/api/mesh/mesh_stream_session_api.cpp: +19/-0
- remaining six files are iteration-3 review records.

git diff --check 25617130e..59b3ea940 -- core \
  .github/workflows/core-conan-release.yml
result: no output (clean)

[iteration-3 fix verdicts]

F-I1-03(재재): NOT RESOLVED (high)
- The new post-insert check exists at
  core/src/api/mesh/mesh_stream_session_api.cpp:685-702. It only runs for the
  call that inserted the binding (`!idempotent`).
- Binding discovery marks a concurrent identical call idempotent at
  core/src/api/mesh/mesh_stream_session_api.cpp:634-655, and that call skips
  the new check at lines 685-703.
- A concrete ordering remains: bind A validates before destroy; destroy erases
  the Actor and finishes its binding-removal pass; A inserts after that pass;
  already-validated bind B observes A's binding as idempotent and returns OK;
  then A detects staleness and removes the binding at lines 698-701. B can
  therefore return successful bind after destroy while no binding remains.
- This contradicts the binding contract that bind validates Actor generation
  and membership epoch and that a successful bind produces a terminal success
  for that binding (core/doc/spec/core/service/05-stream-session.ko.md:106-112),
  and the Actor destroy contract that bound session control is drained before
  successful destroy (core/doc/spec/core/service/04-actor.ko.md:177-182).
- No deterministic concurrent bind/destroy regression appears in the five
  mesh test files; bind coverage is sequential at
  core/tests/integration/test_mesh_node_basic.cpp:565-598 and
  core/tests/integration/test_mesh_peer_admission.cpp:1610-1743.
- Impact: a caller receives a successful bind operation for a destroyed Actor
  generation while the binding has already been or is immediately removed.
- Change scope: Actor-generation validation and the atomic ordering of
  idempotent/new binding registration against Actor destroy removal in
  mesh_stream_session_api.cpp and mesh_actor_api.cpp.
- Verification direction: deterministically pause two binds after initial
  Actor validation, complete destroy's removal pass, then order insert A,
  idempotent observation B, and A revalidation; assert neither bind succeeds
  and no binding remains.

N3-I1-01: NOT RESOLVED (high)
- Local queued_record_t objects and their payload copies are now prepared
  before the remote leg at
  core/src/api/mesh/mesh_messaging_api.cpp:844-870. This resolves the specific
  record construction/copy window.
- The remote leg still commits first at lines 872-889. Afterwards local commit
  performs `std::deque::push_back` and `std::set::insert` at lines 891-898.
  Their container types are declared at
  core/src/runtime/services/mesh/mesh_runtime.hpp:132-145 and :521-525; these
  operations can allocate and are not infallible moves. No catch or rollback
  surrounds this post-remote sequence.
- The frozen contract requires every target in the snapshot to accept or none
  (core/doc/spec/core/service/01-mesh-node.ko.md:367-386) and explicitly calls
  the guarantee an atomic reserve/commit over the whole snapshot (same file
  :460-462).
- Impact: allocation failure after remote commit can escape the C ABI or
  terminate the process; if surfaced, remote targets may have received while
  local targets have not, so the original partial-delivery class remains.
- Change scope: the local mailbox/ready-index commit preparation and the
  remote/local publish commit boundary in mesh_messaging_api.cpp and the
  relevant internal container seam only.
- Verification direction: inject allocation failure separately at the first
  local records push and ready-index insert after a remote target is reserved;
  assert zero delivery on failure and no exception crosses the public C API.

N5: RESOLVED
- After the node lock is reacquired, owner_it is explicitly reloaded at
  core/src/api/mesh/mesh_actor_api.cpp:677-686 before the deadline branch uses
  it at lines 689-696. The stale map iterator is no longer reused.

N6/N3-I3-01: RESOLVED
- The orphan UTF-8 comment was removed. check_name is now immediately followed
  by the Node owner declarations at
  core/src/api/mesh/mesh_c_internal.hpp:32-40.

[F-I1-01 rejected reconsideration]
verdict: REBUTTED; finding remains high.
- Section 5 says peer drain excludes the peer from new snapshots but does not
  cancel an already committed message
  (core/doc/spec/core/service/01-mesh-node.ko.md:241-254).
- That clause does not redefine the already captured publish snapshot.
  Section 7 separately and expressly requires one snapshot and all targets in
  it to accept or none at lines 367-386. Section 9 repeats that Logical
  Multicast guarantees atomic reserve/commit for the entire snapshot at
  lines 460-462.
- The implementation captures ADMITTED peers into remote_targets at
  core/src/api/mesh/mesh_messaging_api.cpp:760-777. If a later commit fails,
  wire_publish_remote_locked calls it unreachable at
  core/src/runtime/services/mesh/mesh_wire.cpp:339-358, and publish_common
  subtracts it from the already captured snapshot at
  core/src/api/mesh/mesh_messaging_api.cpp:910-929.
- Therefore the implementation's statement that the peer "had already left at
  snapshot time" is not what the observed code does: it was ADMITTED when the
  vector snapshot was taken and was lost later. The no-cancellation clause
  explains why an earlier per-target commit is not recalled; it does not grant
  retroactive snapshot mutation or limit all-or-none to capacity failures.
- Impact: a successful NODROP call can deliver to an earlier remote target,
  omit a later snapshotted target, and report a smaller snapshot with zero
  drops.
- Change scope: remote NODROP snapshot, reserve/commit and publish detail/event
  accounting in mesh_wire.{hpp,cpp}, mesh_messaging_api.cpp and the raw routed
  probe seam.
- Verification direction: capture two admitted targets, force the second pipe
  to disappear after reserve but before its commit, and assert the public
  outcome and accounting against both the all-target-or-none rule and the
  committed-message non-cancellation rule.

[P4 full campaign scope]
Reviewed from the current snapshot rather than adopting iteration-3 verdicts:
- mesh runtime: all 8 files under core/src/runtime/services/mesh, including
  runtime state, wire codec, admission, ingress and outbound paths;
- mesh API: all 11 files under core/src/api/mesh, with lifecycle, dispatch,
  messaging, Actor transfer, monitor and STREAM session paths;
- timer: core/src/api/monitoring/timer_api.cpp and
  timer_scheduler_backend.cpp, including per-node scheduler destruction;
- raw socket seam: routed_target_writable and ROUTER check/write behavior at
  core/src/runtime/sockets/common/socket_base_msg.cpp:92-104,
  socket_base_routing.cpp:216-225 and router_send_path.cpp:147-219;
- public include closure and contract gate;
- the five test files registered at core/tests/CMakeLists.txt:93-97;
- core/tests/contract, core/CMakeLists.txt, Conan packaging, build/release
  workflows, CHANGELOG.md and core/doc/internals.

Current executable/static evidence:
- core/build/bin/unittest_public_contract_headers: 1 test, PASS.
- check_public_surface.py against core/build/lib/libzlink.so: PASS,
  functions=196, exports match, removed identifiers absent.
- .github/workflows/build.yml,
  .github/workflows/core-conan-release.yml and
  core/packaging/conan/conandata.yml parse as YAML.
- CMake registers all five mesh tests at core/tests/CMakeLists.txt:93-97 and
  all split wire translation units at core/CMakeLists.txt:886-890.
- Removed-identifier production scan found no live core/include or core/src
  occurrence; historical removal notes remain only in CHANGELOG.md.
- No zero-byte tracked file was found in the 631-file scope.
- The five current regular mesh binaries were executed without rebuilding.
  All 36 cases failed at MeshNode start result 705 before the test bodies due
  this environment's TCP bind restriction, matching the prior review
  limitation. These runs are not counted as pass or defect evidence. The
  manifest's 85/85 and ASAN clean results remain coordinator-provided evidence,
  not independently reproduced in this pass.

[new finding]
N4-I3-01 (low, I3)
- Issue/evidence: two maintained internals pairs still assign ingress, codec
  and admission to only `mesh_wire.cpp/hpp` at
  core/doc/internals/posd-module-structure.ko.md:124-140 and
  core/doc/internals/architecture.ko.md:1218-1227 (English mirrors at
  posd-module-structure.md:115-132 and architecture.md:1249-1258). The actual
  runtime is split into mesh_wire, mesh_wire_codec, mesh_wire_admission and
  mesh_wire_ingress, as correctly recorded in
  core/doc/internals/services-internals.ko.md:12-23 and built at
  core/CMakeLists.txt:886-890.
- Impact: maintainers are directed to the wrong ownership boundary for wire
  decoding, peer admission and ingress lifecycle, undermining the documented
  POSD module map and source tree.
- Change scope: the affected mesh rows/tree entries in the Korean and English
  POSD module-structure and architecture internals documents only.
- Verification direction: compare every mesh runtime file in the internals
  inventory with `git ls-files core/src/runtime/services/mesh` and the CMake
  source list, then verify Korean/English descriptions remain equivalent.

[known risks]
- TSAN auto-HWM lock-order: ACCEPTED/TRACKED, no new RouteMesh finding. The
  `_slot_sync`-held plan prepare/apply path remains at
  core/src/runtime/core/ctx_auto_hwm_recalc.cpp:80-116. TSAN was not rerun, so
  the existing warning is not claimed resolved.
- TSAN raw command mailbox ypipe: ACCEPTED/TRACKED, no new RouteMesh finding.
  send and recv access the cpipe under `_sync` at
  core/src/runtime/core/mailbox.cpp:39-56 and :89-97. No independent dynamic
  evidence removed the existing warning.
- ctx_term linger: ACCEPTED/TRACKED as an existing raw-socket risk, not a new
  S5 finding. A blocky context still defaults socket linger to -1 at
  core/src/runtime/sockets/common/socket_base.cpp:129-134, while termination
  closes attached pipes at socket_base_lifecycle.cpp:137-161. The MIXED test's
  1500ms ordering at core/tests/integration/test_mesh_peer_admission.cpp:2035-2039
  is not bounded-termination proof.

[axis verdicts]
I1 contract implementation: NOT CLEAN
- F-I1-01 high: rejected disposition rebutted.
- F-I1-03 high: bind revalidation fix leaves concurrent idempotent-success race.
- N3-I1-01 high: post-remote local container allocation remains fallible.

I2 POSD/DDD: CLEAN
- no independent I2 finding. Runtime/wire/API/timer/socket responsibility
  boundaries remain separated in code; the stale documentation is classified
  under I3 and the I1 races are not double-counted as design findings.

I3 cleanup completeness: NOT CLEAN
- N4-I3-01 low: stale internals module/source inventory.
- N5 iterator repair and N6 orphan-comment cleanup are clean; public surface,
  removed identifiers, CMake registration, package/workflow syntax and diff
  hygiene produced no additional I3 finding.

[scope hash at end]
command: identical to start
result:
END_COUNT 631
END_HASH d9621658e16a04c53c646286f3386d99f6ac28faf3196616eec4d71e51a8e413  -
HEAD 59b3ea9400327e74e80b0f8d74763c89ccfdf141
verdict: count/hash/HEAD match start and manifest; core scope unchanged.

CORE REVIEW NOT CLEAN
