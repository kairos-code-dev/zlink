# .NET Framework ledger review log

## 기록 범위

- 대상 문서: [`../dotnet-framework-spec-gap-ledger.ko.md`](../dotnet-framework-spec-gap-ledger.ko.md)
- 기록 시각: 2026-08-02 09:28:59 KST
- 기준 `HEAD`: `9efee01aa39ace3db8e0f50c46ba9c12864f2cc2`
- 기준 branch: `agent/framework-contract-runtime-update`
- `origin/agent/framework-contract-runtime-update`는 같은 commit을 가리킨다.
- 목적: 전체 문서 리뷰에서 확인한 기준점, gate 분모, 링크, history 분리와 heading 구조를 ledger에 반영한다.

## Working tree 조건

대상 문서와 이 review에서 새로 만든 log artifacts를 제외한 기존 dirty 변경은 보존한다. 전체 `git status --short` 결과는
[`working-tree-status.txt`](working-tree-status.txt)에 그대로 기록했다. 이 manifest에는 C++,
Java/Kotlin, Node.js의 ledger·E2E·test 변경과 이 review 대상의 상태가 모두 포함된다. 이 변경들은
이 review의 대상이 아니며 reset, checkout, overwrite하지 않는다.

재현 명령은 다음과 같다.

```text
git status --short
git hash-object framework/doc/plan/dotnet-framework-spec-gap-ledger/dotnet-framework-spec-gap-ledger.ko.md
git diff --no-ext-diff --binary -- framework/doc/plan/dotnet-framework-spec-gap-ledger/dotnet-framework-spec-gap-ledger.ko.md | sha256sum
git diff --numstat -- framework/doc/plan/dotnet-framework-spec-gap-ledger/dotnet-framework-spec-gap-ledger.ko.md
```

이 review 시점의 manifest fingerprint는 다음과 같다.

| 항목 | 값 |
|---|---|
| `git status --short` SHA-256 | `4f896675301d5c3aed170fd6a63b8954cef99ada4dc49f2b8728135f15710be3` |
| status line 수 | `247` |
| 대상 문서 content hash | `521aea26bf5458a995f4ed033366e1dbbb949cdd` |
| 대상 문서 diff SHA-256 | `a72498740dba6ed1e8af640b22b1a3c2f3f034c2a80ca9fffd1b4b35965e497d` |
| 대상 문서 diff numstat | `194 insertions, 289 deletions` |

`working-tree-status.txt`와 이 표의 값은 대상 문서와 review log를 추가로 수정하지 않는 한 같은
working tree를 재현하는 기준이다.

### Review model

이 변경은 `.NET` source, public contract와 test를 수정하지 않는 문서·log 구조 변경이다. 따라서
독립 reviewer는 `gpt-5.6-sol`에 `medium` reasoning을 지정했다. Source/public API, concurrency 또는
최종 compliance audit이라면 Sol High 이상으로 올린다.

## 적용한 review finding

1. 문서 기준점을 현재 `HEAD`와 `origin`으로 갱신하고, 재실행하지 않은 A-G0 candidate 결과를 과거
   evidence로 명시했다.
2. `DN-IMP-019~023`을 Phase A checklist와 Phase B 선행 framework gate의 전체 분모에 포함했다.
3. Framework API, Runtime monitoring과 Spot model의 깨진 anchor를 실제 heading에 맞췄다.
4. `DN-IMP-013`의 과거 판정은 current-state 문장에서 제거하고 현재 public shape만 남겼다.
5. A-G0/A-G1의 상세 실행·review history는 이 log로 이동하고 ledger 본문에는 현재 gate와 log 링크만 둔다.
6. Phase A/B heading 번호와 repeated phase card ID를 정리하고, 관련 heading을 명사구로 정리한다.

## DN-IMP-013 history

초기 audit에서는 `ZLinkActorJoinCompletion.Failed`에 `IsRetriable`가 있었고 exact interface와 source
shape가 달랐다. 이후 Sol review에서 retry advice를 public completion contract에 넣지 않는 방향을
선택했고, source·API snapshot·contract test·sample payload를 `OperationId`와 `Kind`만 노출하도록
맞췄다. `ZLinkDeferredActorJoin`은 retry 여부를 public result에 복사하지 않는다.

이 history는 현재 contract 설명이 아니다. 현재 source는
`framework/languages/dotnet/src/Zlink.Framework/Contracts/Actors/IZLinkActorContext.cs`의
`Failed(OperationId, Kind)`와 exact interface를 사용한다.

## A-G0 historical evidence

- 최초 기준은 `3291e338f4f700484780560cd81345a647ef0948`와 dirty working tree였다.
- 기존 contract test는 `74/74`, documentation regression은 `20/20`, sample build는 세 sample에서
  warning·error 0건이었다.
- Roslyn exact declaration과 type owner gate를 추가한 뒤 `DN-IMP-019~023`과
  `DN-TEST-007/008`의 gap을 확인했다.
- source·runtime policy·test·package snapshot을 보강한 뒤 exact declaration과 owner test는 각각
  `2/2 PASS`가 되었고, 전체 ContractTests는 `76/76`, 전체 UnitTests는 `1388/1388`, targeted
  relocation test는 `248/248`, sample regression은 `134/134`였다.
- Package verifier는 source/package public API, clean consumer와 standalone HTTP consumer를 통과했다.
- `ST-C3`의 source-leave failure ordering gap과 Config별 process selector gap은 A-G1 이후 열린 조건으로
  유지했다.
- 과거 Sol High review 결과와 여러 Sol Medium review 결과는 당시 candidate의 review history이며,
  현재 reviewer의 독립 `CLEAN` 증거로 사용하지 않는다.

## A-G1 historical design review

- `DN-IMP-017` review는 accepted operation·actor handoff의 epoch, owner generation fence,
  Shutdown takeover와 rollback lease를 함께 검사하도록 설계를 보강했다. zero-transition waiter와
  owner-aware reopen을 분리한 뒤 해당 card는 `6/6 PASS`, Sol Medium `CLEAN`으로 기록되었다.
- `DN-IMP-011`과 `DN-IMP-010` review는 Relocate 시작 시 고정한 absolute deadline, post-commit
  authority, committed count와 terminal reason을 같은 owner가 보존하도록 순서를 정했다.
- `DN-IMP-007` review는 partial commit 뒤 committed target owner를 유지하고 미commit source workload만
  복원하며, concurrent Shutdown takeover를 `Blocked/ShutdownRequested`로 구분하도록 정리했다.
- 이후 실제 scheduler-level regression에서 full UnitTests가 `1397/1398`로 실패하고 Sol Medium이
  `Medium 1`, `NOT CLEAN`을 판정한 기록이 있다. 이 조건은 A-G1 완료 evidence가 아니며
  [`20260802-085146-g1-scheduler-regression.ko.md`](20260802-085146-g1-scheduler-regression.ko.md)의
  후속 작업으로 유지한다.

## 1차 Sol Medium review 결과

1차 독립 review는 `gpt-5.6-sol / medium`에서 `NOT CLEAN`으로 판정했다. finding은 다음과 같았다.

- `High`: working-tree 조건이 요약 목록뿐이고 Java/Kotlin ledger와 Node AutomaticTurnDispatch 변경 등
  일부 상태를 재현할 수 없었다.
- `Medium`: log의 A-G0/A-G1 표기가 Phase A card ID와 분리되지 않았다.
- `Medium`: DN-IMP와 E2E inventory 일부 heading이 문장형이었다.

다음 조치로 전체 status를 별도 manifest에 고정하고 대상 content hash·diff hash·numstat을 이 log에
기록했다. Phase A/B card reference를 `A-G*`/`B-G*`로 통일했으며, DN-IMP와 DN-TEST·DN-E2E inventory
heading을 명사구로 정리했다. 대상 문서의 변경은 이 조치와 관련된 문서 구조에만 한정한다.

## 2차 Sol Medium review 결과

1차 finding 반영 뒤 독립 `gpt-5.6-sol / medium` read-only 재리뷰를 실행했다. 별도 model-selection
skill은 사용하지 않았으며, 문서·log 구조 review라는 범위에 Sol Medium이 적정하다는 판단을 확인했다.
결과는 `NOT CLEAN`이었다.

- `High`: 다른 dirty 작업이 review 중에도 유입되어 manifest와 fingerprint가 다시 stale 상태가 되었다.
  재리뷰가 관찰한 status line 수는 `247`이었고, 현재 target hash와 log에 기록한 값이 서로 달랐다.
- `Medium`: A-G0/A-G1와 B-G2/B-G3/B-G6의 일부 본문 참조에 phase prefix가 빠졌다.
- `Medium`: current-state verification table의 `Public completion shape` 행에 `IsRetriable` 과거 이력이
  남아 있었다.

이 finding을 반영해 해당 본문 reference는 phase prefix를 붙였고, current-state 표에는 현재 public
shape만 남겼다. Working-tree manifest와 fingerprint는 마지막 갱신 시점에 다시 생성했으며, 다음
검증에서는 manifest 대조 결과와 target hash를 함께 확인한다.

## 최종 Sol Medium review 결과

3차 독립 `gpt-5.6-sol / medium` read-only review 결과는 `CLEAN`이다. unresolved finding은 없다.

- live `git status --short`와 [`working-tree-status.txt`](working-tree-status.txt)가 247줄과
  `4f896675301d5c3aed170fd6a63b8954cef99ada4dc49f2b8728135f15710be3`으로 byte 단위 일치한다.
- 대상 content hash, diff hash와 numstat은 위 fingerprint 표와 일치한다.
- Phase reference는 모두 `A-G*` 또는 `B-G*`로 구분되며, 남은 `ST-G1/G2/G4/G5`는 scenario ID다.
- 과거 `IsRetriable` 내용은 이 log의 [`DN-IMP-013 history`](#dn-imp-013-history)에만 남아 있다.
- 대상 문서의 local link 42개와 anchor 25개가 정상이며 heading은 명사구 형식이다.
- 이번 문서·log 구조 review에는 `gpt-5.6-sol / medium`이 적정하다. 모델 선택에 별도 skill은
  사용하지 않았다.

## 다음 검증

1. 대상 문서의 Markdown link와 anchor를 다시 검사한다.
2. `check_doc_tabs.py framework`와 대상 문서의 diff/whitespace를 검사한다.
3. 현재 `HEAD`와 working tree를 기준으로 문서 상태를 다시 고정한다.
4. Phase A의 남은 scheduler regression과 process evidence를 후속 작업으로 실행한다. 이 review는 문서·log
   구조를 `CLEAN`으로 판정했으며 `.NET` production gap 자체의 완료를 의미하지 않는다.
