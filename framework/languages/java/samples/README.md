# ZLink Java Samples

This directory contains executable Java sample checks for the Phase 10 sample
gate. Each sample is a standalone Gradle application and exits with a non-zero
status when its scenario invariant fails.

Run all required samples:

```bash
./run_samples.sh
```
