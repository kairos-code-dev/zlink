# Bindings POSD Refactor Log

- 날짜: 2026-05-06
- 대상: bindings POSD 리팩토링
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor" bindings/c/src bindings/c/include bindings/c/samples doc/spec/bindings/c/README.md`
- 확인한 draft spec 절: draft spec 전체, `doc/principal/software-design-principles.md`
- 발견한 문제:
  - C binding은 core C API를 그대로 노출하는 얇은 package surface라 새 public abstraction을 추가하지 않았다.
  - C actor samples의 helper는 dispatch drain, join reply, signal wait를 한 곳에 모아 sample별 중복을 줄인다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 수정한 파일:
  - `doc/plan/spot-entry-transport-queues/logs/bindings-posd-refactor-log.ko.md`
- 검증 결과:
  - C binding POSD gate 완료.
  - red flag scan에서 제거가 필요한 C binding 변경사항은 없었다.
- 남은 위험: cpp, dotnet, go, java, node, python, rust 언어별 POSD gate 필요
- 다음 확인: cpp binding POSD gate

## C++ Binding POSD Gate

- 날짜: 2026-05-06
- 대상: C++ binding Actor/Spot surface
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor" bindings/cpp/include/zlink/services/spot.hpp bindings/cpp/include/zlink/types.hpp bindings/cpp/samples/actor_*.cpp bindings/cpp/samples/actor_sample_common.hpp doc/spec/bindings/cpp/README.md`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, active route, snapshot, 제거 API, non-goals
- 발견한 문제:
  - C++ binding은 `actor_ref_t`가 native ref layout을 감싸고, `actor_t`가 `spot_node_t`와 ref 기반 lifecycle을 함께 관리한다.
  - `spot_node_t`가 remote ref 생성, remote actor create-or-get, join/leave, snapshot 호출 복잡성을 흡수한다.
  - 제거된 public Actor handle compatibility layer, Actor channel request wrapper, Actor HWM facade는 발견되지 않았다.
  - `recv_actor_join`/`reply_actor_join`은 Spot join admission 계약만 노출하고 physical channel/router 구현 세부사항을 드러내지 않는다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 수정한 파일:
  - `doc/plan/spot-entry-transport-queues/logs/bindings-posd-refactor-log.ko.md`
- 검증 결과:
  - C++ binding POSD gate 완료.
  - red flag scan은 Actor 관련 공개 surface가 draft 계약과 일치함을 확인하는 용도로만 남았고, 즉시 수정할 얕은 wrapper는 발견되지 않았다.
- 남은 위험: dotnet, go, java, node, python, rust 언어별 POSD gate 필요
- 다음 확인: dotnet binding spec/code/sample/perf gate

## .NET Binding POSD Gate

- 날짜: 2026-05-06
- 대상: .NET binding Actor/Spot/monitor callback surface와 SPOT perf receiver
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor|Actor|dispatch|callback|Sleep" bindings/dotnet/src/Zlink/Actor.cs bindings/dotnet/src/Zlink/Service/Spot.cs bindings/dotnet/src/Zlink/Monitor.cs bindings/dotnet/perf/single/Zlink.BindingBench/src/PerfSpot.cs bindings/dotnet/tests/Zlink.Tests/test_callback_delivery.cs doc/spec/bindings/dotnet/README.md`
  - `bindings/dotnet/tests/run_tests.sh`
  - `bindings/dotnet/samples/run_samples.sh`
- 확인한 draft spec 절: dispatch subject lifetime, Actor ref, unchecked remote ref, join/leave, STREAM bind, snapshot, 제거 API, non-goals
- 발견한 문제:
  - 위험 신호 1: dispatch/monitor callback을 post하는 구조는 호출자가 callback lifetime을 알 수 없게 만들고, drain API를 lifetime 밖에서 실행하게 했다. POSD 기준으로 정보 은닉보다 lifetime 정보 누출과 unknown unknowns를 키우는 구조였다.
  - 대안 A: dispatch와 monitor callback을 계속 post하고, drain API에 별도 snapshot/polling 우회를 둔다.
  - 대안 B: core callback lifetime이 필요한 handler는 native callback thread에서 inline 실행하고, request completion callback처럼 lifetime 제약이 없는 API만 callback-delivery helper를 유지한다.
  - 선택: 대안 B. public surface를 늘리지 않고 callback lifetime 제약을 binding 내부에서 맞추며, special-case 우회 API를 만들지 않는다.
  - 위험 신호 2: SPOT single perf가 direct polling loop로 dispatch readiness를 우회했다. perf 정책과 다르고, caller가 core dispatch lifetime을 직접 맞춰야 하는 얕은 구조였다.
  - 대안 A: snapshot/polling gate를 늘려 flake를 낮춘다.
  - 대안 B: 정책대로 `OnDispatchEvent` 안에서 recv drain하고, ready/active/idle state만 perf 내부에 캡슐화한다.
  - 선택: 대안 B. single SPOT 수신 모델을 정책에 맞추고 추가 public API를 만들지 않는다.
- 수정한 파일:
  - `bindings/dotnet/src/Zlink/Service/Spot.cs`
  - `bindings/dotnet/src/Zlink/Monitor.cs`
  - `bindings/dotnet/perf/single/Zlink.BindingBench/src/PerfSpot.cs`
  - `bindings/dotnet/tests/Zlink.Tests/test_callback_delivery.cs`
  - `doc/plan/spot-entry-transport-queues/logs/bindings-posd-refactor-log.ko.md`
- 검증 결과:
  - .NET callback delivery tests 통과.
  - .NET 전체 tests 141/141 통과.
  - .NET samples 14/14 통과.
  - SPOT perf smoke 포함 single/multi perf가 tcp/64 조건에서 완료됐다.
  - 임시 `Thread.Sleep` close workaround는 제거했고, monitor close 단일 테스트와 전체 테스트가 통과했다.
- 남은 위험: go, java, node, python, rust 언어별 POSD gate 필요
- 다음 확인: go binding POSD gate

## Go Binding POSD Gate

- 날짜: 2026-05-06
- 대상: Go binding Actor/Spot surface와 multi SPOT perf ready gate
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor|Actor|dispatch|callback|ready|Ready|Sleep|time\\.Sleep" bindings/go/actor.go bindings/go/spot.go bindings/go/perf/multi/perf_multi_spot.go bindings/go/perf/multi/perf_multi_spot_ready.go bindings/go/perf/internal/perfcommon/runtime.go bindings/go/samples/actor_* doc/spec/bindings/go/README.md`
  - `go test ./...`
  - `bindings/go/perf/run_benchmarks_multi.sh --pattern MULTI_SPOT --transports tcp --msg-sizes 64 --clean-build`
  - `bindings/go/perf/run_benchmarks_multi.sh --transports tcp --msg-sizes 64 --clean-build`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, dispatch subject lifetime, snapshot, non-goals
- 발견한 문제:
  - 위험 신호 1: Go `MULTI_SPOT` ready gate가 data-plane delivery 성공을 모든 logical spot에서 확인했다. 이는 perf policy의 control handshake barrier를 data path 수신 상태로 대체하는 구조라, ready gate와 측정 데이터 경로가 섞였다.
  - 대안 A: data-plane probe timeout이나 retry 수를 늘려 full suite flake를 줄인다.
  - 대안 B: policy의 control/settle barrier로 ready gate를 단순화하고, active 구간의 데이터 수신은 기존 drain loop가 담당하게 둔다.
  - 선택: 대안 B. perf policy와 일치하고, ready gate가 data-plane delivery-ready event에 의존하지 않는다.
  - 위험 신호 2: `SingleReadyTimeout()`을 multi SPOT ready gate에 사용했다. multi suite 전용 timeout 환경 변수를 우회하는 얕은 공유였다.
  - 대안 A: single timeout 이름을 그대로 두고 문서에 예외를 추가한다.
  - 대안 B: `MultiReadyTimeout()`과 multi SPOT settle helper를 사용한다.
  - 선택: 대안 B. suite별 정책을 코드에 그대로 반영한다.
- 수정한 파일:
  - `bindings/go/perf/internal/perfcommon/runtime.go`
  - `bindings/go/perf/multi/perf_multi_spot.go`
  - `bindings/go/perf/multi/perf_multi_spot_ready.go`
  - `doc/plan/spot-entry-transport-queues/logs/bindings-posd-refactor-log.ko.md`
- 검증 결과:
  - Go tests 통과.
  - Go `MULTI_SPOT` 단일 perf complete.
  - Go full multi perf complete.
  - Actor public surface는 ref 중심이고, 제거된 public Actor handle compatibility layer와 Actor channel request wrapper는 발견되지 않았다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 남은 위험: java, node, python, rust 언어별 POSD gate 필요
- 다음 확인: java binding spec/code/sample/perf gate

## Java Binding POSD Gate

- 날짜: 2026-05-06
- 대상: Java binding Actor/Spot surface와 multi SPOT perf receiver
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor|Actor|dispatch|callback|ready|Ready|Sleep|Thread\\.sleep|sleepQuietly" bindings/java/src/main/java/dev/kairoscode/zlink/service/spot bindings/java/src/main/java/dev/kairoscode/zlink/internal bindings/java/perf/multi/Zlink.BindingBench.Multi/src/main/java/dev/kairoscode/zlink/perf/multi/PerfMultiSpot.java bindings/java/samples/Zlink.Samples/src/main/java/dev/kairoscode/zlink/samples/Actor*.java doc/spec/bindings/java/README.md`
  - `bindings/java/tests/run_tests.sh`
  - `bindings/java/samples/run_samples.sh`
  - `bindings/java/perf/run_benchmarks_multi.sh --pattern MULTI_SPOT --transports tcp --msg-sizes 64 --clean-build`
  - `bindings/java/perf/run_benchmarks_multi.sh --transports tcp --msg-sizes 64 --clean-build`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, dispatch subject lifetime, snapshot, non-goals
- 발견한 문제:
  - 위험 신호 1: Java `Actor`가 native `void *actor` handle 중심 lifecycle을 보존해 public 객체가 제거 API와 같은 개념을 끌고 있었다. POSD 기준으로 구현 결정이 public facade에 새어 나간 구조였다.
  - 대안 A: compatibility handle을 내부 필드로 유지하고 public ref만 추가한다.
  - 대안 B: `SpotNode` ownership과 `ActorRef`를 Actor facade의 유일한 식별자로 삼고, native 호출은 모두 ref 기반 SpotNode API로 내린다.
  - 선택: 대안 B. 제거 API를 첫 구현에서 없애고 Actor가 정확히 하나의 SpotNode에 속한다는 계약을 public 객체 구조에 반영한다.
  - 위험 신호 2: Java `MULTI_SPOT` perf가 logical spot N개를 `SpotNode` N개로 해석하고, active window 이후 backlog를 계속 집계했다. 이는 perf policy의 topology와 measurement anchor가 perf 구현 안에서 섞인 구조였다.
  - 대안 A: timeout과 cooldown publish 시간을 늘려 기존 구조를 통과시킨다.
  - 대안 B: client process당 `SpotNode` 1개와 logical spot N개 topology로 바꾸고, START 뒤 active window 안에서 수신된 active phase만 기록한다.
  - 선택: 대안 B. 측정 의미를 바꾸지 않고 ready/setup, active 집계, post-phase 정리를 분리한다.
  - 위험 신호 3: Java native library loader가 subproject CWD에서 generated bridge/native resource 경로를 찾지 못해 perf 실행 위치에 의존했다.
  - 대안 A: perf runner에서만 working directory를 고정한다.
  - 대안 B: loader가 상위 디렉터리를 탐색해 repo 내 resource 위치를 자체적으로 찾는다.
  - 선택: 대안 B. 호출자가 실행 위치를 알 필요 없게 한다.
- 수정한 파일:
  - `bindings/java/src/main/java/dev/kairoscode/zlink/internal/LibraryLoader.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/internal/Native.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/internal/NativeLayouts.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/service/spot/Actor.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/service/spot/ActorJoinInfo.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/service/spot/Spot.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/service/spot/SpotNode.java`
  - `bindings/java/src/main/java/dev/kairoscode/zlink/service/spot/SpotRoutedSupport.java`
  - `bindings/java/perf/common/src/main/java/dev/kairoscode/zlink/perf/PerfPolicy.java`
  - `bindings/java/perf/common/src/main/java/dev/kairoscode/zlink/perf/PerfReport.java`
  - `bindings/java/perf/multi/Zlink.BindingBench.Multi/src/main/java/dev/kairoscode/zlink/perf/multi/PerfMultiSpot.java`
- 검증 결과:
  - Java tests 통과.
  - Java samples 14/14 통과.
  - Java `MULTI_SPOT` 단일 perf complete.
  - Java full multi perf complete.
  - Actor public surface는 ref 중심이고, 제거된 public Actor handle compatibility layer와 Actor channel request wrapper는 발견되지 않았다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 남은 위험: node, python, rust 언어별 POSD gate 필요
- 다음 확인: node binding spec/code/sample/perf gate

## Node Binding POSD Gate

- 날짜: 2026-05-06
- 대상: Node binding Actor/Spot surface와 single perf worker lifecycle
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor|Actor|dispatch|callback|ready|Ready|Sleep|setTimeout|waitForWorker" bindings/node/src/canonical.ts bindings/node/native/src/addon_spot.cc bindings/node/perf/single bindings/node/samples/actor_* doc/spec/bindings/node/README.md`
  - `npm run typecheck`
  - `bindings/node/tests/run_tests.sh`
  - `bindings/node/samples/run_samples.sh`
  - `bindings/node/perf/run_benchmarks.sh --transports tcp --msg-sizes 64`
  - `bindings/node/perf/run_benchmarks_multi.sh --transports tcp --msg-sizes 64`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, dispatch subject lifetime, snapshot, non-goals
- 발견한 문제:
  - 위험 신호 1: Node `Actor` facade가 old native Actor handle 모델과 제거 API 이름 일부를 유지했다. POSD 기준으로 제거된 구현 결정이 public facade에 남아 호출자가 lifecycle 의미를 잘못 이해할 수 있었다.
  - 대안 A: compatibility 이름을 남기고 내부에서 ref 기반 API로 위임한다.
  - 대안 B: `SpotNode` ownership과 `ActorRef`를 Actor facade의 유일한 식별자로 삼고, 제거된 API 이름을 첫 구현에서 없앤다.
  - 선택: 대안 B. draft의 제거 계약과 Actor가 정확히 하나의 SpotNode에 속한다는 의미를 public surface에 직접 반영한다.
  - 위험 신호 2: Actor readable dispatch가 native callback lifetime subject를 JavaScript 비동기 경계 밖에서 다시 해석할 수 있었다. 이는 callback lifetime 정보를 호출자에게 새게 만드는 구조였다.
  - 대안 A: 별도 detail snapshot API나 Actor polling API를 추가한다.
  - 대안 B: dispatch callback 안에서 Actor part를 preload하고 `recvActorPart()`를 callback info에 캡슐화한다.
  - 선택: 대안 B. 새 public snapshot API를 만들지 않고 lifetime 제약을 binding 내부로 숨긴다.
  - 위험 신호 3: single perf worker 감시가 오류 감시와 완료 감시에 ready timeout을 재사용했다. active duration과 error 감시 의미가 섞여 정상 실행을 실패로 처리했다.
  - 대안 A: smoke duration을 줄이거나 timeout 환경 변수를 외부에서 늘린다.
  - 대안 B: no-error 감시는 timeout 없는 error watcher로 두고, `done` 감시는 active duration 기반 timeout으로 계산한다.
  - 선택: 대안 B. perf 정책의 phase 의미를 코드에 반영하고 측정 조건은 바꾸지 않는다.
- 수정한 파일:
  - `bindings/node/native/src/addon.cc`
  - `bindings/node/native/src/addon_api.h`
  - `bindings/node/native/src/addon_spot.cc`
  - `bindings/node/src/canonical.ts`
  - `bindings/node/perf/single/perf_single_common.ts`
  - `bindings/node/perf/single/perf_pair.ts`
  - `bindings/node/perf/single/perf_pubsub.ts`
  - `bindings/node/perf/single/perf_dealer_dealer.ts`
  - `bindings/node/perf/single/perf_dealer_router.ts`
  - `bindings/node/perf/single/perf_router_router.ts`
- 검증 결과:
  - Node typecheck 통과.
  - Node tests 통과.
  - Node samples 14/14 통과.
  - Node single perf complete.
  - Node multi perf complete.
  - Actor public surface는 ref 중심이고, 제거된 public Actor handle compatibility layer와 Actor channel request wrapper는 발견되지 않았다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 남은 위험: python, rust 언어별 POSD gate 필요
- 다음 확인: python binding spec/code/sample/perf gate

## Python Binding POSD Gate

- 날짜: 2026-05-06
- 대상: Python binding Actor/Spot surface와 multi SPOT perf topology
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|pass-through|wrapper|actor|Actor|dispatch|callback|ready|Ready|Sleep|time\\.sleep|waitFor|readline" bindings/python/src/zlink/_spot.py bindings/python/src/zlink/_ffi.py bindings/python/perf/multi bindings/python/samples/actor_* doc/spec/bindings/python/README.md`
  - `bindings/python/tests/run_tests.sh`
  - `bindings/python/samples/run_samples.sh`
  - `bindings/python/perf/run_benchmarks_multi.sh --pattern MULTI_SPOT --transports tcp --msg-sizes 64`
  - `bindings/python/perf/run_benchmarks_multi.sh --pattern MULTI_SPOT_REQREP --transports tcp --msg-sizes 64`
  - `bindings/python/perf/run_benchmarks_multi.sh --transports tcp --msg-sizes 64`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, dispatch subject lifetime, snapshot, non-goals
- 발견한 문제:
  - 위험 신호 1: Python `Actor`가 native Actor handle을 직접 보유하고 제거된 C API로 lifecycle을 관리했다. POSD 기준으로 구현 세부사항이 public facade에 새어 나간 구조였다.
  - 대안 A: old handle을 내부에 유지하고 public ref helper만 추가한다.
  - 대안 B: `SpotNode`와 `ActorRef`를 Actor facade의 유일한 식별자로 삼고 모든 native 호출을 ref 기반 SpotNode API로 내린다.
  - 선택: 대안 B. 제거 API를 남기지 않고 Actor ownership 의미를 public 객체 구조에 반영한다.
  - 위험 신호 2: dispatch info가 Actor readable subject를 정수 포인터로 보존했다. callback lifetime 제약이 호출자에게 노출되는 얕은 구조였다.
  - 대안 A: subject 포인터를 그대로 두고 사용자가 callback 안에서만 쓰도록 문서화한다.
  - 대안 B: callback 중 `ActorRef`를 복사해 `SpotDispatchInfo.recv_actor_part()`가 node/ref 기반으로 drain하게 한다.
  - 선택: 대안 B. callback lifetime 지식을 binding 내부에 숨긴다.
  - 위험 신호 3: Python `MULTI_SPOT` perf가 SpotNode N개와 data-plane warmup gate를 사용했다. policy topology와 ready gate 의미가 코드 안에서 섞였다.
  - 대안 A: timeout을 늘려 기존 topology를 유지한다.
  - 대안 B: SpotNode 1개와 logical Spot N개 topology로 바꾸고, ready는 control/settle barrier로 처리한다.
  - 선택: 대안 B. `doc/perf` 정책과 같은 의미를 구현한다.
  - 위험 신호 4: multi runner control-line wait가 blocking `readline()`으로 timeout을 무력화했다.
  - 대안 A: timeout을 크게 늘린다.
  - 대안 B: stdout reader queue를 두고 timeout 있는 queue wait로 control line을 읽는다.
  - 선택: 대안 B. 실패를 빠르고 명확하게 분류한다.
- 수정한 파일:
  - `bindings/python/src/zlink/_ffi.py`
  - `bindings/python/src/zlink/_spot.py`
  - `bindings/python/perf/multi/run_benchmarks.py`
  - `bindings/python/perf/multi/perf_multi_spot_client.py`
  - `bindings/python/perf/multi/perf_multi_spot_server.py`
  - `bindings/python/perf/multi/perf_multi_spot_reqrep_client.py`
- 검증 결과:
  - Python tests 55 passed, 10 skipped.
  - Python samples 14/14 통과.
  - Python `MULTI_SPOT` 단일 perf complete.
  - Python `MULTI_SPOT_REQREP` 단일 perf complete.
  - Python full multi perf complete.
  - Actor public surface는 ref 중심이고, 제거된 public Actor handle compatibility layer와 Actor channel request wrapper는 발견되지 않았다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 남은 위험: rust 언어별 POSD gate 필요
- 다음 확인: rust binding spec/code/sample/perf gate

## Rust Binding POSD Gate

- 날짜: 2026-05-06
- 대상: Rust binding Actor/Spot surface
- 수행한 명령:
  - `rg -n "TODO|FIXME|HACK|wrapper|actor|Actor|dispatch|callback|ready|Ready|sleep|wait|zlink_actor_|destroy_remote_actor|send_bound_session_packet" bindings/rust/src bindings/rust/tests bindings/rust/samples doc/spec/bindings/rust/README.md`
  - `cargo check`
  - `cargo fmt --all --check`
  - `bindings/rust/tests/run_tests.sh`
  - `bindings/rust/samples/run_samples.sh`
  - `bindings/rust/perf/run_benchmarks.sh --transports tcp --msg-sizes 64`
  - `bindings/rust/perf/run_benchmarks_multi.sh --transports tcp --msg-sizes 64`
- 확인한 draft spec 절: Actor ref, unchecked remote ref, Entry Spot, STREAM bind, join/leave, dispatch subject lifetime, snapshot, non-goals
- 발견한 문제:
  - 위험 신호 1: Rust `Actor`가 native Actor handle 중심으로 생명주기를 관리했다. POSD 기준으로 제거된 구현 세부사항이 public facade에 새어 나간 구조였다.
  - 대안 A: old handle을 내부에 유지하고 `ActorRef` helper만 추가한다.
  - 대안 B: `SpotNode` handle과 `ActorRef`를 Actor facade의 유일한 식별자로 삼고 native 호출을 ref 기반 SpotNode API로 내린다.
  - 선택: 대안 B. 제거 API를 남기지 않고 Actor ownership 의미를 public 객체 구조에 반영한다.
  - 위험 신호 2: dispatch info가 Actor readable subject를 raw pointer로 노출했다. callback lifetime 제약이 호출자에게 노출되는 얕은 구조였다.
  - 대안 A: subject 포인터를 그대로 두고 사용자가 callback 안에서만 쓰도록 문서화한다.
  - 대안 B: callback 중 `ActorRef`를 복사해 `SpotDispatchInfo.recv_actor_part_with_flags()`가 node/ref 기반으로 drain하게 한다.
  - 선택: 대안 B. callback lifetime 지식을 binding 내부에 숨긴다.
  - 위험 신호 3: Actor samples가 join과 STREAM bind 순서를 흐름별로 다르게 표현했다. user Spot join 전제 조건이 샘플 호출자에게 암묵적으로 남았다.
  - 대안 A: 샘플 설명으로 순서 제약을 보완한다.
  - 대안 B: 모든 Actor 샘플에서 STREAM bind를 join 전에 배치하고 session lifetime을 leave 이후까지 유지한다.
  - 선택: 대안 B. API 전제 조건을 샘플 구조로 드러내고 실패 가능성을 줄인다.
- 수정한 파일:
  - `bindings/rust/src/ffi.rs`
  - `bindings/rust/src/service.rs`
  - `bindings/rust/tests/service_surface_tests.rs`
  - `bindings/rust/samples/actor_room_server_sample.rs`
  - `bindings/rust/samples/actor_gateway_relay_sample.rs`
  - `bindings/rust/samples/actor_single_player_queue_sample.rs`
- 검증 결과:
  - Rust tests 10/10 suites 통과.
  - Rust samples 14/14 통과.
  - Rust single perf complete.
  - Rust multi perf complete.
  - Actor public surface는 ref 중심이고, 제거된 public Actor handle compatibility layer와 Actor channel request wrapper는 발견되지 않았다.
  - 새 POSD 후보 1차: 없음.
  - 새 POSD 후보 2차: 없음.
- 남은 위험: 언어별 POSD gate는 모두 완료
- 다음 확인: 최종 종료 절차
