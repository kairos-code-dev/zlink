# .NET G4 DDD/POSD 리팩터링 ledger

이 문서는 `.NET` G4 반복 리뷰에서 확정한 finding, 비교 대안, 선택과 검증 상태를 기록한다.
실행 절차와 gate는 [공개 계약 gap 구현 계획](../../v11.0/route-mesh-11.0.0-execution-ledger.ko.md)을 따른다.

## Finding 진행표

| loop | finding ID | 위험 신호 | DDD/POSD 근거 | 대안 A | 대안 B | 선택 | test | 상태 |
|------|------------|-----------|---------------|--------|--------|------|------|------|
| .NET-1 | G4-F1 | spec 밖 public handler scan 확장 두 개 | 이중 등록 표면, 정보 누출 | scan/DI를 internal registrar가 소유하고 public 확장 삭제 | 확장을 새 계약으로 추가 | A | solution build, package export | 완료 |
| .NET-1 | G4-F2 | 세 method attribute가 runtime에서 소비되지 않음 | 명목 계약과 구현 분리 | actual Spot/session owner의 scanner와 invoker에 연결 | attribute 계약 삭제 | A | Spot scanner와 session attributed dispatch unit test | 완료 |
| .NET-1 | G4-F3 | 동작 없는 public marker와 raw attribute | dead compatibility export, 얕은 명목 추상화 | 네 export/file 삭제 | 동작을 새 계약으로 추가 | A | contract/package absence assertion | 완료 |
| .NET-1 | G4-F4 | `ZLinkSpotManagerService`의 일곱 pass-through | 얕은 모듈, 중복 책임 | runtime이 `IZLinkSpotManager`를 직접 구현 | service로 orchestration을 모두 이동 | A | solution build, manager contract test | 완료 |
| .NET-1 | G4-F5 | 사용되지 않는 multipart route DI/interface | dead registration, raw-frame 우회 경로 | interface, method, DI mapping 삭제 | 실제 use case가 생길 때 소유 transport의 private operation으로 설계 | A | production no-hit, solution build | 완료 |
| .NET-1 | G4-F6 | typed publish의 도달 불가능한 `PacketName` | 구/신 경로 공존, 불필요 mutable state | method 삭제와 inferred name 불변화 | public override 복원 | A | contract exclusion, solution build | 완료 |
| .NET-2 | G4-F7 | actor handle refresh가 actor id를 잃고 이전 SpotRid를 사용 | domain identity 누락, refresh 지식 누출 | handle 내부 refresh strategy가 원래 논리 key를 보존 | actor 전용 public handle 추가 | A | `Actor_Spot_Handle_Refreshes_By_Actor_Id_After_The_Actor_Moves` | 완료 |
| .NET-2 | G4-F8 | one-way send와 request가 같은 refresh/retry helper 사용 | 전달 보장 혼합, 특수·범용 코드 혼합 | request만 target-not-found에서 1회 refresh, send는 최신 snapshot으로 1회 제출 | 호출마다 policy 인자 전달 | A | location resolver unit, solution build | 완료 |
| .NET-2 | G4-F9 | handle snapshot에 location update 입력 없음 | 위치 수명 책임이 send 호출자에게 누출 | weak shared registry와 local event/watch host가 snapshot 갱신 | 매 send마다 store 조회 | A | `Location_Event_Updates_Existing_Actor_Spot_Handle_Snapshot` | 완료 |
| .NET-2 | G4-F10 | endpoint handle이 `IList<string>`으로 cast되고 실패 method 노출 | 우발적 표면, 정의로 없앨 수 있는 오류 | public contract와 internal read-only collection만 구현 | 별도 read-only adapter 추가 | A | endpoint contract/unit, cast no-hit | 완료 |
| .NET-3 | G4-F11 | auto-connect가 startup 시점 manual endpoint 사본을 보관 | live public 연결 변경과 reconcile 상태 분리 | executor가 thread-safe endpoint handle을 직접 조회 | callback으로 별도 set 동기화 | A | solution build, endpoint runtime unit | 완료 |
| .NET-3 | G4-F12 | route connection set의 무잠금 HashSet/snapshot | 내부 동시성 조건 누출, 상태 손상 가능성 | 자료구조와 router connect/disconnect를 내부 gate로 직렬화 | concurrent dictionary와 매번 정렬 | A | route connection unit, solution test | 완료 |
| .NET-3 | G4-F13 | constructor의 `IEnumerable<T>`가 assembly 전체 DI 등록을 유발 | 마법적 전역 정책, 표준 DI 책임 침범 | registrar 삭제, application dependency는 `IServiceCollection`에 명시 등록 | 새 명시 attribute/API 추가 | A | standard DI registration unit, sample runner | 완료 |
| .NET-3 | G4-F14 | join reply의 읽히지 않는 rolling compatibility payload 두 필드 | dead compatibility wire, 정보 중복 | 필드와 생성 인자 삭제 | 별도 versioned legacy decoder | A | actor handoff unit/E2E | 완료 |
| .NET-3 | G4-F15 | `SpotHandle` 공개 주석이 caller re-resolve를 요구 | 구현/계약 반대, 복잡성 호출자 전가 | event/watch 갱신과 안전한 request 1회 retry를 주석에 명시 | 내부 갱신 제거 | A | documentation regression, contract test | 완료 |
| .NET-4 | G4-F16 | session stream builder에 호출되지 않는 packet-name override | dead mutable compatibility 경로 | override 삭제, typed name 불변화 | public packet override 복원 | A | production no-hit, stream/solution test | 완료 |
| .NET-4 | G4-F17 | manual/auto endpoint가 같은 physical 연결을 독립 해제 | 연결 정책과 socket 상태 분리 | channel/route/Spot 연결 모듈이 source별 owner를 추적하고 마지막 owner만 disconnect | manual 변경 때 reconciler invalidate | A | route ownership unit, E2E all | 완료 |
| .NET-4 | G4-F18 | executor 실패도 reconciler active로 기록 | 오류를 삼켜 retry 의미 소실 | executor가 성공 여부를 반환하고 성공 target만 active 기록 | tick 전체 예외 전파 | A | `Failed_Connect_Is_Not_Marked_Active_And_Retries_On_The_Next_Tick` | 완료 |
| .NET-4 | G4-F19 | optional watch/event 유실 시 handle 갱신 correctness 경로 없음 | 선택 최적화를 필수 경로로 오인 | live handle key를 polling interval마다 resolve하고 watch는 wake-up 경로로 유지 | watch를 필수 public 계약으로 변경 | A | handle poll/watch unit, location E2E | 완료 |
| .NET-4 | G4-F20 | 회수된 handle의 logical key가 singleton registry에 누적 | 내부 resource 수명 누수 | polling maintenance가 dead weak reference와 빈 key 정리 | public dispose/lease 추가 | A | registry maintenance unit | 완료 |
| .NET-5 | G4-F21 | ownership 통합 뒤 사용되지 않는 sorted connection helper file | dead shallow module | file 삭제 | 향후 사용 가능성을 이유로 유지 | A | production reference no-hit, solution build | 완료 |
| .NET-5 | G4-F22 | Spot owner 전환과 물리 연결이 서로 다른 임계 구역에서 실행됨 | 논리 owner와 실제 연결 상태가 달라질 수 있음 | connector gate가 owner 전환, 물리 호출, 실패 rollback을 함께 직렬화 | endpoint별 pending 상태와 waiter 추가 | A | Spot peer ownership unit, solution test | 완료 |
| .NET-5 | G4-F23 | route Spot request가 refresh helper 전에 handle snapshot을 읽음 | 갱신 정책 우회, 호출자 재조회 책임을 적은 계약 충돌 | timeout 계산을 refresh attempt 안으로 옮기고 공개 주석을 자동 갱신 계약으로 통일 | helper에 preflight callback 추가 | A | invalidated handle request unit, contract documentation review | 완료 |
| .NET-5 | G4-F24 | 사용되지 않는 endpoint membership helper | dead shallow internal surface | helper 삭제 | 미래 사용을 위해 유지 | A | production reference no-hit, solution build | 완료 |
| .NET-6 | G4-F25 | manual과 auto 연결 방식이 같은 역할에서 함께 실행됨 | 연결 정책 분산, spec의 단일 owner 위반 | validation에서 역할별 mode를 고정하고 manual callback과 auto executor를 함께 제어 | reconcile tick마다 manual 목록을 검사 | A | registration mode unit, sample/E2E | 완료 |
| .NET-6 | G4-F26 | SpotHandle 갱신이 이전 generation을 적용할 수 있음 | 주소 상태 역행, generation invariant 누락 | handle snapshot이 generation을 보관하고 update/remove를 단조 적용 | logical key별 async gate 추가 | A | `Spot_Handle_Does_Not_Apply_An_Older_Generation` | 완료 |
| .NET-6 | G4-F27 | local row 재등록 오류가 fail-static 경계 밖으로 전파됨 | store 장애 뒤 reconcile task 영구 종료 | local publish와 peer read를 하나의 store 오류 경계로 집약 | loop 최상단에서 모든 오류 catch | A | auto-connect outage/recovery unit, StoreFailure E2E | 완료 |
| .NET-6 | G4-F28 | auto-connect peer 조회 취소를 store 장애로 처리 | lifecycle 취소와 장애 복구 의미 혼합 | 요청 token의 `OperationCanceledException`을 먼저 다시 throw | tick 결과 variant 추가 | A | auto-connect cancellation unit, solution test | 완료 |
| .NET-6 | G4-F29 | SpotHandle 언어 문서와 source comment가 caller 재조회와 draft를 설명 | 정책 중복과 계약 drift | 내부 갱신과 안전한 request 1회 재전송, 정식 spec 절을 명시 | 중복 설명 삭제 후 공통 spec만 링크 | A | documentation regression, contract review | 완료 |
| .NET-6 | G4-F30 | application에 전달되는 SpotHandle monitor를 내부 lock으로 사용 | 동기화 결정이 public 객체에 노출 | private gate로 snapshot 상태를 직렬화 | immutable state를 CAS로 교체 | A | location resolver concurrency unit | 완료 |
| .NET-7 | G4-F31 | remove와 같은 generation의 지연 update가 handle을 재활성화 | tombstone 순서 역전 | tombstone 상태에서는 같은 generation update를 거부 | watch/poll 적용을 단일 직렬 큐로 통합 | A | same-generation tombstone unit | 완료 |
| .NET-7 | G4-F32 | Spot peer metadata 변경을 active handover가 감지하지 않음 | pub/sub endpoint 추가 뒤 연결 누락 | metadata semantic diff도 handover 조건에 포함 | metadata 누락을 전체 연결 실패로 처리 | A | auto-connect metadata handover unit, Spot E2E | 완료 |
| .NET-7 | G4-F33 | auto mode endpoint handle 변경이 현재 runtime에는 무효지만 다음 시작에 남음 | 조용한 무효 변경과 mode drift | validation에서 handle mode를 freeze하고 auto mode mutation 거부 | startup 설정과 runtime handle 타입 분리 | A | `AutoConnect_Role_Rejects_Runtime_Manual_Connection_Mutations` | 완료 |
| .NET-7 | G4-F34 | TicTacToe의 미사용 별도 Redis room-route 저장소 | dead compatibility와 route 정보 이중 소유 | 저장소, DTO, DI, write, package, test 예외 삭제 | 도메인 소비 경로 복원 | A | TicTacToe sample, sample regression | 완료 |
| .NET-7 | G4-F35 | change-stamp 단계가 요청 취소를 store 오류로 처리 | lifecycle 취소 의미 혼합 | token 취소 예외를 먼저 다시 throw | stamp 결과 variant 추가 | A | auto-connect cancellation unit | 완료 |
| .NET-7 | G4-F36 | public `StoreFailureGrace`가 runtime에서 사용되지 않음 | 얕은 설정과 spec drift | 마지막 성공 desired의 미완료 연결만 grace 동안 재시도 | 옵션과 확정 spec 삭제 | A | store failure grace unit, StoreFailure E2E | 완료 |
| .NET-7 | G4-F37 | public source comment에 draft와 객체 의인화 표현 잔존 | 정식 계약 drift와 모호한 설명 | 정식 spec 의미의 중립적 표현으로 갱신 | 중복 설명 삭제 | A | source comment review, documentation regression | 완료 |
| .NET-7 | G4-F38 | remote actor join만 별도 route-ref interface/record 사용 | SpotHandle 정책 분산, 미사용 field, 얕은 seam | `IZLinkSpotHandleResolver`와 내부 snapshot을 사용하고 old path 삭제 | concrete route resolver 직접 주입 | A | remote join unit/E2E, exact-symbol no-hit | 완료 |
| .NET-8 | G4-F39 | tombstone generation을 주소 snapshot에만 의존 | remove 뒤 더 오래된 update가 handle을 재활성화 | 별도 observed generation을 update/remove 모두에서 단조 증가 | tombstone snapshot variant 도입 | A | higher-generation tombstone unit | 완료 |
| .NET-8 | G4-F40 | failed connect pending을 change-stamp 동일 tick이 건너뜀 | 최적화가 재시도 correctness를 변경 | reconciler가 pending 여부를 소유하고 pending이면 stamp skip 금지 | loop가 별도 retry 상태 소유 | A | auto-connect loop retry unit | 완료 |
| .NET-8 | G4-F41 | 모든 metadata 변경을 physical handover로 처리 | 진단 metadata와 transport 정보 혼합 | planner가 Spot pub endpoint connection fingerprint만 정규화 | executor가 handover 필요 여부 판단 | A | metadata fingerprint unit, Spot E2E | 완료 |
| .NET-8 | G4-F42 | remote actor admission 전에 SpotHandle을 snapshot으로 평탄화 | 안전한 refresh 정책 우회 | admission까지 handle을 유지해 공통 1회 refresh를 적용하고 승인 snapshot만 commit에 고정 | joiner가 직접 resolve retry | A | actor handoff unit, Bingo sample | 완료 |
| .NET-8 | G4-F43 | .NET guide가 삭제된 SpotRef와 caller 재조회 계약을 안내 | public 사용법과 확정 interface 이중화 | 예제, 표, 오류 설명을 SpotHandle 자동 갱신 계약으로 이관 | 구 절 삭제 후 spec 링크 | A | guide exact-symbol search, documentation regression | 완료 |
| .NET-8 | G4-F44 | transferred actor의 bound session route가 `OnJoinedActorAsync` 뒤에 설정됨 | lifecycle callback에서 확정 public `BoundSession`을 사용할 수 없음 | target 준비 단계에서 bound route를 먼저 설정하고 pending route 상태 삭제 | sample이 joining actor push를 생략 | A | actor handoff unit, Bingo two-node sample | 완료 |
| .NET-9 | G4-F45 | handover 실패 target을 pending 판단에서 제외 | stamp 최적화가 reconnect 재시도를 영구 생략 | active/desired의 endpoint, owner, fingerprint 차이까지 pending으로 계산 | 별도 pending flag 저장 | A | pending connect loop unit | 완료 |
| .NET-9 | G4-F46 | current Spot request 재시도가 첫 attempt에서 dispose된 parts를 재사용 | retry attempt 간 메시지 소유권 누출 | attempt lambda 안에서 header와 parts를 매번 생성 | retry clone 보관 | A | current Spot request unit, Spot E2E | 완료 |
| .NET-9 | G4-F47 | generation guard가 8192 key에서 전체 map 초기화 | stale replica row 재수락 가능 | runtime 수명 동안 key별 exact generation 유지 | authoritative generation 조회 뒤 안전 eviction | A | generation guard unit, location E2E | 완료 |
| .NET-9 | G4-F48 | 정식 .NET guide/spec에 삭제된 주소 API 잔존 | public 계약 이중화 | SpotHandle resolver 계약으로 교체하고 forbidden-symbol 문서 테스트 추가 | 해당 절 삭제 후 정본 링크 | A | `DotNetDocs_DoNotDocument_Replaced_Spot_Address_Contracts` | 완료 |
| .NET-9 | G4-F49 | 제거된 actor address resolver의 sample allowlist 잔존 | dead compatibility test 분기 | allowlist와 판정 helper 삭제 | 범용 임시 예외 구조 유지 | A | sample regression, exact-symbol no-hit | 완료 |
| .NET-10 | G4-F50 | handover 실패를 key 존재만으로 수렴 상태로 판단 | stamp skip이 reconnect 재시도를 생략 | endpoint, owner, fingerprint semantic diff도 pending으로 계산 | 명시 pending flag 저장 | A | pending handover loop unit | 완료 |
| .NET-10 | G4-F51 | current Spot request retry가 dispose된 encoded parts 재사용 | attempt 간 message ownership 누출 | 각 attempt 안에서 header와 parts를 새로 생성 | retry용 clone 보관 | A | current Spot request unit, Spot E2E | 완료 |
| .NET-10 | G4-F52 | observed generation map을 고정 개수에서 전체 초기화 | lagging replica row 재수락 | runtime 수명 동안 exact key generation 유지 | authoritative generation query 뒤 안전 eviction | A | generation guard unit, location E2E | 완료 |
| .NET-10 | G4-F53 | .NET guide/spec에 삭제된 address resolver 이름 잔존 | public 사용법과 확정 계약 불일치 | 현재 SpotHandle resolver로 교체하고 forbidden-symbol 회귀 테스트 추가 | 중복 절 삭제 | A | documentation regression, exact-symbol no-hit | 완료 |
| .NET-10 | G4-F54 | sample regression에 제거된 resolver allowlist 잔존 | dead compatibility branch | allowlist와 helper 삭제 | 범용 임시 예외 구조 유지 | A | sample regression | 완료 |
| .NET-11 | G4-F55 | SpotHandle registry와 refresh가 선택한 mesh identity를 버림 | 논리 key 정보 손실로 다른 mesh의 같은 SpotRid가 handle을 변경할 수 있음 | 최초 선택한 mesh와 SpotRid 복합 key를 refresh, event, polling 전체에서 보존 | 모든 mesh에 SpotRid 전역 유일성 제약 추가 | A | `Spot_Handle_Registry_Does_Not_Cross_Mesh_Boundaries`, location unit | 완료 |
| .NET-11 | G4-F56 | topology와 service summary가 observed-generation guard를 우회 | 같은 runtime의 public read surface마다 단조 view가 달라짐 | peer row 값과 generation 검증을 query service의 공통 read helper에 집약 | 각 projection loop에 검증 반복 | A | `Topology_And_Service_Summaries_Drop_Older_Peer_Generations` | 완료 |
| .NET-11 | G4-F57 | 무효화된 handle의 one-way send가 encode 뒤 snapshot 예외로 parts를 누수 | 전송 전 message ownership 책임이 불명확 | snapshot을 encode보다 먼저 읽어 생성 전 실패 | 생성 뒤 ownership flag와 finally로 dispose | A | solution build, location unit | 완료 |
| .NET-11 | G4-F58 | 정식 sample guide가 삭제된 RoutingId Spot 호출을 안내 | 확정 public contract와 사용법 분리 | resolver로 얻은 SpotHandle 전달로 수정 | 중복 선택 안내 삭제 후 정식 guide만 링크 | A | documentation exact-symbol search | 완료 |
| .NET-11 | G4-F59 | Spot pub endpoint metadata key 문자열이 host와 planner에 중복 | transport handover 지식 누출 | metadata owner의 단일 상수를 planner도 사용 | 별도 metadata utility 추가 | A | solution build, metadata fingerprint unit | 완료 |
| .NET-12 | G4-F60 | polling이 같은 key의 여러 live handle generation을 최댓값 하나로 축약 | 오래된 handle이 새 handle과 함께 존재하면 삭제를 관측하지 못함 | key마다 살아 있는 개별 generation을 보존하고 조건부 무효화 | polling이 모든 handle 객체를 직접 보유 | A | `Polling_Snapshot_Preserves_Each_Live_Handle_Generation` | 완료 |
| .NET-12 | G4-F61 | watch upsert generation보다 오래된 replica row를 handle에 적용 | push 순서와 store replica 지연이 단조 주소 view를 역행시킴 | row generation이 event보다 낮으면 event generation tombstone 적용 | event별 authoritative store 재시도 | A | location resolver monotonic tests, solution build | 완료 |
| .NET-12 | G4-F62 | routed Spot send의 message parts dispose 책임이 여러 target에 분산되고 일부 예외 경로에서 누락 | 소유권 정보 누출과 오류 경로 자원 누수 | 최상위 runtime send 경계가 모든 성공/실패에서 단 한 번 dispose | 각 target이 공통 ownership wrapper 사용 | A | Spot route unit, solution test | 완료 |
| .NET-13 | G4-F63 | routed Spot request도 입력 parts 소유권이 target별로 분산 | runtime 중지와 target resolve 예외에서 입력 누수 | 최상위 request 경계가 모든 성공/실패에서 입력을 dispose | disposable ownership wrapper를 encoder가 반환 | A | Spot route request unit, solution test | 완료 |
| .NET-13 | G4-F64 | async submitter가 취소 확인 전에 입력 소유권을 확정하지 않음 | encode 뒤 취소와 runtime stop 경쟁에서 native message 누수 | submit boundary가 즉시 소유하고 enqueue 이전 모든 예외에서 정리 | encoder가 ownership wrapper 반환 | A | async submitter cancellation unit, solution test | 완료 |
| .NET-13 | G4-F65 | route Spot one-way send가 이미 취소된 token으로도 전송 가능 | 같은 Submit 계약의 취소 의미가 호출 경로마다 다름 | snapshot과 encode 전에 취소 확인 | dispatcher target마다 취소 확인 | A | routed Spot send cancellation unit | 완료 |
| .NET-13 | G4-F66 | generation guard가 read 성공에서만 전진 | authoritative write/remove/watch 뒤 lagging replica가 이전 row를 다시 공개 | shared guard의 Observe/tombstone을 모든 kind mutation과 watch에 연결 | location view 모듈로 read/write/event sequencing 통합 | A | `Mutation_Events_Advance_The_Shared_Generation_Guard`, watch monotonic unit | 완료 |
| .NET-13 | G4-F67 | `AddZLinkFramework` 중복 호출이 hosted task lifecycle을 중복 등록 | background task owner가 덮어써져 첫 task를 회수하지 못함 | 두 번째 등록을 명시 오류로 거부 | composable registration accumulator 도입 | A | `AddZLinkFramework_Rejects_Duplicate_Registration` | 완료 |
| .NET-13 | G4-F68 | request/join/worker call의 timeout 검증 시점과 오류가 다름 | 같은 fluent 개념의 validation 지식 중복 | 모든 call이 공통 timeout guard 사용 | timeout value object 도입 | A | timeout validation unit, solution build | 완료 |
| .NET-13 | G4-F69 | topology의 State와 Actor MeshName filter가 kind별로 누락 | 같은 filter abstraction 의미가 projection branch마다 달라짐 | 공통 entry predicate를 모든 kind에 적용 | kind별 exhaustive filter 함수 | A | `Topology_Applies_State_And_Actor_Mesh_Filters` | 완료 |
| .NET-13 | G4-F70 | runtime status가 watch capability와 read outage를 반영하지 않음 | 운영 상태 지식이 resolver/query/auto-connect에 분산 | shared store health와 실제 watch capability를 status projection에 주입 | subsystem별 runtime health 보고 | A | `Status_Reports_Watch_Capability_And_Shared_Read_Failures` | 완료 |
| .NET-13 | G4-F71 | Spot builder의 두 connection property가 다른 두 property와 동일 객체를 반환 | 이름만 보고 독립 capability로 오해하면 lifecycle과 연결 상태 의미가 모호함 | 정식 계약에서 중복 property 제거 | 확정 interface를 유지하고 동일 capability/state 별칭임을 계약과 test로 고정 | B | `Spot_Connection_Role_Names_Expose_The_Documented_Capability_State`, source contract | 완료 |
| .NET-14 | G4-F72 | upsert 지연과 remove tombstone을 같은 unavailable boolean으로 표현 | 같은 generation 정상 row가 도착해도 handle이 영구 복구되지 않음 | Available/PendingUpsert/Removed 상태를 분리 | upsert miss에서는 기존 snapshot 유지 | A | `Watch_Upsert_Does_Not_Apply_A_Row_Older_Than_The_Event` recovery assertion | 완료 |
| .NET-14 | G4-F73 | Spot/Actor/Route topology가 live-list를 재사용해 Lost를 만들지 못함 | 일반 live 목록과 운영 topology projection 의미 혼합 | raw row에 generation과 lease를 적용해 kind 공통 Ready/Lost 계산 | Lost를 Peer 전용으로 spec 제한 | A | `Topology_Projects_Lost_State_For_Expired_Owners` | 완료 |
| .NET-14 | G4-F74 | store read의 성공/실패/취소 health 분류가 세 모듈에 반복 | health 정책 지식 중복과 정상 취소 오분류 위험 | 공통 `ZLinkLocationStoreRead` 경계로 집약 | 각 wrapper에서 요청 token 취소만 별도 처리 | A | location query/resolver cancellation and health unit | 완료 |
| .NET-15 | G4-F75 | Removed handle을 같은 generation poll miss/upsert가 Pending으로 약화 | authoritative tombstone 뒤 같은 generation row 재활성화 | Removed와 같은 generation의 pending 전이를 금지 | Removed handle을 polling inventory에서 제외 | A | exact-generation tombstone regression in `Spot_Handle_Does_Not_Apply_An_Older_Generation` | 완료 |
| .NET-15 | G4-F76 | store 내부 OCE와 caller token 취소를 같은 정상 취소로 분류 | 실제 store timeout이 health 장애에서 누락 | 공통 read 경계에 caller token을 전달해 요청 취소만 제외 | store result variant 도입 | A | `Store_Health_Distinguishes_Caller_Cancellation_From_Internal_Cancellation` | 완료 |
| .NET-16 | G4-F77 | 같은 hosted singleton이 여러 DI descriptor에서 dispose될 때 watch CTS를 중복 사용 | lifecycle 소유권이 dispose 호출 횟수에 의존해 정상 shutdown이 예외로 끝남 | CTS와 task 배열을 원자적으로 한 번만 인수해 dispose를 idempotent하게 구현 | DI descriptor 하나만 disposable owner로 만들고 proxy descriptor 도입 | A | `Spot_Handle_Watch_Host_Disposal_Is_Idempotent`, ResilienceLifecycle E2E | 완료 |
| .NET-17 | G4-F78 | StopAsync와 DisposeAsync가 서로 다른 field snapshot으로 종료 | 동시 종료에서 CTS가 먼저 dispose되거나 task 배열이 분리될 수 있음 | lifecycle gate와 공유 shutdown task로 모든 종료 호출을 집약 | 단일 disposable DI owner와 proxy descriptor | A | concurrent `Spot_Handle_Watch_Host_Disposal_Is_Idempotent` | 완료 |
| .NET-18 | G4-F79 | actor 요청 handler가 `DestroyActorAsync`를 호출하면 reply relay보다 native teardown이 먼저 실행됨 | handler 반환과 요청 응답 완료의 lifecycle 경계가 분리되어 응답 경로가 삭제됨 | 기존 bound-session dispatch 지연 큐에서 teardown을 reply 전송 뒤 실행 | actor request dispatcher에 별도 teardown 큐 추가 | A | `ActorDestroy_DuringBoundRequest_IsDeferredUntilReplyFinalization`, SpotService SM-B8 | 완료 |
| .NET-19 | G4-F80 | framework와 connector에 packet-name, LZ4와 frame wire 지식이 중복되고 framework가 connector 내부 protocol에 의존 | wire 정보의 소유권 분산과 변경 증폭 | 중립적인 일곱 번째 protocol package 추가 | 고정된 connector package를 protocol owner로 깊게 만들고 framework는 오류 의미만 변환 | B | connector codec, `StreamWireInteropTests`, package verifier | 완료 |
| .NET-19 | G4-F81 | backend abstraction이 `NativeInstance`와 `NativeRequest` escape hatch를 노출 | binding 구현 세부가 framework runtime 경계를 통과 | backend operation별 capability로 감싼다 | native object에 범용 mapping helper를 유지한다 | A | backend mapping/solution test | 완료 |
| .NET-19 | G4-F82 | actor context가 bound-session factory를 service provider에서 조회 | domain context에 service locator와 DI 구성 지식 누출 | factory를 명시적으로 생성자 주입 | context가 runtime facade를 통해 생성 | A | actor dispatch 24개, unit 전체 | 완료 |
| .NET-19 | G4-F83 | actor destroy/migration이 같은 instance 상태를 서로 다른 method에서 초기화 | lifecycle invariant 중복과 상태 역행 위험 | terminal transition 하나가 공통 상태 변경을 소유 | 상태별 class로 전체 state machine 교체 | A | actor teardown/handoff unit | 완료 |
| .NET-19 | G4-F84 | `EmptyServiceProvider`와 구 stream metadata codec file이 사용되지 않음 | dead file과 구형 runtime path | exact-symbol 확인 뒤 두 file 삭제 | 미래 사용을 위해 유지 | A | production no-hit, solution build | 완료 |
| .NET-19 | G4-F85 | runtime channel options interface의 대부분이 호출할 때 항상 실패 | 확정 interface가 명목 mirror에 머물고 오류 조건을 호출자가 암기 | client/server socket과 routing의 실제 read view를 모두 제공 | runtime 전용 interface로 public contract 변경 | A | runtime options contract/unit | 완료 |
| .NET-19 | G4-F86 | validation, DI, route와 Spot activation이 같은 handler assembly를 반복 scan | 검증한 descriptor와 실행 descriptor의 owner 부재 | registration이 immutable handler catalog를 한 번 소유 | scanner별 assembly cache만 추가 | A | registration/route/Spot unit, solution build | 완료 |
| .NET-19 | G4-F87 | connector transport factory가 dispatch, reconnect와 callback 옵션까지 검증 | transport 선택과 connector 전체 정책 혼합 | 별도 options validator가 전체 불변을 소유 | immutable options public contract로 변경 | A | connector options test | 완료 |
| .NET-19 | G4-F88 | routed send가 이미 decode한 header를 internal dispatcher에서 다시 decode | protocol 지식 누출과 request/send 비대칭 | decoded header를 internal send dispatch에 전달 | dispatcher registry로 resolve와 dispatch를 합침 | A | route dispatcher unit | 완료 |
| .NET-19 | G4-F89 | connector 기본 JSON 설정이 process-global mutable instance를 직접 노출 | consumer 간 시간적 결합과 실행 중 정책 변경 | clone한 copy-on-write snapshot을 원자 교체 | connector별 serializer option을 새 public 설정으로 추가 | A | typed connector serialization unit | 완료 |
| .NET-19 | G4-F90 | 읽히지 않는 diagnostics counter 두 개가 hot path에서 증가 | unused state와 불필요한 동기화 | field, property와 increment 삭제 | 정식 meter로 승격 | A | exact-symbol no-hit, diagnostics unit | 완료 |
| .NET-19 | G4-F91 | serial queue의 capacity bypass boolean 분기에 호출자가 없음 | 구형/신형 admission path가 함께 남음 | 일반 path는 capacity reserve를 직접 호출하고 분기 삭제 | 이름 있는 essential path로 승격 | A | serial queue unit | 완료 |
| .NET-19 | G4-F92 | leaf package가 사용하지 않는 framework transitive dependency를 직접 고정 | package 경계에 상위 구현 dependency graph 누출 | leaf pack에서 transitive pinning을 끄고 직접 dependency만 기록 | nuspec dependency를 수동 필터링 | A | fixed package snapshot, clean consumer | 완료 |
| .NET-19 | G4-F93 | 세 internal capability builder가 단일 bind assignment만 감쌈 | pass-through class와 호출되지 않는 method | 기존 endpoint support에 검증을 모으고 builder 삭제 | capability builder에 전체 책임 이동 | A | channel/route registration unit | 완료 |
| .NET-19 | G4-F94 | peer row acceptance 정책이 resolver와 runtime query에 중복 | domain policy와 diagnostic 문구 분산 | observed-generation owner가 값 검증과 단조 수락을 함께 소유 | 새 acceptance policy class 추가 | A | location resolver/query unit | 완료 |
| .NET-19 | G4-F95 | location health scalar 두 개가 production에서 사용되지 않고 snapshot과 병행 | test-only seam과 상태 읽기 경로 중복 | scalar를 삭제하고 실제 snapshot을 test | internal diagnostic interface 추가 | A | location runtime unit | 완료 |
| .NET-19 | G4-F96 | Redis public XML comment가 Lua, TTL과 임시 draft 번호를 계약처럼 설명 | 구현 결정이 public package 문서 hash에 노출 | public comment는 atomic ownership 계약만 유지하고 구현 설명을 internal owner로 이동 | public comment에서 internals 문서를 링크 | A | XML package snapshot, documentation review | 완료 |
| .NET-20 | G4-F97 | STREAM session 생성마다 configured assembly 전체를 다시 reflection scan | 검증한 handler discovery 결과가 session runtime에 전달되지 않아 startup 지식이 두 경로에 존재 | registration immutable catalog에 unbound session descriptor를 포함하고 session은 filter/bind만 수행 | session registry 내부에 assembly별 static cache 추가 | A | session handler registry/unit, production scan no-hit | 완료 |
| .NET-20 | G4-F98 | framework와 Redis location key codec이 length-prefix와 peer identity 알고리즘을 각각 구현 | canonical wire 지식 중복과 변경 증폭 | 중립적인 internal shared-source formatter를 두 package가 함께 compile | 두 구현을 유지하고 golden vector만 강제 | A | framework/Redis key codec golden vectors, package verifier | 완료 |
| .NET-20 | G4-F99 | Protobuf package가 friend assembly로 framework internal zero-copy ABI를 사용 | 독립 배포 extension이 public contract 밖 내부 변경에 결합 | public serializer와 `ZLinkEncodedPayload.From`만 사용하고 friend grant 삭제 | 새 public zero-copy capability를 spec에 추가 | A | protobuf serialization/contract/package test | 완료 |
| .NET-20 | G4-F100 | connector connect/disconnect callback이 같은 lifecycle 순서를 공유하지 않아 `Disconnected`가 `Connected`보다 먼저 관측될 수 있음 | 상태 전이와 callback 게시 owner 분리, 시간 순서 결합 | lifecycle owner의 단일 직렬 event 경로로 상태와 disconnect reason을 게시 | test에서 순서 assertion을 완화하거나 retry | A | lifecycle deterministic race/repetition, connector 전체 | 완료 |
| .NET-21 | G4-F101 | Immediate callback에서 `Close.Async()`를 기다리면 Close가 현재 connect/receive/heartbeat task를 다시 기다릴 수 있음 | tracked worker가 user callback 완료를 소유하면서 callback의 종료 요청도 같은 worker 완료를 기다리는 순환 의존 | worker 전체 실행 문맥을 owner/kind로 표시하고 Close는 현재 자기 worker만 wait 대상에서 제외 | callback 호출부마다 task scope를 전달하거나 notification을 queue로 바꿈 | A | lifecycle/typed/error/UserCallbackFailed self-close, Immediate/Manual 반복 test | 완료 |
| .NET-23 | G4-F102 | worker-wide `AsyncLocal` identity가 callback의 detached child task에 상속되어 외부 Dispose를 reentrant 호출로 오인 | callback 재진입 권한과 worker identity 혼합, worker join 전 resource finalization 가능 | 중앙 callback 경계의 만료 가능한 permit과 worker를 항상 join하는 단일 finalizer를 분리 | callback을 별도 ordered executor에서만 실행 | A | detached/awaited child Close·Dispose, owner isolation, external repeated Dispose | 완료 |
| .NET-24 | G4-F103 | WebSocket graceful close가 취소되거나 실패하면 transport `Dispose()`를 건너뜀 | caller cancellation과 무조건 수행할 resource cleanup 혼합 | graceful handshake는 취소 가능하게 두되 transport release는 `finally`에서 보장 | lifecycle에 별도 abort/dispose capability 추가 | A | canceled/faulted WebSocket close cleanup | 완료 |
| .NET-24 | G4-F104 | 외부 `Close.Async(token)`이 공유 worker termination 대기에서 token을 무시 | caller wait cancellation과 non-cancelable shared shutdown 의미 혼합 | shared termination은 계속하고 외부 대기만 `WaitAsync(token)`으로 취소 | Close token을 문서상 무효로 정의 | A | blocked-worker Close cancellation, repeated Close | 완료 |
| .NET-24 | G4-F105 | callback 안의 `DisposeAsync()`가 실제 finalization 전에 완료된 것으로 반환 | 교착 회피가 `IAsyncDisposable` 완료 의미를 깨뜨림 | callback에서는 Dispose를 명시 오류로 거부하고 callback-safe Close와 외부 Dispose를 사용 | callback executor를 transport worker에서 분리 | A | callback Dispose rejection과 후속 external Dispose | 완료 |
| .NET-25 | G4-F106 | worker termination task가 terminal callback과 transport close 오류를 포함하지 않아 concurrent Close/Dispose가 먼저 완료될 수 있음 | 하나의 close transaction이 두 completion owner로 분리 | transport 해제, worker join, terminal event와 최종 오류를 단일 shared full-close task가 소유 | worker termination과 close completion task를 별도로 유지 | A | blocked terminal callback, concurrent Close/Dispose, shared close fault | 완료 |
| .NET-25 | G4-F107 | Close caller cancellation이 noncancelable terminal callback을 직접 기다려 반환하지 못함 | shared cleanup 진행과 caller wait cancellation 분리 부족 | full-close task는 계속 실행하고 각 caller만 `WaitAsync(token)`으로 대기 | caller token을 terminal callback에 전달 | A | canceled Close와 later shared completion, terminal callback self-Close | 완료 |
| .NET-26 | G4-F108 | 결합된 비동기 event delegate 호출이 마지막 subscriber의 `ValueTask`만 관찰 | callback 순서, 완료와 오류 정책이 multicast delegate 동작에 누출 | invocation list를 snapshot하고 공통 dispatcher가 각 subscriber를 등록 순서대로 await | 전용 subscriber collection으로 public event 저장소 교체 | A | multiple subscriber order/block/error/self-Close | 완료 |
| .NET-26 | G4-F109 | TCP/TLS stream dispose가 실패하면 `TcpClient` 해제를 건너뜀 | transport별 자원 소유권과 오류 경로가 다름 | stream 정리는 시도하되 socket release는 `finally`에서 보장 | transport resource owner를 새로 도입해 오류 수집 | A | faulting stream과 socket release, repeated Close fault | 완료 |
| .NET-26 | G4-F110 | close 오류 검증용 nullable transport delegate가 connector와 lifecycle을 관통 | test 관심사와 production 연결 정책 혼합, 패스스루 parameter | timeout, metric과 오류 분류를 함께 소유하는 internal transport provider로 격리 | production constructor와 test fixture 조립 경로를 별도로 유지 | B | production/test provider timeout·metric·fault contract | 완료 |
| .NET-26 | G4-F111 | worker scope와 callback permit이 같은 `AsyncLocal` lease 알고리즘을 중복 구현하고 unused 조회 함수가 남음 | 실행 문맥 활성 규칙 중복과 dead code | 하나의 internal execution-context owner가 worker와 callback lease를 캡슐화 | metadata만 공유하는 범용 ambient lease primitive 추가 | A | awaited/detached child, owner isolation, Manual dispatch | 완료 |
| .NET-26 | G4-F112 | 자동 발견된 Spot/session handler가 DI scope에 등록되지 않으면 dispatch마다 생성되고 해제되지 않음 | handler 등록과 수명 소유권 분리, 호출자에게 중복 DI 지식 전가 | Spot/session scope별 handler owner가 provider resolve와 fallback cache/dispose를 함께 소유 | 모든 scanned type을 scoped DI에 등록하고 동적 type은 호출마다 해제 | A | fallback instance reuse/dispose, explicit DI ownership, init failure | 완료 |
| .NET-27 | G4-F113 | Manual 및 worker 밖 callback은 active callback lease가 없어 `DisposeAsync` 거부를 우회 | callback 활성 상태와 Close worker 재진입 권한을 하나의 조건으로 사용 | 모든 callback에 owner lease를 만들고 worker-derived Close 권한은 별도 metadata로 판단 | Manual callback만 별도 검사 분기 | A | Manual typed/state/error, detached completion Dispose, external Dispose | 완료 |
| .NET-27 | G4-F114 | disconnect 중 transport close 예외가 terminal cleanup과 reconnect 시작을 중단 | transport 정리 오류와 상태·pending·reconnect 완료 책임 혼합 | close 오류를 보존하면서 CTS/pending/event/reconnect 처리를 끝까지 수행한 뒤 전파 | close 오류를 error callback으로만 보고 삼킴 | A | faulting disconnect close, pending failure, reconnect start, CTS cleanup | 완료 |
| .NET-27 | G4-F115 | 강제 session 종료 flag가 지연 callback 종료 뒤에도 handler와 DI scope 정리를 영구 억제 | 일시적인 concurrent cleanup 방지와 최종 소유권 포기를 같은 flag로 표현 | serial drain 완료 뒤에는 fallback handler, context와 DI scope를 정확히 한 번 정리 | force-close가 별도 background cleanup task를 소유 | A | forced shutdown blocked callback release, fallback/DI dispose once | 완료 |
| .NET-28 | G4-F116 | receive guard가 disconnect 정리 오류를 receive decode 오류로 다시 분류해 disconnect를 중복 실행 | receive 실행과 lifecycle terminal transaction의 예외 경계 혼합 | receive loop만 catch하고 disconnect transaction은 catch 밖에서 실행 | terminal 전용 예외 타입으로 재분류 차단 | A | EOF/server close fault, no duplicate error/disconnect, reconnect 유지 | 완료 |
| .NET-28 | G4-F117 | force shutdown이 application handler cancellation을 제거하거나 callback 종료 전에 queue drain으로 판정 | cancellation 전달과 실제 callback completion join이 분리 | serial token을 취소하고 work item은 callback task 종료 뒤 completion을 확정 | session executor만 별도 active task를 추적 | A | cancellation-aware/ignoring handler, suspended work, late cleanup | 완료 |
| .NET-28 | G4-F118 | initial connect worker 정리 전 즉시 EOF가 `Reconnecting`으로 전이하면 reconnect task를 만들지 못함 | active-connect task identity와 reconnect 시작 시점의 경쟁 | active-connect clear가 `Reconnecting` 상태를 확인해 후속 reconnect task를 원자 생성하고 시작 | disconnect가 initial connect 정리를 별도로 기다린 뒤 reconnect 생성 | A | immediate EOF close fault, reconnect start와 새 connection 유지 | 완료 |
| .NET-29 | G4-F119 | force shutdown의 동기 `CancellationTokenSource.Cancel()`이 application cancellation callback에서 block | force upper bound와 cancellation callback 실행 책임 혼합 | `CancelAsync()`로 비차단 시작하고 executor Dispose가 cancellation task와 callback drain을 함께 join | cancellation을 별도 thread-pool work item으로 감싸고 관찰 | A | blocking cancellation registration, force upper bound, late cleanup | 완료 |
| .NET-30 | G4-F120 | terminal lifecycle token의 동기 `Cancel()`도 `OnError`/`OnDisconnected` cancellation callback에서 block | application token과 terminal token의 force cancellation 정책 불일치 | terminal `CancelAsync()` task를 session이 소유하고 Dispose가 callback drain 뒤 join·CTS 해제 | terminal callback을 별도 executor로 이동 | A | blocking terminal cancellation registration, node upper bound, CTS cleanup | 완료 |
| .NET-32 | G4-F121 | serial queue가 final work를 거절하면 terminal 상태를 되돌리고 transport와 session scope 정리를 시작하지 않음 | terminal admission과 최종 자원 소유권이 분리되어 종료 경쟁에서 정리 책임이 사라짐 | terminal 상태가 queued work와 dispose 중 어느 쪽이 close를 소유하는지 보관하고, 거절 즉시 shared Dispose를 시작 | terminal 상태를 되돌린 뒤 일반 Dispose가 나중에 호출되기를 기다림 | A | rejected terminal admission, transport close, session 제거와 scoped dependency dispose | 완료 |
| .NET-32 | G4-F122 | connector metrics test reader가 raw sample을 무한 `ConcurrentQueue`에 적재 | 검증 도구가 공통 spec의 bounded snapshot 규칙과 장시간 실행 조건을 위반 | reader가 최근 4,096 sample만 유지하고 listener 미등록 production 계기는 event별 상태를 보관하지 않음을 검증 | test마다 collector를 새로 만들고 종료 시 전체 해제 | A | 8,192 event bounded reader, listener 미등록 1,000,000 event allocation | 완료 |
| .NET-33 | G4-F123 | rejected terminal dispose와 force-close가 경쟁하면 둘 다 transport close를 실행할 수 있음 | terminal state ownership과 실제 transport close transaction이 별도 상태로 존재 | 모든 session 종료 경로가 하나의 shared transport-close task를 사용하고 성공/실패 신호도 그 task가 한 번만 확정 | force-close가 terminal owner를 빼앗는 별도 상태 전이를 추가 | A | blocked transport close 중 rejected terminal dispose/force race, disconnect 1회 | 완료 |
| .NET-34 | G4-F124 | 언어별 계약 문서가 삭제된 E2E test class를 회귀 증거로 참조하고 documentation test가 22개 class allowlist로 이를 건너뜀 | 계약 증거와 실제 test inventory가 분리되어 존재하지 않는 검증도 통과 | allowlist를 삭제하고 각 행을 active unit test 또는 실제 feature-map `E2E:<id>`로 연결하며 unresolved 참조 전체를 실패 | 삭제된 과거 test 이름을 문서에서 모두 제거하되 대체 증거 연결은 별도 ledger에만 기록 | A | documentation regression active method/scenario resolution, removed class no-hit | 완료 |
| .NET-34 | G4-F125 | regression matrix가 제거된 public `Yield(...)`와 Spot yield dispatch를 release gate로 요구 | 확정 Automatic Turn Dispatch 계약과 내부 검증 문서가 반대 의미를 소유 | matrix를 단일 `Async(...)` terminator, turn 반납과 원래 실행 줄 복귀 test/E2E로 교체 | 오래된 절을 삭제하고 Config 8 문서만 링크 | A | matrix AutomaticTurn guard, stale Yield wording no-hit | 완료 |
| .NET-35 | G4-F126 | Redis 최초 connect와 `DisposeAsync` 경쟁이 뒤늦은 connection 저장, disposed gate release와 누수를 허용 | connection 초기화와 종료가 서로 다른 lifecycle owner를 사용 | disposed 상태와 connect gate를 같은 임계 구역에서 관리하고 in-flight 초기화 뒤 정리 | multiplexer를 외부 주입해 DI가 수명 소유 | A | F146 shared dispose transaction까지 통합; Redis lifecycle 2, 전체 39 PASS | 완료 |
| .NET-35 | G4-F127 | Manual callback queue overflow가 request completion을 background runner에서 실행 | Manual 호출 문맥 계약과 overflow 복구 책임 혼합 | request 제출 전에 completion slot을 예약하고 Manual queue에서만 실행 | droppable event queue와 bounded lossless completion queue 분리 | A | overflow request callback thread, close/cancel, bounded admission | 완료 |
| .NET-35 | G4-F128 | 독립 connector/framework package가 friend access로 internal wire ABI를 공유 | 배포 모듈 경계와 실제 정보 소유권 불일치 후보 | protocol source를 각 assembly에 linked compile하면 static correlation owner가 다시 둘로 갈라짐 | package를 합치면 확정된 6개 배포 계약이 바뀜 | 유지 | connector 단일 protocol owner와 F148 exact NuGet dependency `[version]`을 함께 검증 | 완료 |
| .NET-35 | G4-F129 | monitor polling이 `Recv(DontWait)`의 정상 no-data를 예외로 바꿈 | 정상 부재를 오류 제어 흐름으로 정의 | internal `TryRecv`로 no-data를 반환하고 실제 사용 중인 monitoring callback capability는 유지 | callback-only monitor로 polling 삭제 | A | no-data no-throw, real event와 callback monitoring | 완료 |
| .NET-35 | G4-F130 | flow scope와 tracer가 ID 생성을 함께 소유해 observer-only에서 미생성, 누락 scope는 event별 fallback ID로 은폐 | identity 생성 정책 분산과 unknown-unknown 은폐 | capture enabled를 log/observer 통합 정책으로 두고 scope만 생성, tracer는 검증·발화만 수행 | consumer 없이도 항상 flow 생성 | A | observer-only wire propagation, true Off no generation, no-scope rejection | 완료 |
| .NET-35 | G4-F131 | dealer wrapper가 set-only `PeerWeight`를 mirror하고 binding submit/recv/dispose synchronization을 중복 | bindings option/lifecycle 정책 누출과 pass-through lock | bindings getter를 추가하고 framework mirror를 제거한다. wrapper gate는 core의 in-flight API와 close 사이 `EBUSY`를 막는 더 강한 수명 불변식이므로 유지한다. | binding synchronization만 사용하고 close 경쟁의 `EBUSY`를 호출자에게 노출 | A + 수명 gate 유지 | bindings 178, framework adapter 5 PASS; `Systems.Zlink 8.6.6` package SHA-256 `dae37c26458d965cc63114d1fe846b129208feff815dc0a22ddd5f904854f84e` | 완료 |
| .NET-35 | G4-F132 | channel/route validator가 handler group/name/key/conflict 정책을 복제 | handler exposure 지식이 두 owner에 분산 | normalized `HandlerExposure` catalog와 단일 validator | selector가 많은 generic helper | A | handler exposure 19, channel/route registration 35 PASS; 이전 정책 symbol과 catalog 밖 정책 오류 문자열 no-hit | 완료 |
| .NET-35 | G4-F133 | actor location lifecycle이 claim-activate와 raw claim을 함께 노출하고 raw path는 test setup만 사용 | aggregate half-state와 시간적 분해를 production interface에 노출 | raw claim을 coordinator 내부로 숨기고 test는 store/runtime fixture로 seed | 두 API를 유지하고 caller discipline에 의존 | A | production raw claim no-hit, claim rollback/reconcile, fixture seed | 완료 |
| .NET-35 | G4-F134 | location/actor owner의 sync `Dispose`는 cancel만 하고 task·CTS·semaphore 정리를 완료하지 않음 | 동일 type의 dispose 완료 의미가 두 개 | internal async-only ownership으로 host/DI가 full drain을 await | sync Dispose가 bounded blocking으로 full drain | A | async host disposal, background join, primitives dispose once, sync surface no-hit | 완료 |
| .NET-35 | G4-F135 | public 주석이 backing socket, cache, canonical codec 같은 내부 결정을 보장 | 구현 지식 누출과 변경 제약 | 즉시 반영, startup-only, live-owner filtering 등 관찰 가능한 계약만 기술 | 주석 전부 제거 | A | 최초 표현과 F145 추가 누출까지 제거; package XML/API verifier PASS | 완료 |
| .NET-35 | G4-F136 | framework dealer별 1ms request completion pump가 bindings 자동 progress 기능을 대체·비활성화 | core/bindings 기능 재구현과 busy-poll thread | framework pump를 삭제하고 bindings 자동 progress 사용 | bindings에 progress latency 정책을 추가 | A | request completion without framework poller, cleanup/thread no-hit | 완료 |
| .NET-35 | G4-F137 | core retry option과 framework의 ready 기반/25ms polling submitter가 retry 정책을 삼중 소유한 후보 | transport retry와 framework async 수명 책임의 유사성 | core socket retry는 호출 안에서 transport submit만 반복하며 framework의 bounded queue 수락·caller cancellation을 대신하지 못함 | actor bound-session SpotNode에는 bindings public retry option이 없어 새 binding 계약이 필요 | 유지 | `ZLinkAsyncSubmitterTests`의 bounded admission/ready callback과 bound-session relay timeout/cancellation tests로 서로 다른 책임 고정 | 완료 |
| .NET-35 | G4-F138 | actor bound-session registry가 process static이며 key에 runtime identity가 없음 | runtime aggregate 밖 전역 상태와 cross-host cleanup | runtime instance registry로 이동 | process registry에 runtime 복합 key와 scoped cleanup 추가 | A | two-runtime same RID isolation, runtime cleanup | 완료 |
| .NET-35 | G4-F139 | runtime error sink가 static event이고 executor/submitter가 ad-hoc sink를 생성 | host별 lifecycle/logging 오류 owner 누출 | host-scoped sink 하나를 runtime state가 소유하고 주입 | runtime identity가 있는 process broker와 subscription lease | A | static sink 참조 no-hit, unawaited submit 12곳 scoped sink 전달; 집중 25와 영향 61 PASS, build 0 warning/error | 완료 |
| .NET-35 | G4-F140 | actor creation/ownership/handoff가 동일 100ms 무한 reconciliation loop를 여러 곳에 복제 | backoff, terminal 분류, shutdown과 logging 정책 분산 | operation/classifier를 받는 깊은 reconciliation runner로 집약 | aggregate별 policy와 공통 backoff/observability 조합 | A | runner retry/terminal/shutdown/cancel 4와 관련 aggregate 98 PASS; 남은 100ms polling은 예외 reconciliation이 아닌 prepared-commit admission 상태 대기 | 완료 |
| .NET-35 | G4-F141 | public `ZLinkMessage` value가 mutable codec registry와 wire codec을 보관 | value 의미가 외부 runtime configuration 변화에 의존 | startup 후 immutable codec snapshot을 message에 저장 | 생성 시 serializer 하나만 resolve해 저장 | A | registry mutation isolation, lazy decode, custom codec/envelope parity | 완료 |
| .NET-36 | G4-F142 | local Entry Spot fallback이 native target actor를 만든 뒤 activation 누락, admission 거절·예외·취소에서 이번 시도의 actor를 해제하지 않음 | actor 생성과 admission commit/보상 책임이 분리되어 미소유 native actor가 남을 수 있음 | `createdHere`를 소유하는 transaction이 commit 전 모든 비성공 경로에서 기존 teardown/reconciliation 정책으로 새 actor만 해제 | admission을 먼저 실행한 뒤 승인 시 native actor 생성 | A | local rejection/activation 누락/예외/취소/cleanup retry/기존 actor 6, Entry Spot 전체 46, unit 567 PASS | 완료 |
| .NET-36 | G4-F143 | 같은 handler type을 mapped group과 explicit 등록에 함께 노출하면 validator는 허용하지만 실제 registry가 중복으로 거절 | fail-fast exposure catalog와 runtime descriptor 설치 규칙 불일치 | `(kind, packet)` 노출이 반복되면 handler type이 같아도 모두 configuration 단계에서 거절 | explicit 등록 우선순위를 정의하고 동일 descriptor를 중앙 deduplicate | A | same-type mapped+explicit channel/route 포함 handler/runtime boundary 29 PASS | 완료 |
| .NET-36 | G4-F144 | runtime event handler 실패 보고 중 runtime generation이 종료되면 throwing `ErrorSink` getter가 원래 실패를 다른 예외로 교체 | 격리된 callback 오류 보고와 generation 수명 경계 불일치 | 기존 nonthrowing runtime report 경로를 사용해 종료 중 보고는 no-op | dispatcher에 generation-safe reporter를 별도 주입 | A | stopped runtime과 stop race의 throwing handler 포함 집중 test PASS | 완료 |
| .NET-36 | G4-F145 | location/query/store/config의 public XML 주석에 direct store, no cache, cache invalidation, live socket/transport 상태가 추가로 남음 | 관찰 가능한 계약 대신 현재 구현 결정을 고정 | freshness, connection admission, runtime 적용 결과만 설명하도록 수정 | 세부 설명을 제거하고 정식 계약 문서를 참조 | A | 금지 구현 표현 no-hit; 후속 internal 문서 산출물까지 포함한 XML hash `63e227c91aebe18ce66e685a64f2877e5895bf4008ab6385c24c34a774c7d003`, package/API verifier PASS | 완료 |
| .NET-36 | G4-F146 | Redis의 두 concurrent `DisposeAsync` 중 후속 호출이 첫 connection 종료 transaction을 기다리지 않고 반환하며 connect gate도 정리되지 않음 | `_disposed` flag와 실제 종료 완료 owner가 분리 | 하나의 shared dispose task가 connect drain, connection과 gate 정리를 소유하고 모든 호출자가 await | connection resource owner를 분리해 DI 수명에 위임 | A | blocked connection disposal concurrent Dispose와 exactly-once cleanup 2, Redis 전체 39 PASS | 완료 |
| .NET-36 | G4-F147 | connector request callback과 inbound observer detached task가 connector finalization에 join되지 않아 Dispose 뒤 실행되거나 Manual callback이 남을 수 있음 | background task 생성과 종료 완료 책임이 분리 | owned task runner가 admission 중단, cancellation과 drain을 소유하고 connector가 primitive 해제 전 await | request/observer dispatcher가 각각 Complete/DrainAsync 제공 | A | 집중 6, connector 전체 124 PASS; WebSocket disconnect self-dependency 회귀 포함 | 완료 |
| .NET-36 | G4-F148 | connector internal ABI를 friend로 공유하면서 framework NuGet dependency가 exact range가 아님 | 숨은 internal 결합 제약이 package resolver에 전달되지 않음 | framework의 connector dependency를 exact package range로 고정하고 verifier가 `[{VERSION}]` 강제 | friend ABI를 정식 wire-contract package로 분리하거나 package 통합 | A | 실제 `9.9.9` nuspec `version="[9.9.9]"`, normalized snapshot `[{VERSION}]`과 verifier guard PASS | 완료 |
| .NET-37 | G4-F149 | native Entry Spot/User Spot join callback이 caller 취소 뒤 늦게 전달한 reply `Message`의 `TrySetResult` 실패를 무시해 parts를 누수 | terminal completion과 native message 소유권 정책이 두 모듈에 중복되고 losing race owner가 없음 | 공통 join-completion owner가 완료·취소를 원자화하고 terminal race에서 진 모든 reply parts를 해제 | 두 callback에서 각각 `TrySetResult` 실패 시 해제 | A | 두 join late callback disposal과 정상 winner 3, Entry Spot 49, unit 574 PASS | 완료 |
| .NET-37 | G4-F150 | native User Spot join의 multipart request가 backend submit 예외 시 해제되지 않음 | transport 제출 예외 정책과 request message 소유권 분리 | `JoinActor` 제출을 `try/finally`로 감싸 입력 parts를 항상 해제하고 reply는 F149 completion owner가 소유 | request와 completion을 포괄하는 generic native join owner 추출 | A | F149/F150 집중 4와 unit 575 PASS; backend throw에서 parts dispose와 원래 예외 보존 | 완료 |
| .NET-38 | G4-F151 | internal `ZLinkLocationRuntime`의 sync `Dispose`는 heartbeat CTS만 닫고 loop, owner lease, rows와 lifecycle gate 정리를 기다리지 않음 | 같은 type의 sync/async dispose 완료 의미가 다르고 DI store보다 background work가 늦게 끝날 수 있음 | `IDisposable`과 sync path를 제거하고 async-only shared disposal/host stop으로 통일 | sync Dispose가 async transaction을 blocking wait | A | blocked heartbeat/concurrent Dispose와 sync DI fail-fast 2, location runtime 12, unit 577 PASS | 완료 |
| .NET-38 | G4-F152 | Redis store의 connect lease가 `IDatabase` 반환 전에 끝나 실제 command 진행 중 Dispose가 shared connection을 닫을 수 있음 | connection 초기화 수명과 admitted operation 수명이 분리 | 모든 public command 전체를 중앙 operation lease로 감싸고 Dispose가 admission 중단 뒤 전체 drain | connection 수명을 외부 DI owner로 이동 | A | blocked command/concurrent Dispose/admission-after-dispose 1 포함 lifecycle 3, Redis 전체 40 PASS; public command 우회 no-hit | 완료 |
| .NET-38 | G4-F153 | SPOT route bridge request가 cancellation/timeout 뒤 도착한 정상 native reply의 `TrySetResult` 실패를 무시해 parts를 누수 | terminal race와 losing reply 소유권 정책이 actor join과 route request에 다르게 구현 | F149 actor 전용 completion을 범용 native reply completion owner로 승격해 cancellation/timeout/winner/loser를 단일 소유 | raw completion의 각 callback에서 실패 시 parts 해제 | A | route cancellation/timeout late OK, winner/duplicate와 actor 회귀 7, unit 580 PASS; native callback audit clean | 완료 |
| .NET-39 | G4-F154 | async-only `ZLinkLocationRuntime.DisposeAsync` 시작과 대기 중인 `StartAsync`가 경쟁하면 Stop gate 해제 뒤 새 heartbeat generation이 시작되거나 semaphore dispose와 충돌 | shared dispose task에 terminal lifecycle state가 없어 Start admission이 종료와 직렬화되지 않음 | dispose transaction 생성 전에 terminal state를 고정하고 Start가 gate 전·후 모두 거절하며 private semaphore는 object lifetime 동안 유지 | Start/Stop/Dispose를 단일 lifecycle operation queue/state machine으로 교체 | A | disposal 집중 3, location runtime 13, unit 581 PASS; queued/future Start 거절과 shared Dispose 검증 | 완료 |
| .NET-39 | G4-F155 | `ZLinkLocationRuntime.StopAsync`가 caller token 취소의 `OperationCanceledException`을 store failure로 기록하고 성공 반환 | caller cancellation과 store 장애 의미 혼합으로 내부 cancellation 계약 무효화 | caller-requested cancellation을 store failure catch보다 먼저 다시 throw하고 heartbeat/state cleanup은 안전하게 유지 | Stop을 non-cancelable cleanup으로 정의하고 token 제거 | A | caller cancellation 집중 1, location runtime 14, unit 582 PASS; 후속 Stop/shared Dispose 안전 | 완료 |
| .NET-39 | G4-F156 | `ZLinkAsyncSubmitter` request가 native 계층에 수락된 뒤 caller cancellation registration을 제거해 공개 wait가 reply/native timeout까지 계속됨 | native operation 완료와 caller waiter cancellation이 한 completion에 결합 | native accepted operation owner와 caller waiter를 분리해 caller는 즉시 취소되고 late raw/envelope reply는 끝까지 관찰·정리 | 각 public request 호출부가 `WaitAsync(token)`과 late reply ownership을 반복 | A | submitter/failure mapping 26, unit 588 PASS; envelope 2·raw 2 overload late ownership audit clean | 완료 |
| .NET-40 | G4-F157 | `ZLinkAsyncSubmitter.DisposeAsync`가 active Drain/immediate native submit을 join하지 않고 queue parts와 owner socket 정리를 진행할 수 있음 | admission, drain 실행권, native submit과 disposal completion 수명 지식 분산 | shared async disposal transaction이 admission 중단, active drain/submit join 뒤 남은 item fail/dispose를 정확히 한 번 소유 | queue item lease 기반 `CompleteAndDrainAsync` | A | blocked immediate/Drain 경쟁 포함 submitter 16, unit 590 PASS; repeated Dispose same task와 post-close rejection | 완료 |
| .NET-40 | G4-F158 | `ZLinkSessionRequestTracker.Start()` production caller가 없어 session inbound response tracker/pending Message lifecycle이 도달 불가능 | dead request completion surface와 소유권 코드가 실제 public contract처럼 남음 | tracker, context interception과 runtime response branch/file을 삭제 | public session request 계약을 새로 연결 | A | tracker/file/`TryCompleteResponse` production no-hit; stream/session 집중 28와 unit 600 PASS | 완료 |
| .NET-40 | G4-F159 | `ZLinkLocationAutoConnectHost.DisposeAsync` 후속 호출이 첫 cleanup completion을 기다리지 않고 반환 | dispose flag와 실제 종료 완료 owner 분리 | 모든 호출자가 shared dispose task를 await | single DI owner/proxy로 중복 dispose 금지 | A | shared disposal owner 집중 7와 unit 600 PASS; identical task와 exception 공유 | 완료 |
| .NET-40 | G4-F160 | `ZLinkMonitoringHostedService.DisposeAsync` 후속 호출이 첫 cleanup completion을 기다리지 않고 반환 | dispose flag와 listener/task/resource 종료 완료 owner 분리 | 모든 호출자가 shared dispose task를 await | hosted owner 하나만 dispose하도록 등록 구조 변경 | A | shared disposal owner 집중 7와 unit 600 PASS; identical task와 exception 공유 | 완료 |
| .NET-40 | G4-F161 | `ZLinkLocationStoreInstanceOwner.DisposeAsync` 후속 호출이 첫 store disposal completion을 기다리지 않고 반환 | exactly-once 시작과 모든 caller completion 의미 분리 | store disposal shared task를 모든 호출자가 await | DI descriptor 하나만 disposable owner로 구성 | A | shared disposal owner 집중 7와 unit 600 PASS; identical task와 exception 공유 | 완료 |
| .NET-40 | G4-F162 | user-reachable `ZLinkTimer.CancelAsync`/`DisposeAsync` 후속 호출이 첫 pump/CTS 정리 completion을 기다리지 않고 반환 | cancel 시작 flag와 public 종료 완료 의미 분리 | 모든 Cancel/Dispose 호출자가 shared finalization task를 await | timer owner가 별도 stop request와 single disposer 제공 | A | blocked pump/failure 공유 포함 lifecycle 11, unit 604 PASS; identical completion | 완료 |
| .NET-40 | G4-F163 | `ZLinkSpotActivation.DisposeAsync` 후속 호출이 serial/native Spot/handler scope cleanup 진행 중 즉시 반환 | catalog의 여러 cleanup caller가 동일 activation 종료 완료를 다르게 관측 | activation이 shared finalization task를 소유 | catalog 단일 disposal transaction과 non-owning stop request | A | native/serial/handler cleanup 경쟁 포함 lifecycle 11, unit 604 PASS | 완료 |
| .NET-40 | G4-F164 | `ZLinkEntrySpotActivation.DisposeAsync` 후속 호출이 첫 serial/native Spot/handler scope cleanup completion을 기다리지 않고 반환 | exactly-once flag와 모든 caller의 완료 의미 분리 | activation이 shared finalization task를 소유 | catalog 단일 disposal transaction과 non-owning stop request | A | serial/scope cleanup 경쟁 포함 lifecycle 11, unit 604 PASS | 완료 |
| .NET-40 | G4-F165 | `ZLinkSerialExecutionQueue.DisposeAsync` 후속 호출이 첫 caller의 drain과 gate 정리 전에 반환 | serial queue 종료 시작과 완료 owner 분리 | shared finalization task를 모든 호출자가 await | queue owner만 dispose하고 내부 method는 stop request로 축소 | A | blocked cleanup 포함 shared disposal 집중 7, unit 600 PASS; cleanup exception 공유 | 완료 |
| .NET-40 | G4-F166 | `ZLinkChannelRuntimeBundle.DisposeAsync` 후속 호출이 submitter/socket/receive gate 정리 전에 반환 | channel resource aggregate의 exactly-once flag와 완료 의미 분리 | shared finalization task를 모든 호출자가 await | 상위 runtime만 bundle dispose를 소유 | A | blocked cleanup 포함 shared disposal 집중 7, unit 600 PASS; cleanup exception 공유 | 완료 |
| .NET-40 | G4-F167 | `ZLinkScopedHandlerInstanceOwner.DisposeAsync` 후속 호출이 fallback handler 비동기 정리 전에 반환 | handler lifetime owner의 dispose flag와 completion 분리 | shared finalization task를 lock 안에서 생성하고 모든 호출자가 await | scope root 단일 disposer로 축소 | A | blocked cleanup 포함 shared disposal 집중 7, unit 600 PASS; cleanup exception 공유 | 완료 |
| .NET-40 | G4-F168 | `ZLinkMessageFlowObserverPump.DisposeAsync` 후속 호출이 observer와 async scope 정리 전에 반환 | diagnostics owner의 dispose flag와 actual finalization 분리 | shared finalization task를 모든 호출자가 await | runtime state가 sole disposer이고 pump는 stop request만 노출 | A | blocked cleanup 포함 shared disposal 집중 7, unit 600 PASS; cleanup exception 공유 | 완료 |
| .NET-40 | G4-F169 | `ZLinkSpotTimerRegistry`의 unsynchronized List Add/Dispose와 disposed admission guard 부재로 AddTimer가 activation 종료 snapshot과 경쟁 | timer collection mutation과 activation finalization 책임 분리 | registry가 lifecycle gate/closed state를 소유해 AddTimer admission과 snapshot dispose를 직렬화 | activation serial executor가 AddTimer와 Dispose 모두 직렬화 | A | post-close/100회 Add-vs-Dispose 포함 lifecycle 11, unit 604 PASS; all-timer failure aggregation | 완료 |
| .NET-41 | G4-F170 | `ZLinkFrameworkRuntimeState.DisposeAsync` 동시 호출이 전체 aggregate cleanup을 재실행하고 mutable collection을 서로 다른 시점에 열거 | runtime state 종료 admission과 child/resource finalization completion owner 부재 | lock에서 shared finalization task와 frozen child snapshot을 한 번 만들고 모든 caller가 await | top-level runtime만 sole disposer로 두고 child disposal surface 제거 | A | blocked context cleanup, identical task/error, exactly-once 포함 lifecycle 14, unit 611 PASS | 완료 |
| .NET-41 | G4-F171 | `ZLinkStreamNodeRuntime.DisposeAsync` 동시 호출이 CTS, monitor/socket와 session cleanup을 중복 실행 | stream node aggregate의 stop 요청과 full finalization completion 분리 | node가 shared finalization task를 소유 | runtime state sole disposer와 non-owning stop request | A | blocked socket, identical task/error와 exactly-once cleanup 포함 lifecycle 14 PASS | 완료 |
| .NET-41 | G4-F172 | `ZLinkRouteChannelRuntime.DisposeAsync` 동시 호출이 route bundle/socket/receive cleanup을 중복 실행 | route runtime aggregate finalization owner 부재 | runtime이 shared finalization task를 소유 | parent state sole disposer로 surface 축소 | A | blocked router, identical task/error와 exactly-once cleanup 포함 lifecycle 14 PASS | 완료 |
| .NET-41 | G4-F173 | `ZLinkSpotNodeRuntime.DisposeAsync` 동시 호출이 activation/bundle/native node cleanup을 중복 실행 | Spot node aggregate finalization owner 부재 | runtime이 shared finalization task를 소유 | parent state sole disposer로 surface 축소 | A | real runtime blocked native cleanup과 repeated node/catalog Dispose 포함 lifecycle 14 PASS | 완료 |
| .NET-41 | G4-F174 | `ZLinkSpotNodeBundleRegistry.DisposeAsync`가 closed state 없이 snapshot만 비워 종료 중/후 새 publisher bundle 생성을 허용 | bundle admission과 snapshot finalization 경계 분리 | gate 안에서 closed, shared task, snapshot/clear를 원자화하고 이후 GetOrCreate 거절 | owning Spot node serial executor로 creation/disposal 직렬화 | A | create-vs-dispose 100회, identical task, post-close rejection과 accepted exactly-once cleanup PASS | 완료 |
| .NET-41 | G4-F175 | public `ZLinkActorLocation` 주석이 claim→publish 내부 transient state와 query filtering을 설명하고 객체를 의인화 | 호출자가 관찰할 수 없는 lifecycle/query 구현을 공개 보장으로 고정 | `LocationKind`와 nullable `ActorRef`의 관찰 가능한 field 의미만 기술 | pending claim을 별도 internal representation으로 분리하고 public field non-null화 | A | forbidden 내부 표현 no-hit; XML hash `2195f3a5f7a2a1e8533ec0ae0649e1c9c5fee532fcfc7646c1bb527bac5eb644`, package verifier PASS | 완료 |
| .NET-41 | G4-F176 | `ZLinkSpotNodeCatalog.DisposeAsync` repeated/concurrent 호출이 catalog cleanup과 failure를 동일하게 관측하지 않음 | node/activation aggregate finalization completion owner 부재 | catalog가 shared finalization task를 소유 | parent Spot runtime sole disposer로 surface 축소 | A | real runtime blocked native cleanup과 direct catalog identical task 포함 lifecycle 14 PASS | 완료 |
| .NET-41 | G4-F177 | `ZLinkAutoConnectLoop.DisposeAsync` repeated/concurrent 호출이 wake/loop cleanup을 중복 실행할 수 있음 | loop stop flag와 final resource completion 분리 | loop가 shared finalization task를 소유 | AutoConnectHost sole disposer와 non-owning stop request | A | real store/runtime/reconciler fixture repeated Dispose identical task PASS | 완료 |
| .NET-41 | G4-F178 | `ZLinkStreamSessionSerialExecutor.DisposeAsync` repeated/concurrent 호출이 queue/stop source cleanup completion을 공유하지 않음 | session serial stop 시작과 full finalization owner 분리 | executor가 shared finalization task를 소유 | session runtime sole disposer로 surface 축소 | A | blocked queue와 concurrent Dispose identical task 포함 lifecycle 14 PASS | 완료 |
| .NET-41 | G4-F179 | direct child Spot node cleanup 뒤 parent aggregate의 정상 `RequestStop`이 disposed CTS를 cancel해 shutdown을 실패 | stop request와 child CTS finalization 지식 분리로 parent/child cleanup 순서 의존 | 같은 dispose gate로 cancel과 CTS final disposal을 직렬화하고 terminal 뒤 RequestStop은 no-op | CTS disposal을 top-level runtime sole owner로 이동 | A | real runtime child-first cleanup 뒤 parent `StopAsync`, shared child task와 no ODE PASS | 완료 |
| .NET-42 | G4-F180 | `ZLinkSpotNodeCatalog` 종료 snapshot과 creation admission이 다른 gate에 있고 closed state가 없어 pending/new activation이 snapshot 뒤 추가될 수 있음 | `_spots`/`_pending` collection admission과 finalization 지식이 catalog/parent에 분산 | collection gate에서 closed를 원자 설정하고 새 creation을 거절하며 admitted pending을 settle한 뒤 activation snapshot을 dispose | parent Spot-node admission lease만 catalog creation을 허용하고 catalog 독립 lifecycle 제거 | A | blocked Create/GetOrCreate race 2, post-close same-RID rejection, native cleanup exactly-once, unit 613 PASS | 완료 |
| .NET-43 | G4-F181 | actor client request는 router-only SPOT node를 사용하지만 one-way send는 actor factory가 있는 node만 요구해 원격 actor 전송을 거부 | actor 생성·소유권 node 선택과 outbound routing node 선택이 하나의 helper에 결합 | actor 소유권 선택은 유지하고 actor client send/request가 공통 routing node 선택 경계를 사용 | router-only caller마다 빈 actor factory를 등록 | A | router-only actor send 집중 test와 DeliveryDispatch tracking→customer actor 경로 | 완료 |
| .NET-43 | G4-F182 | 같은 process에 actor와 bound stream session이 있어도 push가 보관된 session context를 무시하고 native 원격 relay만 사용 | local session binding 정보와 bound-session 전송 정책이 분리 | binding token으로 local session context를 찾으면 해당 transport에 직접 쓰고, remote binding만 native relay 사용 | sample actor가 session client를 별도로 보관해 직접 전송 | A | local bound-session transport 집중 test, remote retained push 회귀와 DeliveryDispatch 전체 runner | 완료 |
| .NET-44 | G4-F183 | actor client routing node 선택이 pub/sub-only node를 포함한 등록 순서에 의존 | SPOT node 존재와 actor router capability를 같은 조건으로 취급 | 실제 router capability가 있는 node만 actor client routing 후보로 선택 | 모든 SPOT node에 사용하지 않는 router를 추가 | A | pub/sub-only node를 먼저 등록한 혼합 구성과 router-only actor send 집중 test | 완료 |
| .NET-44 | G4-F184 | local binding state와 session context가 별도 registry에서 전이될 때 context가 사라진 local token을 native binding으로 오인 | local/remote 소유권 판정이 context 조회 성공 여부에만 의존 | binding token의 owner kind를 확인해 stale local binding은 결정적으로 거부하고 native token만 원격 relay 허용 | 두 registry를 큰 전역 lock으로 묶어 stream write까지 직렬화 | A | stale local token no-native-fallback, local direct/remote retained push 집중 5 PASS | 완료 |
| .NET-45 | G4-F185 | DI는 locations+pub/sub-only node에도 actor client를 노출하고 actor factory validator도 router 없는 node를 허용 | registration, service exposure와 runtime node 선택이 서로 다른 actor-routing invariant를 사용 | 세 경계 모두 router capability를 단일 필수 조건으로 적용하고 actor factory는 configuration 단계에서 거부 | injected actor client가 첫 호출에서 runtime 오류를 내도록 유지 | A | pub/sub-only client 비노출, router 없는 actor factory startup 거부와 혼합 node routing test | 완료 |
| .NET-46 | G4-F186 | connector의 one-way send queue와 request 직접 write가 서로 다른 전송 경로를 사용해 나중 request가 먼저 전송될 수 있음 | 동일 connection의 outbound 순서 지식이 queue와 request 호출부에 분산 | accepted send와 request frame을 하나의 ordered outbound queue가 쓰고 request는 자기 frame의 실제 write 완료를 기다림 | request 전에 공개 flush API를 추가해 호출자에게 순서 제어를 요구 | A | `RequestQueueWaitsForEarlierAcceptedOneWaySend`, AutomaticTurnDispatch 전체 PASS | 완료 |
| .NET-46 | G4-F187 | request caller cancellation이 시작된 frame write를 중단하면 partial frame 뒤 같은 transport를 계속 사용할 수 있음 | caller wait cancellation과 transport framing 무결성 책임 혼합 | queue 진입 전 취소는 건너뛰고 write 시작 뒤에는 connector lifetime이 frame 전체 write를 소유 | byte 진행 상태를 추적해 첫 byte 전 취소만 보존하고 이후 transport를 강제 종료 | A | `CallerCancellationDoesNotInterruptAnInProgressFrameWrite`, connector focused 3 PASS | 완료 |
| .NET-46 | G4-F188 | request write 실패를 physical write owner와 request 호출부가 모두 transport failure로 처리 | 한 실패의 lifecycle 전이와 error 발행 owner 중복 | 모든 physical write가 통과하는 `SendFrameAsync`만 transport failure를 처리하고 request는 결과만 전파 | queue가 send/request error와 lifecycle을 모두 소유하도록 이동 | A | connector 전체 test와 Codex 재리뷰 `NO DDD/POSD FINDINGS` | 완료 |
| .NET-46 | G4-F189 | actor route fail-fast가 전체 manual endpoint 개수와 과거 설정 여부로 특정 target 연결을 추정 | endpoint 설정 이력과 target route 상태를 같은 정보로 취급 | 명시적인 target RID→endpoint mapping이 있고 그 target의 모든 endpoint를 호출자가 제거한 경우에만 fail-fast하며, unknown/inbound/symmetric route는 native routing에 위임 | core에 target RID별 live route query를 추가해 bindings를 통해 사용 | A | EntrySpot actor 58, TA-B3, Config 9 전체 PASS; Codex 재리뷰 `NO DDD/POSD FINDINGS` | 완료 |
| .NET-47 | G4-F190 | request correlation id 생성이 message-flow 관측 활성 여부에 묶여 tracing Off에서는 정식 request envelope를 만들지 못하고, initiating-call codec이 response/error에도 새 correlation id를 만들 수 있음 | wire 필수 식별자와 선택적 관측 정책이 같은 boolean에 결합되고 응답 상관관계의 소유권이 helper에 누출 | request는 관측 설정과 무관하게 correlation id를 항상 생성하고, initiating-call helper는 request/send/publish만 허용하며 response/error는 원 요청 correlation을 가진 reply 경로만 생성 | 관측 Off request를 비정식 envelope로 허용하거나 response/error helper에 호출자가 correlation id를 전달 | A | envelope focused 16, ObservabilityOps 13, 전체 E2E 181 PASS; Codex 재리뷰 `NO DDD/POSD FINDINGS` | 완료 |
| .NET-47 | G4-F191 | runtime `Weight` 변경이 native serving socket에만 반영되어 restart recipe와 location store peer row가 이전 값을 유지하고 reconciler change-stamp 최적화가 갱신을 건너뜀 | 동일한 runtime option의 serving, 재시작과 discovery 표현이 세 owner에 분산되고 외부 호출자는 어떤 값이 유효한지 알 수 없음 | live config가 native socket과 recipe를 원자 갱신한 뒤 auto-connect owner에 변경을 전달하고, reconciler가 같은 owner/generation row를 새 weight로 갱신할 때까지 pending 상태를 유지 | location runtime이 주기적으로 native socket 값을 읽거나 runtime weight 변경 시 row를 삭제·재등록 | A | same-owner row renewal unit, RL-B4 3회와 RL-B5 집중, ResilienceLifecycle 20, 전체 E2E 181 PASS; Codex 재리뷰 `NO DDD/POSD FINDINGS` | 완료 |
