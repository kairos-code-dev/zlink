검토 증거: 시작·종료 모두 177/177 파일별 hash 검증 통과. aggregate `c1cd9d0068931a5d13b31c49f18da8b5eef9cb0bdb2521896abff9f58fd30d5e`, file-list hash `9f072f1af2b73fde7c08143da66cb651bae33074bfc915a45d18431ccf345f25` 일치.

[1차소스][high] framework/doc/framework/common/sample/README.ko.md:336 — 모든 샘플의 routing id를 애플리케이션이 정한 문자열에서 만든다는 규칙은 하나의 sample 계약으로 구현할 수 없다 — Bingo는 `bingo/README.ko.md:8,191-205`에서 API·Play·Session RID 자동 할당을 필수로 하고, ZoneWorld도 `zoneworld/README.ko.md:319-339`에서 자동 할당을 필수로 하며 `zoneworld/README.ko.md:676-682`에서는 그 RID를 `ActorRefWire.NodeRid`로 전달한다 — 명시 문자열 규칙을 domain·Spot 식별자에만 한정하고 자동 할당 transport RID의 생성·노출 예외를 명시한다.

[1차소스][high] framework/languages/java/e2e/ToActorMessaging/feature-map.ko.md:18 — Docker를 사용할 수 없을 때 외부 Redis endpoint를 쓰는 fallback이 공통 E2E 실행 계약을 위반한다 — 공통 README `framework/doc/framework/common/e2e/README.ko.md:380-415`는 실행별 전용 Docker Redis를 의무화하고 Docker 실패 시 즉시 실패하며 host·외부 Redis fallback을 금지한다. Kotlin 문서 `framework/languages/java/e2e-kotlin/ToActorMessaging/feature-map.ko.md:15-16`도 같은 fallback을 제공한다 — 두 feature-map에서 외부 Redis fallback을 제거하고 전용 container 생성 실패를 명시적 실행 실패로 기록한다.

[1차소스][high] framework/languages/cpp/samples/Bingo/README.ko.md:68 — standalone client가 endpoint 환경 변수 fallback을 공개해 공통 sample 설정 전달 계약과 충돌한다 — 공통 sample README `framework/doc/framework/common/sample/README.ko.md:184-194`는 standalone client가 endpoint를 검증된 CLI option 또는 typed 설정 파일로 받고 환경 변수 interface를 제공하지 않도록 한다. TicTacToe 문서도 `framework/languages/cpp/samples/TicTacToe/README.ko.md:64-66`에서 같은 환경 변수 fallback을 제공한다 — 두 C++ client 문서에서 환경 변수 입력을 제거하고 CLI option만 계약으로 남긴다.

[1차소스][high] framework/languages/java/e2e/ResilienceLifecycle/README.ko.md:8 — client와 server가 공유하는 환경 변수 helper를 현재 구성 요소로 명시해 공통 E2E의 0-env 계약과 충돌한다 — 공통 README `framework/doc/framework/common/e2e/README.ko.md:358-374`는 server·client application이 직접 사용할 수 있는 환경 변수가 0개라고 고정한다. 같은 충돌이 `framework/languages/java/e2e/StoreFailure/README.ko.md:9`와 `framework/languages/java/e2e/RuntimeMonitoring/README.ko.md:8`에도 반복된다 — 세 README의 역할 설명을 role 설정 파일·검증된 CLI 입력 helper로 바꾸고 환경 변수 helper를 현재 구조에서 제거한다.

[1차소스][high] framework/doc/framework/spec/stream-connector/languages/dotnet/03-stream-connector.ko.md:64 — `.NET`과 Java만 수신 개수 조회 API를 정식 public contract로 추가해 Connector parity가 깨진다 — 공통 계약 `32-stream-connector.ko.md:478-489`의 push 관측 표면은 `waitFor`, `expectNone`, `waitForSequence` 세 가지이며 C++·TypeScript exact interface에는 count API가 없다. Java만 `languages/java/03-stream-connector.ko.md:56`에서 `receivedCount`를 반복한다 — 공통 계약으로 승격해 C++·TypeScript exact interface까지 같은 의미를 정의하거나, 근거 없는 `.NET`·Java public API를 exact contract에서 제거한다.

[1차소스][high] framework/doc/framework/spec/stream-connector/languages/cpp/03-stream-connector.ko.md:51 — C++만 connector codec registry와 send/request별 `codec(...)` 선택을 공개해 다른 exact interface와 다른 호출 표면을 만든다 — 같은 문서 `:81,88`이 operation별 codec을 노출하지만 공통 계약 `32-stream-connector.ko.md:293-297`과 Java·TypeScript·.NET exact 계약은 connector 생성 option에 typed codec 하나를 주입한다 — C++도 connector-level typed codec option과 extension 등록으로 정렬하고 operation별 codec 선택 및 connector registry를 제거하거나, 필요하다면 먼저 공통 계약에 다섯 언어 공통 기능으로 설계한다.

[원칙][high] framework/languages/cpp/e2e/RuntimeMonitoring/feature-map.ko.md:34 — canonical `MON-A1`~`MON-D1` 표 뒤에 과거 `이전 A1`~`이전 D1` catalog를 별도 현재 시나리오처럼 유지해 Config 7의 정식 scenario와 구현 증거를 일대일로 대응할 수 없다 — 공통 Config 7은 `config-7-monitoring.ko.md:57-181`의 `MON-*`만 정의한다. 같은 과거 catalog가 .NET `:28-36`, Java `:38-61`, Kotlin `:29-54`, Node.js `:26-53`에도 남아 있고 서로 다른 구형 socket/location/Spot 검증을 기술한다 — 각 구형 증거를 대응하는 `MON-*` 행의 현재 상태·gap 근거로 재분류하고 과거 ID와 변경 이력 서술을 제거한다.

[원칙][medium] framework/languages/dotnet/e2e/RuntimeMonitoring/feature-map.ko.md:26 — 두 번째 표의 header와 delimiter가 canonical 표에서 분리되지 않아 렌더 결과에서 `시나리오` header와 구분선이 첫 표의 일반 행으로 합쳐진다 — 원칙 6은 실제 렌더 확인을 완료 조건으로 삼으며, 현재 파일은 `MON-D1` 행 바로 다음 줄에 새 표를 시작한다 — 표를 분리하되, 근본적으로는 구형 `이전 A1`~`이전 D1` 표를 canonical `MON-*` 행에 통합한다.

[원칙][medium] framework/doc/framework/common/sample/event/shoppingmall.ko.md:47 — 전문 기술문서 산문에 구어체가 반복되어 설명의 격식과 즉시성이 떨어진다 — `그냥 순차 코드`, `까먹는 적립금`(`:412`), `영영 고쳐지지 않는다`(`:482`), `저절로 된다`(`:491`), 다시 `그냥 순차 코드`(`:496`)는 문서 원칙 7.7이 금지하는 회의체·구어체 표현이다 — 각각 `별도 조율 계층 없이 순차 코드`, `여러 주문이 공유해 차감하는 적립금`, `계속 불일치 상태로 남는다`, `별도 분기 없이 이어진다`처럼 검증 가능한 표준 서술체로 고친다.
