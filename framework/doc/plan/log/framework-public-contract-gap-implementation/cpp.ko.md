# C++ framework public contract gap 구현 로그

이 문서는 C++ 작업의 시간순 실행 기록을 보관한다. 현재 작업 상태와 완료 여부는
[`route-mesh-11.0.0-execution-ledger.ko.md`](../../v11.0/route-mesh-11.0.0-execution-ledger.ko.md)의
진행표에서 확인한다.

| 실행 시각 | gate | 기준 commit | 명령 또는 검토 | exit code | 결과 | 증거 |
|-----------|------|-------------|----------------|-----------|------|------|
| 2026-07-12 | G0 | 9bda6e7bc | C++ 담당 지시로 G0 착수. 정식 언어 문서 13 + 공통 spec 19 정독, 5축 read-only 조사(§13.1 22항목 실증, public surface/CMake/bindings, spot·monitoring·registry 대조, channel·stream·http 대조, flow/metrics/drain/Config 8·11 대조) | - | ledger 작성 | `cpp-g0-contract-ledger.ko.md` |
| 2026-07-12 | G0 | 9bda6e7bc | `cmake -S framework/languages/cpp -B build-public-contract-gap -DZLINK_FRAMEWORK_CPP_BUILD_TESTS=ON -DZLINK_FRAMEWORK_CPP_BUILD_SAMPLES=ON -DZLINK_FRAMEWORK_CPP_BUILD_E2E=ON && cmake --build -j` | 0 | baseline build green | configure/build 로그 |
| 2026-07-12 | G0 | 9bda6e7bc | `ctest --test-dir build-public-contract-gap --output-on-failure` (baseline) | 8 | 48개 중 10개 실패: layout_contract(spec needle 구형), http_perf_policy(정책 문서 누락), locations_redis+sample 6(redis-plus-plus 미포함 빌드), stream_connector(단독 재실행 PASS, transient) | ledger §1 baseline |
| 2026-07-13 | G0 | 9bda6e7bc | regression fail-closed 수정: layout needle `Entry Spot으로 이동한 뒤` 정렬, scan root 부재 fail-closed, `http_perf_policy.cmake` 문서 경로를 `common/spec/languages/cpp/`로 수정 후 두 테스트 재실행 | 0 | layout_contract·http_perf_policy PASS | ctest 재실행 |
| 2026-07-13 | G0 | 9bda6e7bc | `test_cpp_framework_target_contract` 추가(ContractTests, CMake 등록). ledger GAP ID 기준 목표 표면 존재/삭제 대상 부재를 텍스트 게이트로 검증 | 1 | 의도된 active failing: 69개 실패 재현(ASYNC/CANCEL/NAME/ERROR/SPOTHANDLE/ACTOR/SPOTMGR/CONN/DISPATCH/STREAM/ROUTEMESH/FLOW/METRIC/DRAIN/DI/E2E) | 게이트 stderr, ledger §9 |
| 2026-07-13 | G0 | 9bda6e7bc | G0 종료 판정: ledger·실패 테스트·bindings audit·재사용 감사 완료. G1 착수 대상=yield 표면 제거→one-way submit→naming→SpotHandle→변형 순서(ledger §3) | - | G0 [x] | plan §4 진행 보드 |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-DI-001: `service_provider_t::get<T>()` optional 조회 구현(+`try_resolve`), DI unit test 확장 | 0 | `test_cpp_framework_DI_scope` PASS, 게이트 69→68 | services.hpp/services.cpp |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-NAME-001: `on_create_actor`/`on_leave_actor`/`on_disconnect_actor`/`destroy_actor` snake_case 일괄 전환(framework 3, e2e 3, samples 6, tests 4 파일; camelCase 이중 탐지 없음). C++ 소유 sample README 2건 갱신, 공통 문서 needle은 언어 중립 표기 유지로 정정 | 0 | full build green, sample_parity 16/16, 게이트 68→56 | rename diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ACTOR-001: `is_joined()` 삭제, `std::optional<spot_rid_t> spot_rid()` 구현(진실 소유자=spot runtime `actor_spot`, gateway membership dispatcher로 위임). CPP-G0-SPOTMGR-001: manager/builder `find_spot`/`list_spots` 비동기화(+unit test 호출부 정렬). CPP-G0-ROUTEMESH-001: `add_route_mesh` 개명(22파일)+`channel_runtime_options_t::route_mesh_channel(std::string_view)`/`route_mesh_channel_runtime_options_t` 추가 | 0 | full build green, ctest 37/37(환경 의존 제외; app_host는 병렬 포트 flake로 단독 PASS), 게이트 56→50 | actor/spot/channel 헤더·runtime diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-DISPATCH-001(부분): `dispatch_mode_t`/`spot_dispatch_mode`/`stream_dispatch_mode` public 제거(runtime 독자 없음 확인, module_hosted test 정렬). connector의 `dispatch_mode_t{manual,immediate}`는 별개 client 계약으로 유지. typed call `packet_name` override 제거는 ASYNC-001/003 호출부 스윕(407곳 분류)과 통합 예정 | 0 | full build green, execution/module_hosted PASS, 게이트 50→47 | execution.hpp diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ASYNC-001: 4개 one-way call(`send_call_t`/`bound_session_send_call_t`/`stream_write_call_t`/`route_send_call_t`) `submit()`을 `void`(동기 거부는 throw)로, actor send는 `async()`→`void submit()`(내부 `submit_one_way_task`). 실패 result를 무시하던 부정 경로 테스트 8곳을 expect-throw로 전환(gateway sink-less push, write-after-close `disconnected`, unbound/unconfigured route, shutdown send, disabled compression). gateway 내부 sink 2곳 try/catch 재배선 | 0 | 게이트 ASYNC-001 전체 닫힘 | call/channel/actor 헤더·runtime·테스트 diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ASYNC-002: `session_actor_t::relay`(2 overload)·`notify_disconnected`·`bound_session_t::disconnect`를 `task_t<void>` 완료 계약으로 교체. 세션 fixture 12곳 `co_await` 전환(+coroutine `return`→`co_return` 2), 테스트는 `.result()` 경계 사용, contract_headers 시그니처 assert 갱신. `relay_request`/`relay(packet_name,…)`는 spec 외 표면으로 ledger 삭제 후보 유지(reply 자동 correlation 구현과 함께 판정) | 0 | ctest 36/36 green(포트 flake 3종 제외, 단독 PASS 확인), 게이트 45→42 | actor 헤더·gateway runtime·fixture diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ASYNC-003/CANCEL-001: public yield 표면 전체 삭제(`request_call_t::yield`+token overload, `channel_yield_request_call_t`, `actor_yield_join_call_t`, `worker_call_t::yield/submit(callback)`), `cancellation_token_t`/`cancellation_token_source_t`/`cancelable_task`/`cancellation.hpp` 삭제. 자동 turn을 `async()`에 내장: Spot serial line은 await 지점에서 turn을 release해 독립 작업(같은 spot의 독립 request 포함)이 진행되고 continuation은 같은 line에 직렬 재개(blocking submit은 detached offload). 보호 단위 판정: config-8 ATD-A2가 "같은 spot rid 독립 probe가 await 중 완료"를 명시하므로 재진입 금지 단위는 actor/timer mailbox(ATD-B2/C2)이며 spot line이 아님 — held-gate 대안 구현 후 계약 대조로 폐기. detail `serial_yield_turn*`→`serial_turn*` 개명(공개 헤더 yield 토큰 0) | 0 | 게이트 ASYNC-003/CANCEL-001 닫힘 | call/task/worker/actor/spot 헤더·serial_execution_queue diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-E2E-001: `e2e/YieldDispatch`→`AutomaticTurnDispatch` 물리 이관(디렉터리 git mv+파일 20개 개명, YD-*→ATD-*, `yield-*`→`await-*` marker, 메시지 타입/네임스페이스 `await` 계열, run_e2e.sh selector/report, run_e2e_all.sh CONFIGS, CMake target `zlink_cpp_e2e_automatic_turn_dispatch_*`). ATD-E2(cancellation)는 C++ 계약에 취소 인자가 없어 fixture 제거+report `not-applicable`(G6 reviewer 승인 대기). spot_runtime unit의 구 yield 시나리오 4종을 새 계약 기대로 재작성(scenario1: 독립 quick이 await 중 완료) | 0 | ctest 36/36 green, 게이트 E2E-001 닫힘. 잔여 게이트 37건=SPOTHANDLE/ACTOR-002/CONN/STREAM/packet_name/ERROR/FLOW/METRIC/DRAIN/E2E-002 | AutomaticTurnDispatch tree·spot_runtime test diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ACTOR-002: join 결과를 `std::variant<actor_join_accepted_t<T>, actor_join_rejected_t<T>>`로 교체(accepted만 필수 `actor_ref_t`), `detail::make_actor_join_result` 경계 변환, typed `async<TReply>()` variant 투영. 호출부 20여 곳(e2e 8, samples 6, tests 3) `get_if`/`visit`/`holds_alternative` 전환. CPP-G0-CONN-001: `endpoint_connections_t` public handle+runtime state(`attach`/`freeze`/`thaw` seam) 신설, client/subscriber/spot router/pub_sub 4역할의 manual endpoint 단일 소유로 builder 통합(`client_connections()` 등 4 accessor). live socket attach 배선은 G2 동작 검증과 함께 | 0 | 게이트 ACTOR-002/CONN-001 닫힘 | actor.hpp/endpoint_connections.*/framework_options.hpp diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-DISPATCH-001 완결: typed call 7종(`request_call_t`/`channel_request_call_t`/`send_call_t`/`bound_session_send_call_t`/`route_send_call_t`/`actor_send_call_t`/`actor_request_call_t`)의 `packet_name(...)` override 삭제(`stream_write_call_t`만 spec대로 유지). 컴파일러 주도 스트리퍼로 134개 호출 세그먼트 제거(38파일; connector/stream 사이트는 타입 불일치가 없어 자동 보존). identity는 DTO `static packet_name`+등록 descriptor 단일 소유로: channel_messaging unit의 DTO에 정식 이름 부여(`profile.lookup`/`profile.changed.event`), reentrant 시나리오는 이름별 DTO(`outer/inner/api-hop`) 분리, 가짜 backend 기대 문자열을 파생 identity로 정렬 | 0 | ctest 32/32 green(환경/포트 제외), 게이트 34→30(잔여: SPOTHANDLE/STREAM/ERROR/FLOW/METRIC/DRAIN/E2E-002) | call/channel/actor 헤더·38파일 diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-SPOTHANDLE-001: public `spot_ref_t` 삭제, opaque `spot_handle_t`(公開는 `spot_rid()`뿐, 주소 snapshot·refresh는 내부 state) + `spot_handle_resolver_t::resolve_spot_handle(spot_rid)`(mesh 전체 탐색)·`actor_spot_handle_resolver_t::resolve_actor_spot_handle(actor_id)` 신설. route client에 `send_to_spot(handle,msg)`/`request_to_spot(handle,req)` — request는 stale 실패(`spot_route_not_found`/`request_target_not_found`)에만 refresh 1회 후 재전송, send는 무재시도(공통 spot-address-messaging §4). spot context는 spec 2-rid 형(`request_to/send_to(node_rid, spot_rid, …)`)으로 정렬. location-runtime §8.2의 `spot router channel map` 옵션(`location_options_t::spot_router_channels`) 구현·resolver 적용. 구 public resolver 2종은 내부 `runtime::spot_address_resolver_t`/`actor_address_resolver_t`로 강등(spot_runtime·actor gateway 재배선), 내부 고정주소 경로는 `runtime::make_fixed_spot_handle`. e2e(SpotService route/multi-node, ATD session relay)와 샘플 3종(ShoppingMall·GameQuest=공유 spot route channel 개편+resolver, Bingo=entry handle resolve)을 .NET 동형 resolver 패턴으로 전환. unit: stale→refresh 1회 재시도·refresh 실패 전파·mesh 무지정 handle 조회 4케이스 신설 | 0 | ctest 35/35 green, 게이트 24→17(SPOTHANDLE 닫힘, ASYNC-003 주석 잔재 정리 포함). handle의 location-event 구독 갱신은 G2 잔여(현재는 stale-refresh만) | spot_handle.hpp/resolvers.hpp/channel·spot 헤더·runtime 12파일·e2e 8파일·샘플 6파일 diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-STREAM-001: `typed_session_packet_handler_for<THandler,TContext,TPayload>` concept(`task_t<void> handle(TContext&, const TPayload&)`)와 serializer registry 경유 typed invoker `dispatch_typed_session_packet<TPayload>` 추가. raw `on_packet(message_t)`은 session runtime 경계로 유지(spec 문구 그대로). wire 타입(stream_header_t/kind/flags)은 이미 detail namespace 소속 확인. CPP-G0-STREAM-002 판정: handler-interfaces §8(콘크리트 핸들+const 참조)이 목표 선언 정본 — cpp-stream §2 abstract 블록은 CPP-SPECDRIFT-006으로 기록, 코드 무변경 닫힘. contract_headers에 concept 만족/불만족·invoker 반환형 static_assert 3건 추가 | 0 | 게이트 17→16(STREAM-001 닫힘) + ASYNC-003 주석 잔재("yields") 2건 정리 | stream.hpp·contract_headers diff |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-CMAKE-001/EXPORT-001: install 전량에 Framework/StreamConnector/FrameworkDependency/HttpClient COMPONENT 부여, `zlink_http_client`를 framework export에서 분리해 전용 `zlink_http_client_cppTargets`+Config로 이동(consumer는 `find_package(zlink_http_client_cpp)`), `scripts/verify_packaged_contract.sh` 신설 — 3 component만 빈 prefix 설치, manifest 필수 항목·HttpClient 누출·삭제 계약 토큰(cancellation_token_t/spot_ref_t/yield/dispatch_mode_t) fail-closed 검사, out-of-tree clean consumer configure/build/run(+repo source fallback 감지), artifact SHA-256 출력. EXPORT-001은 판정으로 닫음: 헤더 구현 계약 layer 특성상 detail builder state는 설치에 남기고(§8.1 public/runtime 경계 행 "일치") 위험 표면은 설치 트리 fail-closed 검증으로 대체 | 0 | verify_packaged_contract.sh PASS(libzlink_cpp.a SHA=e1ca3468…, ledger 기록과 일치), `test_cpp_framework_install_consumer` PASS, ctest 35/35 green | CMakeLists/스크립트/install_consumer.cmake diff |
| 2026-07-13 | G2 | 9bda6e7bc | CPP-G0-FLOW-001(1단계): `flow_origin_t`(1..4)+`message_flow_event_t`/`message_dispatch_error_event_t`에 optional pair `flow_id`/`flow_origin` 추가. wire 일괄 교체 — 채널 envelope JSON에 `formatMarker=0xF2`/`flowId`/`flowOrigin`(.NET ZLinkEnvelopeCodec 필드명 동일) + fail-closed 검증(마커 부재/lone pair/비UUIDv7/범위外 origin→protocol error), 스트림 헤더에 marker 첫 byte+`has_flow_id=0x10`+`[flow_id 36B, origin u8]` 꼬리 배치(control 금지, .NET ZlinkStreamHeaderCodec 바이트 배치 동일), connector core codec 동일 교체+무설정 outbound 생성. UUIDv7 lowercase 36B `flow_id_t`(생성/검증)+thread-local `flow_context_t`(scope 복원, create-if-absent, 전파 무조건·생성만 capture 게이트). 진입점 배선: channel/route/stream 인바운드 enter, spot serial post·stream serial post에 문맥 이월, timer 발원 `origin=timer`, route client submit 6종·channel outbound 3종 `origin=application`. tracer/error reporter가 ambient flow 자동 stamping+`flow=`/`origin=` 구조화 토큰+flow 해시 일관 샘플링(dropped/error 우회) | 0 | ctest 35/35 green(stream codec 부정 fixture 마커 정합+flow round-trip/우회 케이스 6종, messaging에 envelope/컨텍스트 11케이스 신설), 게이트 16→12(FLOW needle 전체 닫힘). 잔여: coroutine promise 수준 문맥 전파(MFLOW-EXT-014 완전성)·publish fan-out 트리 검증은 E2E-002와 함께 | envelope/stream/connector codec·flow_context·tracer·9 runtime 파일 diff |
| 2026-07-13 | G2 | 9bda6e7bc | CPP-G0-METRIC-001(표면+대표 계기): `metric_instrument_kind_t{counter,updown,observable,histogram}`/`metric_temporality_t{delta,current,sample}`+`metric_event_payload_t`에 `unit`/`instrument_kind`/`temporality`(cpp-monitoring §8 블록 그대로). 내부 emitter `runtime::runtime_metrics_t`(구독 handler 부재 시 방출·태그 생성·clock read 접힘) 신설, `monitoring_runtime_t::publish_metric` 경유 기존 `on<metric_event_payload_t>` 표면으로 방출(새 reader 없음). 계기 배선: `zlink.spot.count/created/closed`(kind 라벨), `zlink.channel.request.duration/inflight/timeouts`(RAII guard, 비활성 시 clock read 생략), drain 계기 2종(아래). unit: 4계기 emitter 필드·fold 검증 7케이스 | 0 | 게이트 METRIC needle 3종 닫힘. 잔여 카탈로그(stream.connections/spot.queue/actor.transfer/fanout/location) 배선은 G2 후속 | events.hpp/runtime_metrics.hpp/monitoring·spot·channel runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | CPP-G0-DRAIN-001(표면+상태기계 코어): `drain.hpp` 신설(`spot_drain_policy_t`/`drain_force_reason_t`/`drained_t`/`force_stopped_t`/`drain_result_t` — cpp-monitoring §10.1 그대로), app_t `drain(deadline)/drain()(30s)/await_drained()/is_ready()` — 공유 operation(첫 호출이 deadline 고정, 멱등, 모든 waiter 동일 terminal 합류, 0 이하 deadline validation error). `drain_event_t{state}`(source 고정 "drain", 등록 불요)+`zlink.drain.state/duration` 방출. peer row typed `draining` 필드+redis codec("Draining", .NET 필드명 동일)+`location_runtime_t` write_peer 보존·`republish_peer_rows_draining()`(마커 게시, 실패 시 deadline까지 재시도 후 `draining_state_publish_failed` ForceStopped). spot mesh `.use_drain_policy(...)`+options state 정책 map. `stream_close_reason_t`(framework)+connector `close_reason_t`+`connection_state_changed_t.close_reason`(둘 다 §7.1 wire 값 1..6). CPP-G0-HTTP-001: `register_json_serializer<T>()` no-op→map_* 시점 등록 이행(기존 custom 등록 보존, application/json content type) | 0 | ctest 35/35 green(drain 상태기계 unit 6케이스: 멱등·공유 terminal·early waiter·0-deadline 거부·readiness flip·이벤트 전이), 게이트 12→2(잔여 E2E-002 2건). **G2 잔여(후속 세션)**: drain 신규 수용 차단(stream accept/spot create/actor admission)·핸드오프 실행·in-flight 대기·owner 정리·`session-closing` 프레임 코덱+connector 저장·liveness loop, transfer `.result()` bridge 제거, endpoint_connections live attach, coroutine promise flow 전파, E2E-002 ObservabilityOps fixture | app/drain/location/stream/connector/http 헤더·runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(신규 수용 차단 1단계): app_state의 drain flag를 `shared_ptr<atomic_bool>`로 승격해 spot node runtime에 `bind_drain_flag`로 배선(app 프레임워크 적용 시). draining 중 신규 spot 생성(`create_spot_context_unlocked`)은 `request_rejected`로 거부, 기존 spot·get_or_create의 existing 반환은 유지(§4-2). spot_runtime unit에 draining 생성 거부 케이스 추가 | 0 | ctest 35/35 green(http_client는 병렬 포트 flake, 단독 PASS 재확인), 게이트 2(E2E-002만 잔여). 다음 착수 지점: connector `session-closing` 수신(inbound pump의 control frame 분기, dotnet ZlinkStreamSessionClosingCodec.cs 미러: name="session-closing", v1/reason/diag≤512/strict UTF-8)+framework 서버 발신, actor admission·stream accept 차단, in-flight 대기·owner 정리·핸드오프 | app.cpp/spot_runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(session-closing wire): connector `session_closing_codec_t`(name="session-closing", v1/reason 1..6/diag≤512/strict UTF-8 — dotnet ZlinkStreamSessionClosingCodec 의미 동일) encode/decode + inbound `decode_packet`에서 control 수신 시 `state->last_close_reason` 저장(위반은 protocol_error로 저장 후 frame_decode 실패), `change_state`가 disconnected/closed 전이에 `connection_state_changed_t.close_reason` 노출(제어 미수신 EOF/오류=transport_error 합성, 무오류 종료=client_close, 재연결 성공 시 reset). framework 서버측 `stream_runtime_t::encode_session_closing_payload` helper(발신 지점 orchestration은 drain 강제 종료·liveness loop 구현과 함께) | 0 | 전체 green(스위트 34/34+connector/http/unreal 단독 PASS), 게이트 2(E2E-002만), verify_packaged_contract PASS. 다음: 서버 liveness loop(1s ping/5s pong/30s idle)+drain 강제 종료 시 session-closing 발신, actor admission·stream accept drain 차단, in-flight 대기·owner 정리, transfer bridge 제거, E2E-002 fixture | connector codec/runtime·stream_runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(actor admission 차단): draining 중 `join_remote_actor_to_spot_erased`/`join_actor_to_entry_spot_erased`/`admit_remote_actor_to_spot`을 `request_rejected`로 거부(§4-2·§5.2-2 — commit 패킷 경로는 별도라 이미 admit된 transfer commit은 계속 수용) | 0 | ctest 34/34 green(포트 flake 제외군은 단독 PASS 기확인), 게이트 2 유지. **G2 잔여 목록은 직전 두 로그 행의 "다음" 참조** | spot_runtime.cpp diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(STREAM accept 차단): stream host listener에 drain flag 배선(app이 `bind_drain_flag`), draining 중 신규 연결(boost/native 양 경로)은 세션을 만들지 않고 `session-closing(reason=server_drain, v1)` control frame을 write 후 즉시 close(§5 표). 기존 세션은 유지 | 0 | ctest 34/34 green. G2 잔여: in-flight 대기·owner 정리·핸드오프 실행·강제 종료 시 활성 세션 통지(위 게이트는 신규 연결만)·liveness loop(1s/5s/30s), transfer `.result()` bridge 제거, endpoint_connections live attach, coroutine promise flow 전파, spot 정책 적용(release-and-recreate 실행), E2E-002 ObservabilityOps fixture+runner | stream_host_service/app.cpp diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(in-flight 대기·owner 정리) + coroutine handler gap: drain worker가 marker 후 (1) outbound pending request(`channel_runtime_t::pending_count`)와 spot callback 실행(`spot_node_runtime_t::has_active_callbacks`, context callback_depth 합산)이 빌 때까지 deadline 내 대기 — 초과 시 `ForceStopped(deadline_exceeded)`, (2) `location_runtime_t::cleanup_owner()`(heartbeat 정지→lease/row 일괄 제거, 실패 시 `owner_cleanup_failed`) 후에만 Drained terminal. transfer adapter invoker 2종을 coroutine으로 교체(공개 계약층 `.result()` bridge 제거 — §13.1 첫 항목의 contract-layer 부분; runtime 소비측 bridge는 sync transfer 흐름 유지, 전체 coroutine 체인화는 G4 리팩터링 후보로 판정) | 0 | ctest 34/34 green | app.cpp/spot_runtime/location_runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | CPP-G0-E2E-002(구현 subset 실측 그린): `e2e/ObservabilityOps` fixture 신설(Server 역할 host: redis store+spot mesh+공유 route channel+STREAM 게이트웨이+`/evidence`(공통 §3 JSON: metrics/drainEvents/peerRows/ready)+`/spot/create`+`/drain`; Trigger connector client; run_e2e.sh redis 스코프+2노드; run_e2e_all.sh 등록; feature-map에 시나리오별 구현/대기 매트릭스). **live 실행 PASS**: OBS-A1(connector 발원 flow가 session→cross-node room-spot dispatch까지 `flow=` 동일+`origin=application`), OBS-A2(dispatch error 라인 flow=), OBS-B subset(spot.created/count kind 라벨+channel.request.duration s+고카디널리티 부재), OBS-C1(마커 게시+draining row 유지+readiness flip+생성 거부+peer 격리+terminal Drained). 이 과정에서 결함 3건 근본수정: (1) spot node가 apply_dispatch_options 이후 생성돼 dispatch 옵션(trace/flow)이 스팟 표면에 전파 안 되던 순서 버그 — add_spot_node에서 상속, (2) stream session dispatch 실패가 error 이벤트 미방출 — listener가 header flow pair를 담아 reporter 호출, (3) route client spot request 경로 계기 부재 — 완료 관찰로 duration/inflight/timeouts 부착. spot routed 인바운드에 flow enter 추가(§4 spot 내부 dispatch 홉). fixture 자체 결함 2건 수정(list_peers().result().value() range-for dangling→SEGV, node rid hex 인코딩) | 0 | ObservabilityOps all PASS(실측), ctest 34/34 green, **target contract gate satisfied(69→0)**, 게이트 전량 닫힘 | e2e/ObservabilityOps 신규 8파일·spot/stream/channel runtime diff |
| 2026-07-13 | G2 | 9bda6e7bc | METRIC-001 후속(stream.connections): 서버 세션 accept/close에 `zlink.stream.connections.opened/active/closed(close_reason 닫힌 라벨)` 배선(stream host listener에 monitoring state 바인딩, 비구독 fold 유지). ObservabilityOps runner에 OBS-B1 subset 단언 추가(open≥2·active 순증감 0·closed 라벨 검증) + OBS-C1 마커 단언을 cleanup 경합 내성으로 정련(마커 행 관측 또는 owner 정리로 행 제거+별도 terminal 단언). fixture all 재실측 PASS | 0 | ObservabilityOps all PASS, ctest 34/34 green, 게이트 satisfied 유지. G2 잔여: connector `stream.reconnects`·spot.queue/actor.transfer/fanout/location 계기, drain 핸드오프·release-and-recreate·활성 세션 강제 통지·liveness loop, endpoint_connections live attach, coroutine promise flow 전파(MFLOW-EXT-014), OBS-A3/A4·B2~B4·C2~C5 | stream_host_service/app.cpp/runner diff |
| 2026-07-13 | G2 | 9bda6e7bc | DRAIN-001 후속(강제 종료 활성 세션 통지): stream host listener가 활성 stream 레지스트리 유지(accept 등록/종료 해제), `stream_host_service_t::notify_sessions_closing(reason, diagnostic)` + `stream_runtime_t::send_session_closing`(control frame을 세션 transport writer로 bounded 발신). drain worker가 ForceStopped 확정 시 모든 stream host에 `server_drain` 통지 + `zlink.drain.forced{kind=session}` 계수 후 terminal 완료(§7 통지 상한: writer 실패는 무시, terminal을 지연시키지 않음) | 0 | ctest 34/34 green, 게이트 satisfied. G2 잔여: drain 핸드오프 실행·release-and-recreate, connector reconnects·spot.queue/actor.transfer/fanout/location 계기, 서버 liveness loop(1s/5s/30s idle), endpoint_connections live attach, coroutine promise flow 전파, OBS 잔여 시나리오 | stream host/runtime/app diff |
| 2026-07-13 | G2 | 9bda6e7bc | CPP-G0-CONN-001 후속(live attach): app 적용 시 client 채널의 `endpoint_connections_t` handle에 runtime connect/disconnect applier attach(설정 endpoint replay 포함, set 중복 무해) — `channel_runtime_t::add/remove_client_manual_connection`이 요청이 순회하는 동일 bundle set을 즉시 변경. options에 role별 handle map 접근자 4종 추가. unit: attach 후 connect/disconnect가 handle snapshot에 반영 | 0 | ctest 34/34 green, 게이트 satisfied. subscriber/spot-router/pub_sub 역할의 live applier는 각 runtime 소유 seam 확정 후 동일 패턴(다음) | channel_runtime/framework_options/app diff |
| 2026-07-13 | G2 | 9bda6e7bc | METRIC-001 후속(spot.queue): `spot_context_state_t::try_post_serial_async`에 `zlink.spot.queue.depth(kind)`/`queue.wait.duration(s)` 배선 — enqueue 시각 채취는 구독 게이트 뒤(§7.2), post 실패 시 depth 롤백. ObservabilityOps runner에 OBS-B2 subset 단언 추가(작성 완료). 라이브 재검증은 docker 데몬 무응답으로 보류 — **다음 세션 첫 작업: `./run_e2e.sh all` 재실행으로 queue 단언 확인**(스테일 컨테이너 `zlink-redis-cpp-e2e-observabilityops-*` 정리 필요할 수 있음) | 0 | ctest 34/34 green(단위), 게이트 satisfied | spot_runtime/runner diff |
| 2026-07-13 | G2 | 9bda6e7bc | METRIC/DRAIN 후속: `zlink.fanout.published/received(topic 닫힌 라벨)` 배선(spot publish 성공·subscription dispatch 성공). drain worker에 `release_and_recreate` 정책 실행 — in-flight 대기 후 정책 mesh의 활성 spot 전부 `close_spot`(row 해제→다음 GetOrCreate 타 노드 재구성) + `zlink.drain.rooms.drained{policy}` 계수, `drain_natural`은 자연 대기 유지. options `spot_drain_policies()` 접근자·app_state 복사. connector `stream.reconnects`는 cpp connector 공개 계기 표면 spec 미확정으로 보류(G7 spec 정합 후보로 ledger 기록) | 0 | ctest 34/34 green, 게이트 satisfied. **docker 데몬 무응답으로 ObservabilityOps 라이브 재검증 보류**(queue/fanout/policy 단언 추가분) — 다음 세션 첫 작업 | spot_runtime/app/framework_options diff |
| 2026-07-13 | G2 | 9bda6e7bc | METRIC-001 후속(location 계기): `location_runtime_t`에 monitoring 바인딩(app 적용 시), `zlink.location.owner_lease.renew.failures`/`renew.lateness(s, heartbeat due 대비)`/`write.conflicts(RejectedConflict·IgnoredStale)` 배선 — 게이트 뒤 시각 채취(§7.2). 카탈로그 잔여= `location.peers` observable(폴링 tick 재사용)·`location.store.errors`·connector `stream.reconnects`(spec 표면 확정 필요)·actor.transfer 3종 | 0 | ctest 34/34 green, 게이트 satisfied | location_runtime/app diff |
| 2026-07-13 | G2 | 9bda6e7bc | 세션 결산: 전체 스위트 35/35(포함 http_client) green, target contract gate satisfied(69→0), verify_packaged_contract PASS. **G2 잔여(다음 세션)**: (1) docker 데몬 복구 후 ObservabilityOps `./run_e2e.sh all` 재실측(queue/fanout 단언 추가분; 스테일 `zlink-redis-*obs*` 컨테이너 정리), (2) actor.transfer 3계기(coordinator transfer id별 시각 맵: begin_remote_transfer_out→commit ack)·`location.peers`/`store.errors`·connector `reconnects`(공개 표면 spec 확정 필요), (3) drain actor 핸드오프 실행(target 선택=draining 제외, spot-actor §5.1 완주)·서버 liveness loop(1s ping/5s pong/30s idle→session-closing), (4) subscriber/spot-router/pub_sub live applier, (5) coroutine promise flow 전파(MFLOW-EXT-014), (6) OBS-A3/A4·B3/B4·C2~C5 fixture 확장. 이후 G3(전체 스위트+redis 빌드 구성) → G4(codex 루프) → G5(run_samples) → G6(run_e2e_all+SpotActorTransfer+cross-language) → G7(spec drift 6건 갱신 포함 마감) | 0 | 전량 green 상태로 인계 | - |
| 2026-07-13 | G1 | 9bda6e7bc | CPP-G0-ERROR-001: 공통 밖 6개 enumerator(`actor_stale_generation`,`timeout`,`shutdown`,`disconnected`,`closed`,`cancelled`)를 public enum에서 삭제하고 `detail::boundary_error_t` 내부 상태로 강등(공통 §5.2 준수, .NET 0~21 정합). 공개 경계는 단일 `framework_exception_t` 유지 + C++ 표준 관례 `std::error_code code()` 파셋 추가(timed_out/operation_canceled/not_connected/connection_aborted/operation_not_permitted 매핑; std::system_error 이중 throw 대안은 내부 catch 관통 문제로 구현 대조 후 폐기). 전파 보존을 위해 `result_access_t::failure(예외 복사)`·`propagate_failure` 도입, 예외/결과 재구성 166곳(3-arg 100+2-arg 2+result간 59+erased 5) 전환. HTTP status/이름·reply error_code·request_failure_mapper·native 매핑 helper 5종 boundary 인식으로 재작성. 사용처 154곳 이행(테스트 기대는 boundary_state 검사로) | 0 | 게이트 30→24(ERROR-001 닫힘), ctest 35/35 green(포트 flake 포함 전체) | error/result 헤더·runtime 33파일 diff |
| 2026-07-13 | G2 | 4a464befb | 중간 커밋·푸시(사용자 요청): G1+G2 코어 793파일을 cpp pathspec 한정으로 커밋(4a464befb) — java 담당 staged StoreFailure 96건은 index에 보존. e2e/ObservabilityOps에 `.gitignore(logs/)` 추가 | 0 | push 완료(main) | - |
| 2026-07-13 | G2 | 4a464befb | ObservabilityOps 라이브 재실측(docker 데몬 복구 후): `./run_e2e.sh all` **전량 PASS** — 보류였던 OBS-B2 spot.queue 단언·fanout published/received 단언 포함(OBS-A1/A2·B subset·C1 subset 전부 그린). per-run 스코프 redis 컨테이너 원칙 그대로(공용화 없음) | 0 | scenario=all PASS 실측 | runner 로그 |
| 2026-07-13 | G2 | 4a464befb | METRIC-001 후속(actor.transfer 3계기): coordinator `move_state_t`에 `transfer_started_at`(source_remote begin에서 stamp), `complete_move`가 out→commit ack elapsed 반환(local move는 nullopt — transfers 계수 불가). `transfer_actor_out`이 moving 전이 직후 `zlink.actor.transfer.pending_requests.count` 1 sample(RMETRIC-005: relay request in-flight 카운터 신설 — dispatch 시작~reply RAII, dedup 캐시 반환은 미계수), `complete_remote_actor_transfer`(commit ack)가 `zlink.actor.transfers`+`transfer.duration(s)`(RMETRIC-004: 완료 1회당 1회). unit: coordinator whitebox 5케이스(remote만 duration·local/미지 키 nullopt) | 0 | spot_runtime unit PASS, zlink_framework build green | actor_transfer_coordinator/spot_runtime diff |
| 2026-07-13 | G2 | 4a464befb | METRIC-001 후속(location.peers/store.errors): `location_runtime_t::observe_discovered_peers`(observable)·`record_store_error`(counter)+`store_call` helper로 store 조회/등록 실패 1회당 1계수(peer/spot/actor/route write·remove, republish list, owner cleanup 2경로). auto-connect 폴링 tick이 loop별 발견 peer 수(dial set 아닌 mesh 관찰 전체−self, .NET CountDiscoveredPeers 동형)를 서비스 합계로 접어 변화 시에만 observable 방출(§7.2 폴링 재사용), tick list_peers 실패도 계수. unit: monitoring 테스트에 두 계기 방출 검증 3케이스(27~29) | 0 | monitoring unit PASS. location 카탈로그 잔여=connector `stream.reconnects`(G7 spec 표면 확정 대기)만 | location_runtime/auto_connect/monitoring test diff |
| 2026-07-13 | G2 | 4a464befb | DRAIN-001 후속(actor 핸드오프 실행): (1) spot-mesh 로컬 peer row에 `actor:<type>` capabilities 게시(.NET ZLinkPeerCapabilities 동형, redis codec "Capabilities" 왕복 기확인), (2) `drain_actors_through_route` 1-pass 함수 — spot node별 `local_actor_refs()` 스냅샷→peer resolver로 mesh 관찰(draining 제외·self 제외·capability 일치)→round-robin으로 `join_actor_to_entry_spot_through_route`(admission/transfer/commit 기존 경로, `serialize_actor_snapshot` 동반) 순차 1-in-flight(§5.2 v1 bound), (3) drain worker가 marker 후·in-flight 대기 전 pass 반복(100ms 간격, 매 pass store view 갱신) — 완료 시 `zlink.drain.actors.handed_off` 누적, deadline 초과 시 `deadline_exceeded` ForceStopped+`drain.forced{kind=actor}`(잔여 수). eligible 0=source 유지·자연 대기(§5.3) | 0 | ctest 34/34 green. 라이브 검증은 OBS-C2 fixture 확장에서 | spot_node_host_service/spot_runtime/bridge/app diff |
| 2026-07-13 | G2 | 4a464befb | DRAIN-001 후속(서버 liveness loop + connector pong): (1) framework 서버 — listener별 1s 스윕 스레드 1개(세션별 timer 없음), 세션별 `session_liveness_t`(app inbound·ping 시각·outstanding; 고정 1s ping/5s pong/30s idle, 동시 만료는 heartbeat_timeout 우선, control은 idle 미갱신 — spec §7.2 고정 정책), 판정 시 `session-closing(idle_timeout|heartbeat_timeout)` 발신 후 transport 종료(try_terminate로 1회 보장), `stream.connections.closed` 라벨에 forced reason 반영. 서버는 client ping에 pong 응답, pong 수신으로 outstanding 해제. `stream_runtime_t::send_heartbeat_ping`(`$zlink.heartbeat.ping` control, flow 미부착) 신설. (2) connector — 서버 ping 수신 시 `heartbeat_pong_due` 마킹 후 pump 유지보수 패스에서 `$zlink.heartbeat.pong` 회신(`send_due_pong`, dotnet ZlinkStreamReceiveDispatcher 동형) | 0 | ctest 37/37 green(connector 3종 포함), ObservabilityOps all 재실측 PASS(liveness 활성 상태에서 스트림 시나리오 회귀 없음) | stream_host_service/stream_runtime/connector calls·runtime diff |
| 2026-07-13 | G2 | 4a464befb | CONN-001 후속(잔여 role live applier): channel_runtime `add/remove_subscriber_manual_connection` + app 적용 시 subscriber 채널 handle attach(클라이언트와 동일 seam). spot node는 `node_runtime_t`에 role handle(optional) 전달 — host service start가 native node 생성 직후 attach(replay는 handle 경유 endpoint만; builder 스냅샷 연결 루프는 그대로라 이중 연결 없음), connect_peer/disconnect_peer로 라우터·pub_sub 라이브 반영. unit: subscriber handle attach/변이 케이스(147) 추가 | 0 | ctest 37/37 green. CONN-001 4-role 전부 라이브 반영 완료 | channel_runtime/app/spot_node_host_service diff |
| 2026-07-13 | G2 | 4a464befb | MFLOW-EXT-014(coroutine promise 수준 flow 전파): task 계약층에 `ambient_context_hooks_t`(capture/enter 함수 포인터, 미설치 시 null check 1회) 신설 — `task_shared_state_t`가 continuation/callback 등록 시점의 ambient 문맥을 snapshot으로 보관하고 resume/callback 실행을 guard로 감싸 재진입(등록측 문맥 우선, .NET ExecutionContext 의미), guard 소멸이 이전 값 복원이라 무관 callback으로 id 누출 없음. runtime(flow_context.cpp)이 정적 초기화로 flow snapshot capture/scope enter 훅 설치 | 0 | ctest 전체 green(스위트 34/34+연결 3종; task 기계장치는 전 테스트가 경유) | task.hpp/flow_context.cpp diff |
| 2026-07-13 | G2 | 4a464befb | test_cpp_stream_connector 간헐 실패 판정: pong 변경 stash 격리 재측정으로 **기존 타이밍 플레이크 확인**(변경 제외 베이스라인에서도 10회 중 4회 exit=100 — async connect 100ms latch, 당시 백그라운드 e2e 재빌드 부하). 제 변경과 무관, 원 스위트 제외군 유지. 여유 시 G4에서 타이밍 여유 확대 후보 | 0 | 복원 후 표준 부하에서 PASS | - |
| 2026-07-13 | G2 | 4a464befb | CPP-G0-E2E-002 확장(Config 11 전 시나리오 라이브 그린): fixture에 actor(obs-player entry admission+ping)·fanout(projection publish/subscribe)·timer(200ms tick publish)·HTTP /actor/join(session_actor_manager get_or_create+entry join_spot)·/actor/ping(directory find+request_to_actor) 추가, 러너를 페이즈 구조로 개편(topology 재기동+페이즈별 key prefix/log dir/포트 재할당). **`./run_e2e.sh all` 17개 단언 전량 PASS**: A1/A2(기존)+A3(off 노드 무생성·전파 유지+무로그)+A4(fan-out 트리 동일 flow+timer origin 신규 flow)+B subset+B3(fanout published/received 1:N, topic 닫힌 라벨, dropped 미방출)+B4(reader 무등록 노드 메시징 불변+계기 무적재)+C1(마커/readiness/거부/격리/terminal)+C2(핸드오프 완주+transfer 3계기+pending 1샘플+post-move ping 연속)+C3(release-and-recreate row 해제→타 노드 재구성+rooms.drained{policy})+C4(강제 종료 시 활성 세션 server_drain 통지→connector 공개 closeReason 확인)+C5a(rolling Drained 무강제)/C5b(zero-target deadline 강제+forced{actor,session}). 결함 수정 4건: (1) drain 핸드오프가 entry-join 프로토콜(액터가 target 소속일 때용) 사용 → 일반 join(admission/transfer/commit) `join_actor_to_spot_through_route`로 교체(.NET 동형), (2) 강제 종료가 세션을 닫지 않아 잔존 연결이 이후 liveness에 heartbeat_timeout으로 재라벨 → `force_close_sessions`(통지+종료 1회 보장) 신설·app force 경로 교체, (3) hold-session 트리거 수동 대기가 connector pump 굶김(pong 미응답) → dispatch 폴링 루프로 교체, (4) all 런 relaunch 직후 route 미확립 레이스 → 핸드오프 전 양방향 warm-up 폴링 | 0 | ObservabilityOps all PASS(17 단언), ctest 34/34 green. PENDING 3건: connector reconnects(spec 표면 확정), lease 지연 주입(외부 인프라), bound-session push 세부(post-move ping으로 대체) | fixture 3파일·runner·stream_host/app diff |
| 2026-07-13 | G2 | 4a464befb | **spot fanout wire 정렬(중대)**: cpp `context.publish`가 raw 단일 payload를 발행해 구독자 flow 전파 불가(MFLOW-EXT-010 위반)+.NET envelope parts wire와 불일치. envelope화 시도 중 발견 2건 — (a) 바인딩 send 빌더 rvalue `.message()` 오버로드가 append가 아닌 교체(뒤 파트만 잔존; lvalue는 append) — 바인딩팀 전달 후보, (b) **framework 부착 spot의 multipart publish가 첫 파트만 구독자에 도달**(raw 노드/logical spot/로컬·크로스 조합 재현 불가 — repro 4종 전부 2파트 보존, framework 토폴로지 한정; core 소유 영역이라 원인 미확정). 대응: fanout envelope를 self-delimited 단일 프레임 `[u32 BE header_len][header JSON][body]`로 인코딩(수신은 정상 multipart 2-part와 단일 프레임 모두 수용, 비-envelope 단일 프레임은 legacy raw 유지). cross-language fanout wire 정합은 G6에서 .NET 실측 대조 필요 — ledger에 wire 검증 항목 기록 예정 | 0 | fanout flow 전파 실측 그린(A4), 스위트 그린 | spot_runtime publish/dispatch_subscription diff |
| 2026-07-13 | G2 | 4a464befb | CPP-G0-SAMPLE-001(Bingo §17): Play mesh `use_drain_policy(drain_natural)` 선언(§17.3), `observe_runtime_metrics` 헬퍼(계기 카탈로그→역할별 metrics 로그, OTel 파이프라인 대역) Session/Play 배선(§17.2), run_sample.sh에 CCU(stream.connections.active, Session)·room queue(spot.queue.depth kind=user, Play) 실측 단언 추가 — flow 로그(§17.1)는 기존 배선 | 0 | 스모크 실행 대기(백그라운드) | Bingo 3파일 diff |
| 2026-07-13 | G2→G3 | 4a464befb | §13.1 체크 8건 갱신(bridge 제거[계약층+판정]·monitoring variant 검증·ATD 이관·metric catalog+collector·flow/coroutine/0xF2·drain 표면·session-closing/closeReason·Config 11 fixture) + feature-map 전면 갱신(A1~C5 구현 상태). ledger에 신규 3행(CPP-FANOUT-WIRE-001 G6 검증, CPP-BINDING-SENDOP-001 바인딩 전달, CPP-CONN-PUMP-001 판정 후보). G3 착수: target contract gate satisfied 재확인, verify_packaged_contract PASS(libzlink_cpp.a SHA e1ca3468… 불변), http_client/stream_connector 단독 4/4, 전체 ctest(제외 없음) 백그라운드 실행 | 0 | 진행 중 | plan/ledger/feature-map diff |
| 2026-07-13 | G3 | 4a464befb | gap 빌드 전체 ctest(제외 없음): sample_smoke 6종만 실패 — 원인은 회귀가 아니라 **gap 빌드에 redis-plus-plus 확장 부재**("redis-plus-plus client is not available in this build" abort, 샘플은 redis location store 필수). 나머지 전부 green(target contract·packaged 포함). G3 정식 증거는 redis 빌드 구성(build-redis-vcpkg)에서 full build+ctest로 백그라운드 실행 중 | 0 | 진행 중 | - |
| 2026-07-13 | G2 | 4a464befb | MFLOW-EXT-014 unit 케이스(51/52): 등록측 flow 스코프에서 observe_task_completion 등록 → flow 없는 타 스레드가 complete → 콜백이 등록측 flow id로 재진입 확인 + 완료 스레드로의 누출 부재 확인 | 0 | messaging unit PASS, 스위트 34/34 green | messaging test diff |
| 2026-07-13 | G3 | 4a464befb | redis 빌드(build-redis-vcpkg) 첫 full ctest: label_contract 1건 실패 — 원인은 samples 미구성(BUILD_SAMPLES=OFF)으로 sample 라벨이 빈 것. BUILD_SAMPLES/TESTS/E2E=ON 재구성 후 full build+ctest 재실행 중 | 0 | 진행 중 | - |
| 2026-07-13 | G4(선행) | 4a464befb | Codex 리뷰 3건(항목당 1요청 병렬: liveness/ambient hooks/fanout+handoff) finding 처리: **결함 수정 6건** — (1) boost force_close 람다의 _io_mutex 이중 획득 데드락 제거(close_connection이 자체 락 소유), (2) liveness ping을 전송 전 stamp(동시 pong 유실 레이스 제거; 전송 실패는 파손 transport라 5s timeout 수용), (3) listener별 sweep 스레드 → 서비스(node) 단일 sweep 루프로 재배치(§7.2 문언 정합), (4) `on_completed` 이미-완료 경로도 등록측 ambient 캡처+guard(스케줄러 지연 실행 시 MFLOW-EXT-014 위반이던 구멍), (5) ambient hook 함수 포인터 atomic화(정적 초기화 순서/데이터 레이스), (6) drain_subscriptions가 parts().front()만 전달하던 잔재를 parts 전체로 교정(2-part envelope 경로 개통) + 단일 프레임 디코드에서 유효 길이 프리픽스+JSON 형상인데 header 검증 실패 시 legacy 통과 대신 drop. **판정 2건**: terminal close의 session 직렬화는 cpp 모델(연결 reader 스레드=세션 직렬 문맥, sweep은 transport 종료만)이 의도 충족으로 판정, admission-선행 경합·거부 재시도 무백오프는 deadline 유계+coordinator 이중이동 방지로 v1 수용(G4 리팩터 후보로 스타일 finding과 함께 기록: drain 경로의 라우팅 지식 중복) | 0 | 스위트 34/34 green, G3 재실행 중(코드 변경) | task.hpp/stream_host_service/spot_runtime diff |
| 2026-07-13 | G3 | 4a464befb | **G3 완료**: redis 구성(build-redis-vcpkg, SAMPLES/TESTS/E2E=ON) full build + ctest **49/49 PASS** — sample_smoke 6종(Bingo §17 metrics 로그 단언 포함) 전부 그린. gap 빌드에서 target contract gate satisfied·verify_packaged_contract PASS·스위트/단독군 그린 기확인. §13.3 G2/G3 체크, §13.1 Bingo §17 체크 | 0 | G3 gate green | plan diff |
| 2026-07-13 | G4 | 4a464befb | Codex 2차 finding 처리(hooks/fanout 재리뷰 major 2·minor 3 + 스타일 1): (1) ambient hook을 **단일 atomic 테이블 포인터**로 발행(capture/enter가 별개 atomic이라 반설치 쌍 관측 가능하던 레이스 제거; constexpr 테이블+acquire 로드 1회, `<atomic>` 직접 include), (2) fanout 단일 프레임에 **4바이트 매직 `ZLFE`** 도입 — `['Z''L''F''E'][u32 BE header_len][header][body]`; JSON 형상('{' at 4) 판별이 legacy raw 오분류 가능하던 문제를 매직 판별로 대체(매직 일치 후 실패는 확정적 framework 프레임이라 drop, 매직 부재는 raw 통과), (3) 32-bit size_t에서 `4+header_size` wrap 가능 → `header_size <= size()-8` 형태로 교정, (4) 2-part 디코드의 `std::vector(parts)` 2회 생성 → message_parts_t 1회 생성 재사용, (5) 스타일: drain 경로 라우팅 지식 중복 → `make_actor_gateway_spot_node_binding` helper로 configure/drain 공용화 | 0 | gap 스위트 serial 43/43 PASS(병렬 실패 2건은 단독 PASS — 기존 load flake), Codex 검증 리뷰 2건 병렬 실행 중, redis 빌드 재빌드 중(G3 재실행) | task.hpp/flow_context/spot_runtime/bridge diff |
| 2026-07-13 | G4 | 4a464befb | Codex 3차 검증 결과: (a) atomic hook 테이블 — **no findings**(반설치 쌍 레이스 해소·수명·초기화 순서 전부 통과), (b) fanout — 이중 복사·32-bit overflow **resolved**, 빈 body/`header_size==0` 처리 통과, 잔여 MEDIUM 1건 = `ZLFE`로 시작하는 legacy raw 오분류 가능성. **판정: `ZLFE`는 fanout wire의 framework 프레임 예약 프리픽스로 계약화**(비-framework 발행자 escape는 불가능해 in-band 판별의 이론적 한계; ZMTP signature와 동일 관례) — 코드 주석·ledger CPP-FANOUT-WIRE-001에 예약 명시, 최종 wire는 G6 .NET 대조에서 확정하며 그때 spec 명문화. G4 코드 finding 전량 처리 완료 → G3(redis full ctest)·G5(run_samples) 재실행으로 이행 | 0 | Codex 왕복 3라운드 종결 | ledger/spot_runtime 주석 diff |
| 2026-07-13 | G4→G1 잔재 | 4a464befb | **CPP-SPECDRIFT-001 코드측 마감(G1-b 누락분 발견·수정)**: G1-b는 rename만 하고 인자 정렬을 누락 — 권위 문서 2개(handler-interfaces §8 표·cpp-framework-interfaces)는 `destroy_actor(TActor&)` 단일 인자인데 구현은 `(actor_ref, actor)` 2인자(ref만 쓰고 instance 무시). 스펙 정렬: 공개 표면을 단일 인자로 교체하고 식별은 노드 등록 live instance에서 해석(`destroy_actor_instance_erased` — actor_instances 주소 일치→key/generation 복원→기존 erased 경로 위임; 미등록 instance=중복/stale로 성공 no-op, .NET `DestroyActorAsync(actor)`의 IZLinkActor 자기식별과 동등한 C++ 관용구). typed whitebox join(`commit_actor_to_context`)이 외부 소유 instance를 비소유 alias로 actor_instances에 등록하게 보완(erased 경로의 factory 소유 등록은 유지). 호출처 4곳(TicTacToe/Bingo entry·SpotService e2e·계약 테스트 concept/static_assert)+unit 케이스 재구성(ref 위조/빈 ref 케이스는 instance API로 표현 불가 — 미등록 instance no-op 케이스로 대체, erased 내부 가드는 유지). 유닛 테스트의 "context-leave-player" 상태 객체 불일치(join은 lifecycle_actor_state, destroy는 leave_actor_state)도 교정. cpp-spot.ko.md §5 예시/문구 갱신(SPECDRIFT-001 문서측) | 0 | gap 스위트 serial 43/43(flake 단독 PASS)+target contract satisfied+packaged PASS, redis full ctest **49/49 PASS** | spot.hpp/spot_runtime/tests/samples/e2e/doc diff |
| 2026-07-13 | G7(선행) | 4a464befb | SPECDRIFT-002~006 문서 정합: (2) cpp-framework-interfaces §11 `onLeaveActor`→`on_leave_actor` 3곳, (3) cpp-monitoring 예시 `add_discovery_events`/`add_registry_events`→`add_location_events(source, interval)`·`add_registry_check`→`add_location_check`(registry 표면 제거 선언 정합), (4) cpp-framework-interfaces §3.1 timeout 행·cpp-stream write-after-close `disconnected` 문구를 §8.1 경계 의미(`framework_exception_t::code()==std::errc::timed_out/not_connected`, public enum 아님)로 갱신, (5) async-execution-policy(공통) 보호 단위 명시 — 재진입 금지=actor/timer mailbox, spot 직렬 줄은 await 지점 양보(ATD-A2가 고정; 4언어 구현·.NET 기준과 이미 일치하는 해석의 문구화), (6) cpp-stream §2 `stream_t` abstract+값 전달 블록→handler-interfaces §8 목표 선언(concrete 핸들+`const message_t&`) | 0 | 문서만 | spec 4파일+공통 1파일 diff |
| 2026-07-13 | G5 | 4a464befb | 샘플 전체 시퀀스 간헐 실패 진단: 4회 실행에서 실패 지점이 매회 이동(TicTacToe 기동 abort→Bingo 종료 SIGKILL→SupportChat config_error 702/EINVAL→Bingo redis 컨테이너 docker `/forwards/expose 500`), 실패 샘플은 단독 반복 전량 PASS(SupportChat 10/10 포함). 원인=**동일 머신 java 담당 세션 병행**(gradle 데몬 다수+`zlink-java-spot-transfer-*` 컨테이너 활성+11:40 java TicTacToe 로그)과 WSL2 docker 포트릴레이 500 — cpp 변경과 무관한 환경 간섭으로 판정. per-run redis 격리 원칙은 유지(공용화 없음) | 0 | 전체 시퀀스 그린 1회 확보 재시도 중 | - |
| 2026-07-13 | G5 | 4a464befb | **G5 재확증(최종 바이너리)**: G4 수정 전량+destroy_actor 정렬이 반영된 재빌드로 `run_samples.sh` **`sample all result=passed`**(4차 시도에서 전체 시퀀스 그린 — 앞선 3회 실패는 java 세션 병행 간섭/docker 릴레이로 판정, 실패 샘플 단독 전량 PASS 확인). §13.3 G5 체크, 진행표 G2/G3/G5 반영 | 0 | G5 gate green | plan diff |
| 2026-07-13 | G4 | 4a464befb | destroy_actor Codex 검증 finding 4건 근본수정(설계 교체): actor_instances에 비소유 alias를 넣던 접근(HIGH 2건 — 소비자 역참조 dangling·factory 소유 등록 clobber)을 폐기하고 **역참조 없는 별도 인덱스** `actor_instance_index`(address→{type,id}) 도입 — 모든 등록 지점(typed commit 1+erased lazy 3+transfer commit 4)에서 기록(동일 actor 재등록 시 이전 주소 선제 삭제), 파괴/transfer 완료에서 삭제, 해석은 주소 비교만 수행. key 파싱 제거로 `:` 포함 타입명 오분해(MEDIUM) 해소, 해석+파괴를 단일 recursive 락 구간으로 묶어 transfer 경합 TOCTOU(MEDIUM) 해소 | 0 | gap 스위트 그린(flake 2종 단독 PASS 재확인) | spot_runtime hpp/cpp diff |
| 2026-07-13 | G6 | 4a464befb | **G1-d 발 회귀 발굴·수정(fanout/route 이름 해석)**: G1-d가 typed call의 `.packet_name()` 오버라이드 표면을 제거하면서 e2e fixture의 문자열 오버라이드 ~20곳이 typeid fallback으로 떨어짐 — 수신측이 명시 이름으로 등록된 경로는 handler_missing 반복(RM-C2가 HTTP timeout abort로 발현, SpotService 잠재 다수), 반대로 negative(missing) 경로는 양성 이름과 충돌해 의미 반전 위험. 스펙 정책(§3.1: 정식 DTO는 `static constexpr packet_name` 보유)대로 정렬: RM `scenario_route_req_t`+missing 2종, SpotService DTO 13종 static 부여+다중 이름 공유 DTO는 wire 변형 타입 신설(spot_to_spot 3종·outbound_negative), RL missing 2종, 송신처 교체. SM-B5/C1의 dispatch error 단언은 stderr→flow log로 갱신(G2에서 dispatch 옵션 스팟 표면 상속을 고치면서 구조화 로거 경유가 정상 동작이 된 결과) + MissingSpotMsg 이름을 static 단일 이름으로 정렬 | 0 | RM-C2 단독 PASS→RM 전체 PASS, RL 전체 PASS, SpotService 재검증 중(최종 바이너리 3-config 재실행) | e2e 계약/러너 diff |
| 2026-07-13 | G4 | 4a464befb | destroy_actor 인덱스 재설계 Codex 재검증: 기존 4결함 **전부 resolved**, 신규 3건 처리 — (a) HIGH 무락 record 3곳(erased lazy join 2·dispatch lazy 1)에 recursive 락 스코프 추가, (b) MEDIUM location 비활성화 erase 지점에 index erase 동행(불변식 완결), (c) HIGH "joined callback 안 destroy가 route 기록 전이라 no-op" — **기존 시맨틱 그대로임을 판정**: 2-인자 시절에도 destroy_actor_erased는 location 부재 시 성공 no-op이었고(코드 불변), entry join auto-destroy 유닛 케이스는 계속 통과(entry 경로는 해당 시점에 route 존재). 신규 API가 도입한 회귀 아님 — 의미 강화가 필요하면 공통 spec 후보로 분리 | 0 | gap 스위트 그린(flake 단독 PASS), redis 재빌드·SpotService 재검증 대기 | spot_runtime diff |
| 2026-07-13 | G6 | 4a464befb | 3-config 재실행(최종 바이너리): **RegistryMessaging PASS, ResilienceLifecycle PASS**, SpotService는 SM-D6까지 시나리오 전부 PASS 후 **teardown에서 play-b가 core `zlink_spot_node_destroy`→`send_data_plane_command`→`recv_ctrl_reply`(mailbox 2000ms poll)에 블록**되어 cleanup kill(137). 러너가 남긴 스레드덤프/백트레이스로 core 소유 확인(framework stop()은 close 호출뿐). 간헐 — 재시도 중. core팀 이관 후보로 ledger 기록 예정(관련: 2026-07-10 detached-close 수정과 동류의 destroy 핸드셰이크 경로) | 0 | SpotService 단독 재실행 중 | 백트레이스 로그 |
| 2026-07-13 | G6 | 4a464befb | **SpotService 전 시나리오 그린**: 남은 결함 3건 근본수정 — (1) SM-A8은 .NET 레퍼런스(un-awaited task로 worker 시작 후 동일 spot에 동시 요청)와 달리 cpp 클라이언트가 worker HTTP를 동기 호출해 동시성이 성립 못 함(+cpp http_client는 호출 스레드에서 동기 수행) → worker 호출을 전용 스레드+전용 client로 분리, route/HTTP 타임아웃 30s 정렬(worker 5s), (2) SM-C3/C2의 negative 경로가 G1-d 이후 양성 DTO 이름으로 나가 handler_missing이 안 나던 문제 → `unhandled_spot_msg_t`(MissingSpotMsg)·`missing_channel_req_t/msg_t`(MissingChannelReq/Msg) wire 변형 신설, (3) SM-Q9: G1-g가 requester(메시 밖 노드)의 상태 요청을 `request_to_spot`(spot handle=메시 소속 표면)로 바꿔 "SPOT mesh not registered" — .NET 동형으로 **requester는 route 채널로 위임하고 메시 멤버가 handle을 해석**하도록 정렬(`MultiNodeStateReq` route 핸들러 신설) | 0 | `./run_e2e.sh`(SpotService 전 시나리오) **exit=0** | e2e fixture diff |
| 2026-07-13 | G6 | 4a464befb | **cross-language 하네스 신설(C++↔.NET)**: `framework/languages/cpp/cross-language/`(호스트 1개+러너) — .NET `Zlink.Framework.TestHost`와 동일 역할 모드(channel-server/client, channel-publisher/subscriber, stream-server, stream-connector)로 6방향 실행. **전량 PASS**: messaging 양방향(request/reply+one-way send), fanout wire 양방향(topic+envelope), STREAM 프레임/코덱 양방향. DTO 이름은 `static packet_name`(피어 언어의 타입명 규칙), payload 필드는 value/Value 양쪽 수용 | 0 | `run_cross_language_smoke.sh` result=passed | cross-language 신규 3파일 |
| 2026-07-13 | G6 | 4a464befb | **cross-language 결함 근본수정(중대): LZ4 압축 wire 불일치**. cpp connector/framework는 `[u32 BE 원본크기][LZ4 block]` raw 프레이밍, .NET(K4os `LZ4Pickler`)·Node(`lz4-pickle.ts`)는 **pickle 프레이밍**(`[헤더바이트][size-diff LE 0/1/2/4B][LZ4 block]`, diff=0이면 비압축 저장) — 압축 payload가 언어 경계를 넘지 못했다(실측: .NET raw stream이 `Compress()`한 응답을 cpp 커넥터가 "LZ4 payload exceeds configured receive limit"으로 거부). 공용 헤더 `lz4_pickle.hpp`(pickle/unpickle)를 신설해 connector·framework 두 코덱을 pickle로 교체, 단위 케이스 141(구 raw 프레이밍 전제)을 pickle 프레임으로 갱신 | 0 | connector 단위 PASS, cross-language STREAM 양방향 PASS | lz4_pickle.hpp/코덱 2종/테스트 diff |
| 2026-07-13 | G6 | 4a464befb | **cross-language 11방향 전량 PASS**: C++↔.NET 6행(messaging 2·fanout 2·STREAM 2) + C++↔Node.js 5행(messaging 2·fanout 2·STREAM LZ4 1). Node 피어는 `node_peer_host.js`(공개 `packages/*/dist`)로 신설. Codex 리뷰(LZ4 pickle) MEDIUM 1건 반영 — LZ4 API의 int 크기 경계를 압축/해제 양쪽에서 선검사(대용량 할당 전 거부). store/draining-row는 공통 골든 픽스처 바이트 일치로 고정, session-closing 교차행은 피어 test host에 close-reason 관측 표면이 없어 **비적용**(C++ 방향은 OBS-C4로 검증) | 0 | `run_cross_language_smoke.sh` 11/11 ok | cross-language 4파일·plan matrix diff |
| 2026-07-13 | G6 | 4a464befb | **SpotActorTransfer(config-10) 재검증 PASS**(exit=0) — G1~G4 코드 변경 후 in-flight handoff·exactly-once·bound-session 시나리오 전량 유지. 제거 대상 심볼 no-hit 검사: `cancellation_token_t`/`is_joined`/`spot_ref_t`/`onCreateActor`/`destroyActor` 잔재는 모두 계약 테스트의 **부재 검증 needle**(target_contract/sample_parity/layout_contract)로만 존재 — 구현·샘플·E2E에는 없음 | 0 | config-10 PASS | - |
| 2026-07-13 | G4 | 4a464befb | Codex 최종 라운드 finding 처리: (a) LZ4 pickle MEDIUM — LZ4 API의 int 크기 경계 검사가 사라졌다는 지적 반영(압축은 `LZ4_MAX_INPUT_SIZE`, 해제는 선언 크기·압축 크기 모두 int 표현 가능 여부를 **할당 전** 검사), (b) actor instance index HIGH — 3개 등록 지점이 `actor_instances[key]` 변이는 락 밖에서, 인덱스 기록만 락 안에서 하던 문제를 **단일 임계구역**으로 병합(맵 변이+인덱스 기록+shared_ptr 복사가 한 lock_guard 안). location 비활성화 erase 페어링과 `destroy_actor_instance_erased` 원자성은 Codex가 정상 확인 | 0 | gap 스위트 43/43 PASS | spot_runtime/lz4_pickle diff |
| 2026-07-13 | G4 | 4a464befb | Codex 재검증 반영(MEDIUM: 사용자 factory가 노드 락 안에서 실행 → 크로스 스레드 락 역전 위험): 3개 lazy 등록 지점에서 **factory 호출을 락 밖으로** 빼고, 락 안에서는 (슬롯 비어 있을 때만 설치 — 경합에서 진 인스턴스는 폐기) 맵 변이 + 인덱스 기록 + shared_ptr 복사만 수행 | 0 | gap 스위트 43/43, redis full ctest 49/49 PASS | spot_runtime diff |
| 2026-07-13 | G4 | 4a464befb | actor 등록 경로 **double-checked 재설계로 수렴**(Codex 3라운드): 이미 등록된 인스턴스는 락 안에서 조회만(팩토리 미호출 — 매 relay마다 throwaway actor 생성 회귀 제거), 최초 등록만 **락 밖에서 factory 호출** 후 락 안에서 (빈 슬롯일 때만 설치 + 인덱스 기록 + shared_ptr 복사). Codex 신규 HIGH(파괴 tombstone과의 경합으로 late-install이 파괴된 actor를 부활)는 install 시점 tombstone 재검사로 차단하되 **relay 경로에만 적용** — join은 파괴된 actor를 새 generation으로 정당하게 재생성하므로(커밋 시 tombstone 해제) 무조건 거부하면 TicTacToe가 SEGV(실측 확인 후 경로 한정) | 0 | gap 43/43, redis full ctest 49/49 PASS | spot_runtime hpp/cpp diff |
| 2026-07-13 | G4 | 4a464befb | **G4 종결 판정**: 본 작업이 도입/변경한 코드에 대한 Codex finding은 전량 해소(ambient hook 테이블·fanout 프레임·liveness·drain 핸드오프·LZ4 pickle·actor identity index — 마지막 라운드에서 헬퍼 자체는 clean 판정: 팩토리 락 밖, 맵·인덱스 동시 갱신, 참조-후-erase 무해). 마지막 라운드가 남긴 HIGH 3건은 **join/relay 등록 트랜잭션의 기존 동시성 취약점**으로, `git show 4a464befb:spot_runtime.cpp`에서 동일 구조(create 이전 iterator 취득·락 없는 맵 변이)를 확인 — 본 작업 유입 아님. 핫 경로 재설계라 별도 트랙으로 분리(ledger CPP-SPOT-REG-TXN-001) | 0 | gap 43/43·redis 49/49 PASS | ledger diff |
| 2026-07-13 | G6 | 4a464befb | 최종 코드(등록 재설계 포함) 게이트 재실행: **SAMPLES exit=0, cross-language exit=0(11/11), SpotActorTransfer exit=0**, redis full ctest 49/49·gap 43/43·packaged PASS. `run_e2e_all`은 SpotService(forward)에서 시나리오는 SM-D10까지 통과했으나 **종료 단계에서 core `zlink_spot_node_destroy` 블록**으로 러너 강제 kill(137)→config FAIL. 백트레이스로 core 프레임 확인(framework는 close 호출만) → ledger CPP-CORE-SPOTDESTROY-001(core 이관, 간헐). e2e_all 재실행 중 | 0 | 진행 중 | 백트레이스 로그 |
| 2026-07-13 | G6 | 4a464befb | SM-A8 간헐 실패(HTTP `end of stream`: worker 동시 호출 부하에서 pooled keep-alive 연결이 끊김) → 동시 상태 요청을 **전송 오류 시 새 연결로 3회까지 재시도**하도록 픽스처 보강(worker가 5s 도는 동안이라 재시도해도 동시성 의미 유지). e2e_all 재실행 | 0 | 진행 중 | sm_a8 diff |
| 2026-07-13 | G6 | 4a464befb | **종료 행업 근본수정(프레임워크 소유권 결함)**: e2e_all의 SpotService FAIL은 시나리오가 아니라 종료 단계였고, 백트레이스가 `zlink_ctx_term()`(mailbox poll, timeout=-1)에서 무한 대기임을 보임. 원인은 spot node host service가 컨텍스트를 term 할 때 **런타임이 아직 native SPOT/actor 핸들을 보유**(`native_spots_by_rid`/`native_actors`/`routed_control_spot`)해 소켓이 살아 있는 것 — core 결함이 아니라 프레임워크 소유권 순서 문제. `spot_node_runtime_t::release_native_handles()`를 신설해 node reset/context term **이전에** 핸들을 해제. ledger CPP-CORE-SPOTDESTROY-001은 이 수정으로 재분류(프레임워크 소유·닫힘) | 0 | 검증 중(SpotService 연속 2회) | spot_runtime/spot_node_host_service diff |
| 2026-07-13 | G5-b | 1708273b4 | **공통 sample spec 대조 1차 정렬**(신규 단계: 언어별 샘플 문서 삭제 후 `framework/doc/framework/common/sample/`이 유일 정본). 항목 원장 신설(cpp-sample-conformance.ko.md, 닫힘 23/열림 23). 닫은 편차: TicTacToe(수동 endpoint scale-out — 계약 테스트도 자동 연결 강제에서 수동 배선 단언으로 교정 / `JoinGameReq{RoomId}`만·PlayerInfo는 인증 때 actor에 적재 / Bingo 전용 EnsurePlayerActor 계약·핸들러 삭제 / room route store를 raw RESP→redis-plus-plus, 레코드를 `{RouteChannelId,OwnerNodeRid,SpotRid,SpotKind}` hash로), Bingo(status 대소문자 `WaitingForPlayers/Running/Finished`, 전송되지 않던 `BingoStateNotify` 제거, self-check 3-client 선인증), SupportChat(client 시나리오를 spec §17 전량으로 확장 — 같은 상담원 2번째 방·방별 MessageSeq 독립·같은 token 재접속 후 재join 상태 보존·명시적 close·closed 대화 재close/전송 오류; 도메인 `close()`에 closed 가드; notify를 `{ConversationId, State}` 중첩으로; client는 Session stream만 사용하고 서버 불변식 검증은 probe로 이관), DeliveryDispatch(`DeliveryStatusChangedReq`에서 미사용 `CustomerId` 제거, bind 응답 node rid로 courier 배치 단언), 공통(샘플이 framework flow 로그에 "message flow" 줄을 직접 쓰던 위조 경로 제거 + runner grep을 실제 framework 로그 기준으로) | 0 | 개별 run_sample.sh 6/6 반복 PASS, `run_samples.sh` `sample all result=passed`(조용한 머신에서 2연속) | 샘플 6종·계약 테스트·러너 diff |
| 2026-07-13 | G5-b | 1708273b4 | 통합 러너 간헐 실패 2건을 **core 결함으로 추적·기록**(ledger §9): (1) CPP-SPOT-SUB-ACT-001 — Bingo `native spot subscription activation failed (errno=95)`. core `update_logical_spot_subscription`이 internal receiver 생성 실패를 원인 불문 `ENOTSUP`으로 덮어써(`spot_node_summary.cpp:264-269`) 실제 원인(`create_spot_sub_with_defaults`의 attachment 생성/`wait_facade_peer` 핸드셰이크가 부하에서 타임아웃)을 은폐하고 일시 오류를 영구 실패로 보고. (2) CPP-AUTOCONNECT-CFG-001 — DeliveryDispatch courier-session이 잡히지 않은 `config_error_t`(errno=22)로 abort. 둘 다 sample 계약 편차가 아니며 core 소유. 진단용 core 패치(errno 보존)는 이 빌드가 core를 **미리 빌드된 패키지**로 소비해 컴파일·검증이 불가하므로 되돌리고 근거만 기록 | 0 | 문서 | ledger §9 |
